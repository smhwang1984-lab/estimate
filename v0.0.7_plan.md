# v0.0.7 작업 계획서 (승인 대기)

> `.agents/AGENTS.md` 원칙에 따라 **구현 전 계획서**입니다. 사용자가 "승인"이라고 명시하기 전까지
> 코드/문서/버전/빌드/설치파일 생성은 진행하지 않습니다.

---

## 0. 사전 점검으로 확인한 사실 (짐작 아님)

| 확인 항목 | 결과 |
|---|---|
| 양식 원본 위치 | `견적_산정\양식\견적용.xlsx` (spec의 datas로 번들 → `_internal\견적용.xlsx`) |
| 기계 시트 구조 | 데이터 행 7~13 (**7행뿐**), 합계 행 14, 푸터 17~19(㈜텍스타 / coment / 날짜 / 생산부장) |
| 기계 시트 병합 | `H6:J6`, `C18:K19`, `P17:W17`, `P18:W18`, `P19:W19` |
| 견적서 시트 구조 | 데이터 행 15~56(42칸), 합계 행 57, `B7 = =기계!P18`(날짜 참조) |
| **견적서 15~21행** | `=기계!$B7` ~ `=기계!$B13` → 정상 (7건까지만) |
| **견적서 22~56행** | 전부 `=기계!#REF!` → **배포 중인 양식 자체가 이미 깨져 있음** |
| openpyxl `insert_rows` 실증 | 셀은 내려가지만 **병합 범위는 그대로 남음**(3.1.5에서 직접 재현). 푸터가 어긋남 |
| 결과적으로 | 8건째부터 기계 푸터가 밀려 `=기계!P18`(날짜)이 빈 칸을 가리키고, 견적서는 `#REF!` |
| 기계 print_area | `$A$1:$W$19` 고정 → 행이 늘어도 인쇄 영역이 안 늘어남 |
| 견적서 print_area | **없음** + 시트 max_row가 1,048,567 (C1048567:D1048567 유령 병합) → PDF 뽑으면 수천 페이지 |
| Excel COM | 이 PC에서 `Excel.Application 16.0` 정상 생성 확인 |
| 실사용 파일 대조 | `NT3611415 외 41건(26.07.02).xlsx`은 견적서 42행 전부 `=기계!$B7`~`$B48`로 연속 참조 → **사용자의 정상 문서 형태는 "수식 링크"** |

---

## 1. 출력 데이터 (v0.0.7.md 출력 1~4번) — 핵심 구조 변경

### 문제의 뿌리

지금은 항목이 늘면 `ws.insert_rows(합계행)`으로 한 줄씩 밀어 넣습니다. 이 방식은
① 병합을 못 따라가고 ② 견적서 참조를 못 고치고 ③ 인쇄 영역을 못 늘립니다.
요청 1~4번은 전부 같은 뿌리라서, 패치 4개가 아니라 **배치(layout) 방식 1개**로 바꿉니다.

### 바꿀 방식 — "블록 이동 + 참조 재생성"

`core/excel_io.py`에 아래 기본 동작을 새로 만듭니다.

1. **`shift_block_down(ws, from_row, delta)`**
   - 지정 행 이하의 **셀 값·서식·행 높이·병합 범위**를 함께 delta만큼 내립니다.
   - 병합 범위를 직접 재생성하는 것이 핵심입니다(openpyxl이 안 해 주는 부분).
2. **`layout_machine_sheet(ws, need_rows)`**
   - 현재 합계 행과 그 아래 푸터 블록을 찾고, 필요한 데이터 행 수만큼 부족분을 계산해
     푸터+합계를 통째로 내린 뒤, 새 데이터 행에 7행 서식을 복사합니다.
   - 합계: `=SUM(W7:W{마지막데이터행})` 재작성 (요청 3번의 "sum이 되게").
   - `print_area`를 `$A$1:$W${푸터마지막행}`으로 갱신.
3. **`sync_estimate_sheet(wb)`** — 요청 4번(양식 1번 시트 견적서 동일 적용)
   - 기계 시트의 실제 데이터 행 개수 M을 읽어 견적서를 다시 맞춥니다.
   - 데이터 행 `15 ~ 14+M`에 `B=ROW()-14`, `C==기계!$B{r}`, `E==기계!$C{r}`, `G==기계!$W{r}` 재생성
     → **`#REF!` 42칸이 이때 전부 정상 수식으로 덮여 사라집니다.**
   - M이 42칸을 넘으면 견적서도 같은 `shift_block_down`으로 합계 행 아래를 내려 칸을 늘립니다.
   - M이 42보다 적으면 남는 칸은 **빈 칸으로 지웁니다**(안 지우면 0원 행이 찍힘).
   - 합계 `G{합계행} = =기계!$W{기계합계행}`, `B7 = =기계!P{푸터 날짜 행}` 재지정.
   - 견적서 `print_area`를 `$B$1:$H${합계행}`으로 **신규 지정**(유령 1,048,567행 무력화).
4. 값이 아니라 **수식 링크를 유지**합니다. 사용자의 실제 정상 문서(`NT3611415 외 41건`)가
   그 형태이고, 나중에 엑셀에서 기계 시트를 고치면 견적서가 따라 움직여야 하기 때문입니다.

### 부수 정리

- `find_next_input_row()`의 `insert_rows` 경로 제거 → 위 배치 함수로 대체.
- 누적 저장은 지금처럼 **기존 파일을 열어 이어 붙이는 방식을 유지**합니다(사용자가 엑셀에서
  직접 손댄 내용을 날리지 않기 위함). 배치 함수는 "필요한 만큼만 늘리는" 동작이라 안전합니다.
- Size 왕복 불일치 정리: 읽을 때 H/I/J를 `" x "`로 합치는데 쓸 때는 H 한 칸에만 넣습니다.
  → I/J에 값이 있을 때만 합치도록 바꿔 왕복 시 표기가 변형되지 않게 합니다.

### 검증 방법 (구현 후 실행)

- 7건 / 8건 / 43건 / 60건 4가지로 내보내고 재열람하여
  ① 기계 합계 SUM 범위 ② 푸터 병합 위치 ③ 견적서 M행 참조 ④ 견적서 합계 ⑤ 인쇄 영역
  다섯 가지를 **매 케이스 직접 읽어 확인**합니다.
- Excel COM으로 실제 재계산시켜 견적서 금액이 기계 시트 최종단가 합과 일치하는지 대조합니다.

---

## 2. 출력 형식 선택 / PDF (v0.0.7.md 출력 5번)

- "다운로드" 시 **출력 형식 선택창**을 띄웁니다: `엑셀(.xlsx)` / `PDF` / `둘 다`.
- PDF 생성은 **Excel COM(`ExportAsFixedFormat`)** 으로 합니다.
  - 이유: openpyxl은 수식만 쓰고 계산 결과를 안 넣습니다. 엑셀을 거치지 않는 PDF 경로
    (reportlab 등)는 모든 합계·최종단가가 **빈칸이나 0으로 찍힙니다.** 엑셀이 열면서
    재계산하므로 COM 방식만 숫자가 맞습니다.
  - 구현은 **PowerShell 경유 COM 호출**(subprocess)로 해서 `pywin32` 의존성을 안 넣습니다.
    → 배포 용량 증가 0MB (v0.0.5의 "프로그램이 무거워" 방침 유지).
  - 엑셀이 없는 PC에서는 "PDF는 Microsoft Excel이 설치된 PC에서만 가능합니다" 안내 후
    엑셀 파일만 저장합니다(기능 자체가 죽지 않게).
- 인쇄 영역·가로/세로·1페이지 맞춤을 위 1번 배치 단계에서 같이 정리합니다.

---

## 3. UI 개선

### 3-1. 다크 팔레트 깊이 (v0.0.7.md UI 1번)

현재 문제(실측): `bg`와 `row_bg`가 **둘 다 `#11161c`로 완전히 동일**해서 표 본문이 배경에
붙어 보이고, `panel #171d25` / `card #18212b`도 차이가 3~4단위라 층이 안 보입니다.

제안 팔레트(다크모드 유지, 층만 분리):

| 키 | 현재 | 제안 | 역할 |
|---|---|---|---|
| `bg` | #11161c | **#0d1218** | 가장 깊은 바닥 |
| `row_bg` | #11161c | **#141b23** | 표 본문 (바닥에서 뜸) |
| `row_alt_bg` | – | **#171f28** (신규) | 짝수 행 줄무늬 |
| `panel` | #171d25 | **#1b232d** | 상단/검색 패널 |
| `card` | #18212b | **#1f2833** | 입력면 |
| `line` | #2a3340 | **#333f4e** | 구분선 |

- 헤더 아래·패널 경계에 1px 밝은 선을 넣어 층 경계를 눈에 보이게 합니다.
- 짝수 행 줄무늬(zebra)를 넣어 표의 깊이를 만듭니다.
- **모든 신규 키는 `theme.json`과 `core/config.py`의 `DEFAULT_COLORS` 양쪽에 넣습니다.**
  (`_merged()`가 기본값에 없는 키를 조용히 버리므로 — v0.0.6에서 이미 겪은 함정)

### 3-2. 체크박스 깜빡임 (v0.0.7.md UI 2번)

원인: `toggle_item_selection()` → `refresh_table()` 이 **표 전체를 destroy 후 재생성**합니다.
40행 × 위젯 13개 = 500개 이상을 매 클릭마다 지웠다 다시 그립니다.

- `self.row_widgets[no]`에 행별 위젯(행 프레임·위/아래 선·체크박스)을 등록합니다.
- 체크 토글 시 **해당 행의 색만 다시 칠하고** 요약줄(선택 N건/총 견적금액)만 갱신합니다.
- `전체 선택` / `선택 해제`도 재생성 없이 등록된 행들의 색만 바꿉니다.
- 스크롤 위치가 튀지 않도록 `yview` 좌표를 유지합니다.
- 표 재생성이 정말 필요한 경우(검색·업로드·저장)에만 기존 `refresh_table`을 씁니다.

### 3-3. 소재 스펙·사이즈 표기 (v0.0.7.md UI 3번)

- 표 열 구성: `체크 / 기종 / 품번 / 품명 / 소재(Material) / 사이즈(Size) / 가능여부 / 최종단가`
- 폭 재배분(신규 키 `col_material_width` 130, `col_size_width` 200, 기종 200→160, 품번 260→220)
- 최소 창 너비 `min_width` 1180 → **1320** (열 2개가 늘어 기존 폭으로는 잘림)
- 신규 키는 `theme.json` + `config.DEFAULT_LAYOUT` 양쪽에 추가.

---

## 4. 버전 / 빌드

- `estimate_app/__init__.py` `APP_VERSION = "0.0.7"`
- `installer/Setup.iss` 버전 0.0.7 (**양쪽 모두** — v0.0.6 기록의 관례)
- `build.bat setup` 으로 `dist\Estimate\` + `installer\Output\MachineEstimate_Setup_v0.0.7.exe`
- 작업 종료 후 `ver_plan.md`에 v0.0.7 항목 추가(기존 문서 관례대로 요청/반영/검증/미실행 4단락)

---

## 5. 확정된 결정 사항 (사용자 선택 완료)

| 항목 | 결정 |
|---|---|
| 출력 형식 | **엑셀 / PDF / 둘 다 선택창** |
| PDF 대상 시트 | **견적서 시트만** (기계 시트의 공정별 단가는 PDF에 안 나감) |
| 다크 팔레트 | **3-1 제안대로** (층 분리 + 짝수 행 줄무늬) |
| 기종(model) 열 | **기계 시트 B열에 삽입** |
| 양식 원본 `#REF!` | **같이 수리** (수정 전 `.bak` 백업) |

### 5-1. 기종 열 삽입에 따른 양식 재구성 (`견적_산정\양식\견적용.xlsx`)

수정 전 `견적용.xlsx.bak_20260808` 백업을 먼저 만듭니다.

**기계 시트 열 변경 (B에 1칸 삽입 → 전부 한 칸씩 이동)**

| | 현재 | 변경 후 |
|---|---|---|
| NO | A | A |
| **기종** | (없음) | **B (신규)** |
| 품번 / 품명 / Coment | B / C / D | C / D / E |
| 가능여부 / Qty / Material | E / F / G | F / G / H |
| Size (헤더 병합) | H~J | I~K |
| 공정 10종 (5축~프로그램) | K~T | L~U |
| SUM / 단가 / 최종단가 | U / V / W | V / W / X |
| 단가 행(5행) | K5~T5 | L5~U5 |
| 합계 행(14행) | V14 라벨 / W14 SUM | W14 라벨 / X14 SUM |
| 푸터 병합 | P17:W17, P18:W18, P19:W19, C18:K19 | Q17:X17, Q18:X18, Q19:X19, D18:L19 |
| print_area | `$A$1:$W$19` | `$A$1:$X$19` |

openpyxl의 `insert_cols`는 **수식과 병합을 안 고칩니다.** 따라서 7~13행의
`SUM`/단가/`ROUNDDOWN` 수식, 합계 SUM, 병합 4개, 인쇄 영역을 **전부 직접 다시 씁니다.**

**견적서 시트 참조 변경**

| 셀 | 현재 | 변경 후 |
|---|---|---|
| B7 (날짜) | `=기계!P18` | `=기계!Q18` |
| C15.. (품번) | `=기계!$B7` | `=기계!$C7` |
| E15.. (품명) | `=기계!$C7` | `=기계!$D7` |
| G15.. (단가) | `=기계!$W7` | `=기계!$X7` |
| 22~56행 | `=기계!#REF!` | **정상 연속 참조로 복구** |
| G57 (합계) | `=기계!$W14` | `=기계!$X14` |

견적서에 기종 열을 새로 만들지는 **않습니다**(거래처 제출 양식이라 요청에 없음).
기종은 기계 시트에만 기록됩니다.

### 5-2. 기존 파일 호환 — 열 위치를 헤더에서 읽어 판단

기존 `견적_산정` 폴더의 실제 파일들은 **양식이 서로 다릅니다.**
(예: `NT3611415 외 41건`은 최종단가가 W가 아니라 V열)
지금 코드처럼 열 번호를 고정하면 새 양식 도입 시 기존 파일 업로드가 깨집니다.

- `resolve_columns(ws)` 신규 — 6행 헤더 문구(`품번`/`품명`/`Coment`/`가능여부`/`Qty`/
  `Material`/`Size`/`최종단가`/`기종`)를 읽어 열 위치를 **파일마다 스스로 판단**합니다.
- 업로드 읽기와 누적 저장 쓰기가 같은 판단 결과를 씁니다.
  → 오늘 누적 파일이 구버전 양식이면 **그 파일의 양식 그대로** 이어 씁니다(깨뜨리지 않음).
- 헤더를 못 찾으면 새 양식 기준으로 처리하고 안내 문구를 띄웁니다.

### 5-3. 같이 고칠 기존 결함 (`excel_row` 날짜 넘김)

- 어제 저장한 항목이 `excel_row=9`를 들고 있으면, 오늘 새로 만들어진 파일(빈 양식)에도
  9행에 써 버려 **7·8행이 비고 다른 항목을 덮어쓸 수 있습니다.**
- 항목에 `excel_file`을 같이 저장해, **대상 파일이 다르면 행 번호를 새로 배정**하도록 고칩니다.

---

## 6. 작업 순서

1. 양식 원본 백업(`.bak_20260808`) → 기종 열 삽입 + 수식/병합/인쇄영역 재작성 + `#REF!` 복구
2. 고친 양식을 다시 읽어 **모든 수식·병합·참조를 한 칸씩 대조 확인**
3. `resolve_columns` (헤더 기반 열 판단) + 기존 실사용 파일 5개 이상 업로드 재현 확인
4. `excel_io` 배치 함수(`shift_block_down` / `layout_machine_sheet` / `sync_estimate_sheet`) 신규
   + 저장·내보내기 경로 교체 + `excel_row` 파일 귀속 수정
5. 7건 / 8건 / 43건 / 60건 케이스로 출력 검증 (수식·병합·인쇄영역·합계 직접 읽어 확인)
6. 출력 형식 선택창 + Excel COM PDF(견적서 시트) 생성 및 실제 PDF 페이지 수 확인
7. 팔레트 깊이 + 줄무늬 (`theme.json` / `config.py` **양쪽 동시** 반영)
8. 행 부분 갱신(깜빡임 제거) + 스크롤 위치 유지
9. 소재/사이즈 열 추가 + 폭 재배분 (`min_width` 1320)
10. 실행 화면 캡처로 육안 확인
11. 버전 0.0.7 반영(`__init__.py` + `Setup.iss` **양쪽**) → 빌드 → 설치 파일 생성
12. `ver_plan.md` 기록 및 보고

## 7. 위험 요소 (미리 알려 두는 것)

- **양식 구조 변경은 되돌리기 번거로운 작업입니다.** 백업 파일(`.bak_20260808`)을 남기지만,
  이미 만들어진 기존 `견적_산정` 폴더의 과거 파일들은 **건드리지 않습니다**(구버전 양식 그대로 유지).
- PDF는 이 PC에 Excel 16.0이 있어 동작하지만, **Excel이 없는 PC에서는 PDF 생성이 안 됩니다.**
  그 경우 엑셀 파일만 저장하고 안내 문구를 띄웁니다.
- 실제 설치(관리자 권한)를 통한 업그레이드 설치 검증은 이전 버전들과 마찬가지로
  이 세션에서 수행할 수 없습니다. 빌드·설치파일 생성까지만 확인합니다.
