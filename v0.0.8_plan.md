# v0.0.8 작업 계획서 (승인 대기)

> `.agents/AGENTS.md` 원칙에 따라 **구현 전 계획서**입니다. 사용자가 "승인"이라고 명시하기 전까지
> 코드/문서/버전/빌드/설치파일 생성은 진행하지 않습니다.
>
> 이번 요청의 강조점은 **① UI 개선 ② 프로그램 운영의 가벼움** 두 가지입니다.
> 그래서 6번(UI)과 7번(가벼움)을 가장 두껍게 잡았고, 두 항목 모두 **실측한 숫자를 근거로** 적었습니다.

---

## 0. 사전 점검으로 확인한 사실 (짐작 아님)

전부 이 PC에서 직접 실행해 얻은 결과입니다. 추측으로 적은 항목은 없습니다.

### 0-1. 엑셀 양식 구조

| 확인 항목 | 결과 |
|---|---|
| 양식 위치 | `견적_산정\양식\견적용.xlsx` (시트 `견적서` / `기계` / `Sheet1`) |
| 기계 시트 | 데이터 행 7~13, 합계 행 14, 푸터 17~19, `print_area = $A$1:$X$19` |
| 기계 시트 병합 | `I6:K6`(Size 헤더), `Q17:X17`, `Q18:X18`, `Q19:X19`, `D18:L19` |
| **Size 열** | 헤더만 `I6:K6`으로 3칸 병합. **데이터 행(7~13)에는 병합이 전혀 없음** |
| **Size 값이 들어가는 곳** | `_write_item_values()`가 `I`열 한 칸에만 씀. I열 폭은 **7.75** (J 9.125 / K 8.75) |
| → 사용자가 본 증상 | 값은 I칸에만 있고 J·K는 빈 칸이라 **한 항목의 사이즈가 3열로 쪼개져 보인다** |
| 견적서 시트 | 데이터 행 15~56(42칸), 합계 행 57(`B57=' 합 계 '`), `print_area = $B$1:$H$57` |
| 견적서 병합 | 행마다 `C:D`(품번) / `E:F`(품명) / `G:H`(단가) 3개씩, 총 136개 |
| v0.0.7의 `#REF!` 수리 | **정상 반영 확인**. 15~56행 전부 `=기계!$C7`~`$C48` 연속 참조로 복구돼 있음 |

### 0-2. PDF — 5가지 방식으로 재현 시도, **전부 정상 생성됨**

| 시험 조건 | 결과 |
|---|---|
| 소스 실행(콘솔 있음) | `ok=True`, PDF 1페이지, 62,146바이트 |
| `pythonw` (핸들 상속) | `ok=True` |
| `pythonw` 완전 분리 (`stdin=None stdout=None`) | `ok=True` |
| **PyInstaller frozen exe** (`console=False`, 동일 excludes) | `ok=True`, `ps1` 경로 `_internal\assets\`에서 정상 발견, PowerShell 5.1.26100.8875 |
| 한글 파일명 + 공백 포함 폴더 | `ok=True` (`선택견적_2026-08-08.pdf`, `출력 시험 폴더\`) |
| 설치본 점검 | `C:\Program Files (x86)\Estimate\_internal\assets\export_pdf.ps1` **존재**(920바이트, 소스와 동일) |

Excel COM으로 재계산해 값도 대조했습니다 → 3건 × 135,000원, 기계 합계 405,000원,
견적서 합계 ₩405,000. **금액 계산은 정상입니다.**

**결론: PDF 실패를 이 PC에서 재현할 수 없습니다.** 그런데 사용자 답변은
*"'PDF로 만들지 못했습니다' 창이 떴다"* 입니다. 즉 **실패는 실제로 일어났고**,
`export_pdf.ps1`이 0이 아닌 종료코드를 냈다는 뜻입니다.
지금 코드는 그 사유를 **화면에 한 번 띄우고 그대로 버립니다**(`detail[:300]`).
그래서 원인을 특정할 근거가 남아 있지 않습니다. → 2번에서 이 점을 최우선으로 고칩니다.

### 0-3. PDF에서 실제로 발견한 결함 (재현 완료)

Excel COM으로 산출물을 읽어 확인한 견적서 시트:

```
row 15 : [2]='1' [3]='PN-001' [5]='테스트품명1' [7]='₩135,000 '
row 16 : [2]='2' [3]='PN-002' [5]='테스트품명2' [7]='₩135,000 '
row 17 : [2]='3' [3]='PN-003' [5]='테스트품명3' [7]='₩135,000 '
row 18 : [2]='4' [3]='0'      [5]='0'          [7]='₩0 '        <-- 쓰레기 행
```

원인이 정확히 특정됩니다. `sync_estimate_sheet()`의 판정식이

```python
has_data = src_row < gigye_summary_row      # 3건인데 gigye_summary_row = 14
```

**"실제로 쓴 항목 수"가 아니라 "양식의 칸 수"를 기준으로 삼습니다.**
기계 시트 10~13행은 비어 있는데도 견적서 18~21행이 그 빈 칸을 참조하고,
엑셀은 빈 셀 참조를 `0`으로 표시합니다. → **거래처에 나가는 PDF에 `0 / 0 / ₩0` 행이 4줄 찍힙니다.**
PDF 안의 텍스트를 직접 뽑아 봐도 `(PN)(-)(0)(0)(2)(0)(0)(0)(0)(0)(0)(0)`으로 그 0들이 들어 있습니다.

### 0-4. 검색 이상 증상 — 원인 2개 모두 확정

`refresh_table()`에는 스크롤 위치를 되돌리는 코드가 없습니다(`yview_moveto` 전체 검색 결과 0건).
스크롤을 내린 뒤 검색해 실측했습니다.

```
검색 전(맨 위)  yview: (0.0,   0.4228)
아래로 내린 뒤   yview: (0.5772, 1.0)
검색 직후       yview: (0.5772, 1.0)    <-- 그대로. 결함 확정
```

→ v0.0.8.md의 **"창이 맨 위에 위치한 상태에서 검색해야 함"(2번)** 과
**"검색 데이터들이 중간에 위치함"(3번)** 은 서로 다른 문제가 아니라 **같은 원인 하나**입니다.
검색해도 스크롤이 내려간 채라 결과가 화면 중간부터 보입니다.

### 0-5. 화면이 무거운 원인 — 전부 표 그리기 한 곳

같은 프로세스에서 5회 반복 측정한 중간값입니다(33건 기준).

| 구간 | 시간 |
|---|---|
| `Theme()` 생성 | 0.001s |
| `theme.apply()` | 0.001s |
| `session.load()` (25.7KB / 33건) | 0.002s |
| `build_dashboard()` | 0.111s |
| **`EstimateApp.__init__` 전체** | **0.114s** |
| `openpyxl` import (이미 지연 로딩됨) | 0.550s |
| **첫 표 그리기 (`refresh_table` + 화면 반영)** | **3.886s** |
| ↳ 위젯 destroy + 생성만 (배치 계산 제외) | 1.136s |
| ↳ 나머지(Tk 배치 계산 + 실제 그리기) | 약 2.75s |
| 검색 1회 (표 전체 재생성) | 0.664s |
| 표 본문 위젯 수 | **529개 / 33행 = 행당 16개** |
| [참고 측정] 기존 위젯을 지우지 않고 **텍스트만** 교체 | 0.152s |

**핵심: 프로그램의 무거움은 시작 코드(0.114s)가 아니라 표를 매번 통째로 다시 그리는 데
전부 몰려 있습니다.** 위젯을 destroy/재생성하지 않는 방향이 옳다는 것은 확실합니다.

> **0.152s의 측정 범위를 분명히 해 둡니다(과장하지 않기 위해).**
> 이 값은 라벨 198개에 **같은 폭의 문자열**을 넣고 `update()`한 시간입니다.
> 실제 7-1의 행 갱신은 여기에 더해 ① 행이 담당하는 항목 번호 교체 ② 줄무늬·선택 배경 재도색
> ③ NEW 배지 표시/제거(위젯 생성·삭제가 실제로 일어남) ④ 가능여부 배지의 3색 전환
> ⑤ hover 대상 목록 재구성 ⑥ 남는 행 `grid_remove()` 를 해야 하고, **문자열 폭이 매번 다릅니다.**
> 폭이 달라지면 Tk의 배치 계산이 다시 도는데, 그 배치 계산이 3.886s 중 약 2.75s였습니다.
> 따라서 0.152s는 **하한선(참고값)이고, 약속하는 값이 아닙니다.**
> 7번의 목표는 **첫 표 그리기 1.0s 미만 / 검색 1회 0.2s 미만**으로 잡고,
> 구현 후 같은 측정 코드로 실제 값을 재서 보고합니다.

### 0-6. 명칭 / 아이콘 / 배포

| 확인 항목 | 결과 |
|---|---|
| `APP_TITLE` | `"기계 시트 표준 견적 입력 시스템"` (`estimate_app/__init__.py`) |
| `Setup.iss` `MyAppName` | 같은 문구 |
| 아이콘 | `Machine_Estimate.spec`에 `icon=` **없음**, 코드에 `iconbitmap` **없음** → 파이썬 기본 아이콘 |
| ESC 바인딩 | `<Escape>` 검색 결과 **0건** (팝업·본화면 모두 없음) |
| 배포 규모 | `dist\Estimate` **22MB / 947 파일**, 설치 파일 9.59MB |
| 설치 위치 | `C:\Program Files (x86)\Estimate` (32비트 `{autopf}`) |

### 0-7. Size 열 통합안 — 시제품으로 왕복 검증 완료

3건 / 7건 / 12건 세 경우로 내보낸 뒤 데이터 행에 `I:K` 병합 + 가운데 정렬을 적용하고
**다시 읽어 들여** 확인했습니다.

| 건수 | 합계 행 | 다시 읽은 건수 | Size 왕복 | 데이터 행 병합 | 정렬 | print_area |
|---|---|---|---|---|---|---|
| 3 | 14 | 3 | 원문 그대로 | `I7:K7`~`I13:K13` | center | `$A$1:$X$19` |
| 7 | 14 | 7 | 원문 그대로 | `I7:K7`~`I13:K13` | center | `$A$1:$X$19` |
| 12 | **19** | 12 | 원문 그대로 | `I7:K7`~`I18:K18` | center | **`$A$1:$X$24`** |

읽기 경로가 `I·J·K`를 `" x "`로 합치는데 병합 후 J·K가 `None`이 되어 **I값만 그대로 나옵니다.**
12건(합계 행이 19로 밀린 경우)에서도 병합·인쇄영역이 정상 확장됨을 확인했습니다.

---

## 1. 산출에러 1 — 사이즈 열 3열 통합 + 가운데 정렬

**바꿀 것**

- `excel_io`에 `merge_size_cell(ws, row, cols)` 신규: Size 헤더의 병합 폭(`_merge_span`)만큼
  그 데이터 행도 병합하고 `Alignment(horizontal="center", vertical="center")`를 준다.
- 호출 지점 **세 곳 모두**에 넣는다. 한 곳만 넣으면 칸이 늘어난 파일에서 병합이 빠진다.
  1. `write_items_to_sheet()` — 항목을 쓰는 행
  2. `layout_machine_sheet()` — 합계 행을 밀어 **새로 만든 데이터 행** (`copy_row_style`/
     `apply_row_formulas`는 병합을 만들지 않으므로 여기서 빠지면 12건 이상에서 어긋난다)
  3. 이미 병합돼 있으면 다시 병합하지 않도록 확인 후 처리 (openpyxl은 중복 병합 시 오류)
- 헤더 폭이 1칸인 구버전 양식(`resolve_columns`가 판단)에서는 **아무것도 하지 않는다.**
  0-7에서 확인한 대로 폭을 헤더에서 읽어 결정하므로 기존 파일을 깨지 않는다.

**검증 방법** — 0-7과 같은 3 / 7 / 12 / 43건 네 경우로 내보내고, 매 경우
① 데이터 행 병합 범위 ② 정렬값 ③ 다시 읽었을 때 Size 문자열 ④ print_area를 직접 읽어 대조한다.

---

## 2. 산출에러 2 — PDF

0-2에서 확인한 대로 **재현이 안 됩니다.** 그래서 "원인을 찍어서 고친다"가 아니라
**① 다음에 실패하면 원인이 남게 만든다 ② 실패해도 사용자가 빈손이 되지 않게 한다
③ 재현 안 되는 원인 중 실제로 흔한 것들을 미리 막는다** 세 갈래로 갑니다.

### 2-1. 실패 사유를 남긴다 (가장 급한 것)

지금은 `messagebox` 한 번 띄우고 사유가 사라져, 다음에 같은 일이 나도 알 수 없습니다.

- `%LOCALAPPDATA%\MachineEstimate\pdf_error.log`에 **시각 / xlsx 경로 / pdf 경로 /
  반환코드 / stdout / stderr 전문**을 덧붙여 기록한다.
- 오류창 문구에 **로그 파일 경로를 함께 표시**한다 → 다음 보고 때 그 파일만 보면 원인이 특정된다.
- 잘라내던 `[:300]`을 늘려(전문은 로그, 화면은 1000자) 사유가 중간에 끊기지 않게 한다.

### 2-2. 실패해도 빈손이 되지 않게 한다 (실사용상 가장 아픈 부분)

현재 `dashboard.export_items()`는 **'PDF'만 고른 경우 xlsx를 임시 폴더에 만들고
`finally`에서 지웁니다.** 그래서 PDF가 실패하면 **결과물이 하나도 남지 않습니다.**
사용자가 "출력이 안 됨"이라고 표현한 상태가 바로 이것입니다.

- PDF 실패 시 **"엑셀 파일로라도 저장할까요?"를 묻고**, 예를 누르면 임시 xlsx를
  사용자가 고른 위치에 `.xlsx`로 옮겨 준다(임시 폴더 삭제 전에 처리).
- 완료창에 저장된 파일의 **전체 경로**를 계속 표시한다(이미 하고 있음 — 유지).

### 2-3. 흔한 실패 원인을 미리 막는다

재현이 안 되니 "이게 원인이다"라고 쓰지 않습니다. 다만 아래는 현장 PC에서 실제로 잘 생기는
조건이고, 막는 비용이 거의 없어 같이 넣습니다.

| 막을 조건 | 조치 |
|---|---|
| 그룹 정책(GPO)으로 PowerShell **스크립트 파일 실행이 차단**된 경우 (`-ExecutionPolicy Bypass`는 정책보다 약할 수 있음) | `-File`이 아니라 **`-EncodedCommand`**(Base64/UTF-16LE)로 넘긴다. 스크립트 파일 실행 정책의 적용 대상에서 벗어난다 |
| Excel 첫 실행 마법사/라이선스 동의창이 떠서 COM이 멈춤 | `$excel.Interactive = $false`, `$excel.EnableEvents = $false`, `$excel.AskToUpdateLinks = $false`, `$excel.AutomationSecurity = 3` 지정 |
| 링크 업데이트 확인창 | `Workbooks.Open($path, 0, $true)` — UpdateLinks=0, ReadOnly=true 명시 |
| Excel 콜드 스타트가 90초를 넘김 | 제한을 **180초**로 올리고, 시간 초과 시 오류 문구에 "180초 안에 응답하지 않았습니다"를 명시해 *Excel 없음*과 *느림*을 구분할 수 있게 한다 |
| Excel 자체가 없는 PC | COM 생성 실패를 별도 메시지로 구분해 "Microsoft Excel이 설치된 PC에서만 가능합니다"로 안내(현재 문구 유지) |
| `$excel`이 null일 때 `finally`가 2차 오류를 내며 원래 사유를 덮음 | `finally`에서 null 검사 후 정리 |
| **`.ps1` 파일 자체의 인코딩** (아래 확인 사실) | `-EncodedCommand`로 바꾸면 파일을 읽지 않으므로 위험이 사라짐 |

**확인한 사실 (인코딩)** — `export_pdf.ps1`은 **BOM 없는 UTF-8**인데 한글 주석이 들어 있습니다.
PowerShell 5.1은 BOM 없는 `.ps1`을 UTF-8이 아니라 **시스템 ANSI 코드페이지로 읽습니다.**
실제로 그 바이트를 코드페이지별로 디코드해 봤더니 `cp949`·`cp1252`·`cp1251`은 **디코드 실패**,
`cp437`은 주석 줄에 `"` 같은 문자를 새로 만들어 냅니다.
다만 그 문자는 전부 `#` 뒤(주석 안)에 있고 PowerShell 주석은 줄 끝에서 끝나므로,
**이것이 지금 증상의 원인이라는 근거는 없습니다.** 원인이라고 적지 않겠습니다.
잠재 취약점이고 `-EncodedCommand`로 바꾸면 함께 사라지므로 같이 정리하는 것입니다.

> **회귀 검증 (반드시 함께 수행)** — 2-3의 변경은 **지금 정상 동작하는 경로의 동작 방식을
> 바꿉니다**(`-File` → `-EncodedCommand`, COM 속성 4개 추가, `Workbooks.Open` 인자 명시).
> GPO 차단 상황은 이 PC에서 확인할 수 없지만, **되던 것을 깨뜨리지 않았음은 확인할 수 있습니다.**
> 0-2의 **5가지 재현을 변경 후 전부 다시 실행**합니다
> (소스 / `pythonw` / 완전분리 / **frozen exe 재빌드** / 한글경로+공백폴더).
> Base64/UTF-16LE 인코딩을 미묘하게 틀리거나 `AutomationSecurity = 3`이 파일을 거부하면,
> **재현 안 되던 실패를 재현되는 실패로 바꾸는 것**이 됩니다.

### 2-4. 견적서 `0 / 0 / ₩0` 쓰레기 행 제거 (0-3에서 재현 완료)

- `sync_estimate_sheet()`의 판정을 **`row_has_input_data(gigye_ws, src_row, cols)` 하나로** 바꾼다.
  기계 시트의 그 행에 실제로 값이 있을 때만 참조를 넣고, 없으면 비운다.
- 나머지 행은 `.value = None`으로 확실히 비운다
  (`ws.cell(..., value=None)`은 지우기가 아니라는 점은 코드 주석에 이미 기록돼 있음 — 그대로 지킨다).
- **판정 수단은 하나만 둔다.** "이번에 쓴 행 번호 집합"을 `write_items_to_sheet()`에서
  넘겨받는 방식도 가능하지만, 그러면 판정 기준이 둘이 되어 **서로 어긋날 수 있습니다.**
  누적 저장은 기존 파일에 이어 쓰므로 *이번 호출에 없지만 값은 있는 행*(어제/오늘 앞서 저장한 항목)이
  정상적으로 존재하고, 그 행도 견적서에 나와야 합니다. 시트를 직접 보는 `row_has_input_data`가
  **누적 저장과 선택 다운로드 두 경로에서 모두 옳은 유일한 기준**입니다.
  → 집합을 넘기는 방식은 채택하지 않습니다.

**검증 방법** — 3건 / 7건 / 8건 / 43건으로 내보내 **Excel COM으로 재계산한 뒤**
견적서 15행~합계행을 한 줄씩 읽어 ① `0` 행이 하나도 없는지 ② 합계가 기계 시트 최종단가 합과
일치하는지 ③ PDF 페이지 수가 1페이지인지를 매 경우 확인한다.

---

## 3. ESC키 적용

| 요청 | 조치 |
|---|---|
| 카드 팝업에서 ESC로 닫기 | `popup.bind("<Escape>", ...)`. **단, 입력값이 바뀐 상태면** "저장하지 않고 닫을까요?"를 한 번 묻는다. 아무것도 안 바꿨으면 바로 닫힌다 |
| 체크박스 선택 후 ESC로 선택 취소 | 본 화면에 `<Escape>` → `clear_selection()`. 0-5의 개선(부분 갱신)으로 표를 다시 그리지 않고 선택 표시만 지운다 |

- 팝업은 `grab_set()` 상태라 ESC가 팝업으로만 갑니다 → 두 동작이 서로 간섭하지 않습니다.
- 값 변경 여부는 팝업을 열 때의 입력값 스냅샷과 현재 값을 비교해 판단합니다.
- **왜 그냥 닫지 않는가**: 공수 시간을 열 칸 넘게 입력한 뒤 ESC를 잘못 누르면 전부 사라집니다.
  ESC 한 번으로 닫히는 편의(요청 그대로)는 지키면서 사고만 막는 선택입니다.

---

## 4. 검색 후 이상 증상 (3가지 → 원인 2개)

| 요청 | 원인 | 조치 |
|---|---|---|
| 1. 카드가 하나하나 깜빡인 뒤 검색됨 | `refresh_table()`이 529개 위젯을 **destroy 후 재생성**(0.664s). 행이 하나씩 다시 그려져 깜빡임으로 보인다 | 7-1의 **행 위젯 재사용**으로 해결. 위젯을 지우지 않고 텍스트·색만 교체 (목표 0.2s 미만) |
| 2. 창이 맨 위에 있어야 검색됨 | 0-4에서 확정. 검색 후 `yview`가 `(0.5772, 1.0)` 그대로 | 표를 갱신한 뒤 `row_canvas.yview_moveto(0)` |
| 3. 검색 데이터들이 중간에 위치함 | **2번과 같은 원인** (스크롤이 내려간 채라 결과가 중간부터 보임) | 위와 동일 |

- 스크롤 되돌림은 **검색·검색초기화·업로드처럼 목록의 내용이 바뀔 때만** 합니다.
  "더 보기"나 체크 토글에서 맨 위로 튀면 그게 더 불편합니다.
- 갱신 중 화면 떨림을 없애기 위해 표 갱신 전후로 그리기를 한 번만 반영하도록 묶습니다.

**검증 방법** — 아래로 스크롤한 뒤 검색해 `yview()`가 `(0.0, …)`이 되는지 실측하고,
검색 1회 소요 시간을 0-5와 같은 방법으로 다시 재서 0.664s → 0.2s 미만인지 확인한다.

---

## 5. 프로그램 명칭 변경 / 아이콘

| 대상 | 현재 | 변경 후 |
|---|---|---|
| `estimate_app/__init__.py` `APP_TITLE` | 기계 시트 표준 견적 입력 시스템 | **`Estimate(견적)`** |
| `installer/Setup.iss` `MyAppName` | 같은 문구 | **`Estimate(견적)`** |
| 창 제목 | `{APP_TITLE} v{APP_VERSION}` | `Estimate(견적) v0.0.8` (형식 유지) |

- **양쪽 모두 고칩니다.** 한쪽만 고쳐 어긋났던 전례가 v0.0.6 기록에 있습니다.
- `Setup.iss`의 `AppId`는 **건드리지 않습니다** — 같은 설치로 인식돼야 업그레이드가 됩니다.
- 시작 메뉴/바탕화면 아이콘 이름도 `MyAppName`을 따라 자동으로 바뀝니다.
- 화면 안에 남은 옛 명칭 문구(상단 제목 등)도 같이 확인해 정리합니다.

**아이콘** (사용자 선택: 직접 제작)

- 새 팔레트의 보라→하늘 그라데이션에 흰 `E` 마크를 얹은 `.ico`를 **외부 라이브러리 없이**
  직접 생성합니다(BMP 기반 ICO 구조를 코드로 씀). **PIL을 추가하지 않으므로 배포 용량 증가 0MB**입니다.
- 16 / 32 / 48 / 256 px 네 크기를 담습니다(작업표시줄·바탕화면·설정 화면이 서로 다른 크기를 씁니다).
- **두 곳에 모두 지정해야 합니다.**
  - `Machine_Estimate.spec`의 `EXE(icon=...)` → 실행 파일·바탕화면 아이콘
  - `root.iconbitmap(...)` → **Tk 창 좌상단·작업표시줄 아이콘** (spec만 고치면 창 아이콘은 그대로 파이썬)
- 파일은 `estimate_app/assets/estimate.ico`에 두고 spec의 `datas`에 포함시킵니다.

---

## 6. UI 개선 — 밝은 그라데이션 테마 (요청 강조 ①)

사용자 선택: **밝은 그라데이션 테마로 전환.**

### 6-1. 팔레트

현재 `TSERP Deep Charcoal`(다크)에서 밝은 계열로 바꿉니다. 참고 예시의 보라→하늘 그라데이션,
흰 카드, 옅은 구분선이 특징입니다.

| 키 | 현재(다크) | v0.0.8(밝게) | 역할 |
|---|---|---|---|
| `bg` | #0d1218 | **#f5f6fa** | 앱 바닥 |
| `panel` | #1b232d | **#ffffff** | **ttk 위젯 전체의 면 색** (아래 주의 참고) |
| `grad_from` | – | **#6553f0** (신규) | 헤더 그라데이션 시작(보라) |
| `grad_to` | – | **#2a76c4** (신규) | 헤더 그라데이션 끝(파랑) |
| `grad_fallback` | – | **#6553f0** (신규) | Canvas를 못 쓸 때 헤더 단색 대체값 |
| `panel_fg` | – | **#ffffff** (신규) | 그라데이션 위 글자 |
| `panel_muted_fg` | – | **#dde4ff** (신규) | 그라데이션 위 보조 글자 |
| `panel_2` | #234060 | **#eef1fb** | 버튼 바탕 |
| `card` | #1f2833 | **#ffffff** | 카드·입력면 |
| `card_alt` | #1a2535 | **#f7f9ff** | 입력 칸 바탕 |
| `line` | #333f4e | **#e3e7f2** | 구분선 |
| `text` | #dde4ec | **#1c2436** | 본문 글자 |
| `muted` | #8b97a7 | **#616b82** | 보조 글자 |
| `accent` | #4fb0ff | **#6553f0** | 주 강조(보라) |
| `accent_2` | #9cc8ff | **#2f6fd0** | 보조 강조(금액 등). 흰 배경에서 읽히도록 예시보다 진하게 |
| `success_bg`/`fg`/`border` | #2c5c44/#7ddc9e/#3ecf8e | **#e3f6ec / #0f7a44 / #46c98b** | 가능 |
| `warn_bg`/`fg`/`border` | #5a3a1a/#ffb648/#d88a4f | **#fff3e0 / #9a5b00 / #eda23b** | 검토필요 |
| `danger_bg`/`fg`/`border` | #40222a/#ff9aa2/#e05561 | **#fdeaec / #b3252f / #e5606c** | 불가 |
| `new_bg`/`fg`/`border` | #5a3a1a/#ffb648/#8a5a2a | **#fff2d9 / #8a5800 / #e8bd6a** | NEW 배지 |
| `pane_head_bg`/`fg` | #1d2530/#8b97a7 | **#eef1fb / #616b82** | 구역 제목 |
| `th_bg`/`th_fg`/`th_underline` | #1a2535/#c8d8ec/#4fb0ff | **#f0f3fc / #2b3550 / #6553f0** | 표 헤더 |
| `row_bg` / `row_alt_bg` | #141b23/#171f28 | **#ffffff / #f9fbff** | 표 본문·줄무늬 |
| `row_hover` | #212a35 | **#eef4ff** | 마우스 올린 행 |
| `row_line` | #1f2832 | **#eceff7** | 행 구분선 |
| `row_selected_line` | #3e85bf | **#6553f0** | 선택 행 표시줄 |

> **주의: `panel`을 그라데이션 색으로 쓰면 안 됩니다 (사전 점검에서 확인).**
> `panel`은 헤더 바 색이 아니라 **ttk 위젯 전체의 배경색**입니다. `ui/theme.py` 57~81줄에서
> `TFrame` / `TLabel` / `TRadiobutton` / `TCheckbutton` / `TLabelframe` /
> `Value.TLabel` / `Muted.TLabel` 이 모두 `panel`을 씁니다. 그리고 `ui/popup.py`는
> **ttk 위젯 50개**로 이뤄져 있어(`ttk.LabelFrame` 3개 + 그 안의 라벨 전부),
> `panel`을 보라(#6553f0)로 두면 **팝업 내부가 통째로 보라색이 되고 그 위에
> 남색 글자(#1c2436)가 얹혀 읽을 수 없게 됩니다.**
> → 헤더 바는 `grad_from`/`grad_to`로만 칠하고, `panel`은 밝은 면 색으로 유지합니다.

**위 표의 색은 이미 명도차를 계산해 통과시킨 값입니다** (WCAG 상대휘도 공식으로 직접 계산).
처음 잡았던 값 중 5개가 기준에 미달해서 바꿨습니다 — 특히 **밝은 하늘색(#4fa8ff) 그라데이션
끝에 흰 글자는 2.51:1** 밖에 안 나와 제목·요약줄이 읽히지 않았습니다. 조정 후 결과입니다.

| 검사 항목 | 조합 | 결과 |
|---|---|---|
| 본문 글자 (카드/바닥/줄무늬/입력칸) | `text` 위 4종 | 14.35 ~ 15.50 : 1 |
| 보조 글자 | `muted` #616b82 위 3종 | 4.94 ~ 5.34 : 1 |
| 최종단가 금액 | `accent_2` #2f6fd0 / 흰·줄무늬 행 | 4.71 / 4.88 : 1 |
| 표 헤더 | `th_fg` / `th_bg` | 10.96 : 1 |
| 구역 제목 | `pane_head_fg` / `pane_head_bg` | 4.73 : 1 |
| **그라데이션 위 제목(흰 글자)** | 시작 / 25% / 50% / 75% / 끝 | **5.19 / 5.17 / 5.11 / 4.93 / 4.68 : 1** |
| 그라데이션 위 보조 글자 | `panel_muted_fg` / 양끝 | 4.10 / 3.70 : 1 (기준 3.0) |
| 체크박스 V 글리프 | `bg` / `accent` | 4.80 : 1 |
| 배지 4종 (가능/검토/불가/NEW) | 각 fg / bg | 4.80 / 4.95 / 5.63 / 5.45 : 1 |

그라데이션은 **중간 지점까지 5개 구간을 계산**했습니다. 한쪽 끝만 맞으면 가운데서 글자가
묻히기 때문입니다. 본문 글자 기준 4.5:1, 큰 글자·배지 기준 3:1을 전 구간에서 넘깁니다.

- **신규 키 5개(`grad_from`/`grad_to`/`grad_fallback`/`panel_fg`/`panel_muted_fg`)는
  `theme.json`과 `core/config.py`의 `DEFAULT_COLORS` 양쪽에 넣습니다.** `_merged()`가
  기본값에 없는 키를 조용히 버리는 함정은 v0.0.6에서 이미 겪었고 v0.0.7 계획서 110줄에 기록돼 있습니다.
- **`bg`는 단순한 배경색이 아니라 "강조색 위에 얹는 대비색"으로도 쓰입니다.** 확인한 4곳:
  `widgets.py:15`(체크박스 글리프), `dashboard.py:235`(`update_row_visual`의 `box_fg`),
  `theme.py:63`(버튼 active/pressed 글자), `theme.py:86`(콤보박스 선택 글자).
  밝은 팔레트에서 `bg`(#f5f6fa)는 `accent`(#6553f0) 위에서 **4.80:1**로 성립합니다(위 표에 포함).
  처음 잡은 #6d5efc로는 4.18:1이라 미달이었고, 그래서 `accent`를 한 단계 진하게 잡았습니다.
- **다크 팔레트는 버리지 않습니다.** `theme.json`에 `presets.dark` 항목으로 v0.0.7 색 전체를
  적어 둡니다. **단, `load_theme()`은 `colors`만 읽으므로 이건 자동 전환이 아니라
  "되돌릴 때 복사해 붙일 원본"입니다.** 되돌리려면 `presets.dark` 값을 `colors`에 덮어쓰면 되고,
  **재빌드는 필요 없습니다**(`theme.json`은 `%LOCALAPPDATA%\MachineEstimate`에 두면 그쪽이 우선).
  화면에서 버튼으로 전환하는 방식은 이번 범위에 넣지 않습니다(요청에 없고, 검증 범위만 커집니다).

### 6-2. 그라데이션을 어떻게 그리는가 — **PIL을 쓰지 않습니다**

Tk에는 그라데이션 기능이 없습니다. 방법이 둘인데, 이 선택이 "가벼움" 요청과 정면으로 맞물립니다.

| 방법 | 용량 | 판단 |
|---|---|---|
| PIL(Pillow)로 이미지 생성 | 모듈 76개 추가. spec 주석에 *"확인 없이 늘리지 말 것"* 으로 **명시적으로 제외해 둔 것** | **쓰지 않음** |
| **`tk.Canvas`에 `create_line`으로 한 줄씩 칠하기** | **0MB** | **채택** |

- 헤더 바 높이 약 84px → 선 84개. 시작·끝 색을 픽셀 행마다 선형 보간해 칠합니다.
- 제목·요약줄은 위젯이 아니라 **Canvas의 `create_text`**로 얹습니다
  (Tk 위젯은 투명 배경이 안 되므로, 라벨을 올리면 그라데이션 위에 사각형 얼룩이 생깁니다).
- 버튼은 `create_window`로 캔버스에 배치합니다(버튼은 자체 배경이 있어 자연스럽습니다).
- 창 크기가 바뀌면 다시 그려야 하므로 `<Configure>`에 붙이되, **폭이 실제로 달라졌을 때만**
  다시 칠합니다. 안 그러면 창을 끌 때마다 84줄을 반복해 그려 무거워집니다.

> **같이 고쳐야 하는 것: `summary_var`.**
> Canvas의 텍스트 항목은 `textvariable`을 지원하지 않습니다. 지금 요약줄은
> `tk.Label(textvariable=self.summary_var)`로 붙어 있어서, 제목·요약줄을 `create_text`로
> 옮기면 **오류 없이 조용히 갱신이 멈춥니다**(요약 문구가 그대로 얼어붙음).
> 바꿔야 할 곳 3군데를 미리 적어 둡니다 — `__init__`의 `"목록을 불러오는 중입니다..."`,
> `update_summary_text()`, `refresh_table()`. `summary_var`에 trace를 걸어
> `itemconfigure(text=...)`로 밀어 넣는 방식으로 처리하고, **요약 건수가 실제로 바뀌는지
> 화면에서 확인**합니다(선택 건수·총 견적금액이 살아 있어야 함).

### 6-3. 글꼴

| 키 | 현재 | v0.0.8 | 이유 |
|---|---|---|---|
| `family` | 맑은 고딕 | **맑은 고딕** (유지) | 한글이 정확히 나오는 것이 우선. 예시의 라틴 전용 글꼴은 한글 글립이 없어 대체 글꼴로 떨어짐 |
| `table_family` | Consolas | **맑은 고딕** | 참고 예시는 고정폭이 아닙니다. 품번·품명·소재가 훨씬 자연스러워집니다 |
| `num_family` | – | **Consolas** (신규) | **금액·수량 열만** 고정폭 유지. 안 그러면 자릿수가 안 맞아 금액 비교가 어려워집니다 |
| `title_size` | 20 | **21** | 헤더가 그라데이션이 되어 제목을 조금 키워야 무게가 맞습니다 |

`num_family`도 `theme.json` + `DEFAULT_FONTS` **양쪽**에 추가합니다.

### 6-4. 모양 다듬기

- 표 헤더 밑줄을 보라(`th_underline`)로, 선택 행 표시줄도 보라로 맞춰 강조색을 하나로 통일합니다.
- 카드·입력 영역에 1px 옅은 테두리(`line`)를 둘러 흰 배경 위에서 면이 구분되게 합니다.
  (다크에서는 배경이 어두워 테두리가 없어도 층이 보였지만, 흰 배경에서는 안 보입니다.)
- 팝업창 배경도 `bg`를 따라 밝게 바뀌므로, 팝업 안 `LabelFrame`·`Entry` 색을 함께 점검합니다.

**검증 방법** — 본 화면과 팝업창을 실제로 띄워 화면 캡처로 육안 확인합니다.
확인 항목: ① 그라데이션 이음새가 보이지 않는지 ② 흰 배경에서 금액·보조 글자가 읽히는지
③ 배지 3종(가능/불가/검토필요)이 구분되는지 ④ 줄무늬와 hover가 보이는지
⑤ 창 크기를 바꿔도 그라데이션이 어긋나지 않는지 ⑥ **요약줄(선택 건수·총 견적금액)이 실제로
갱신되는지**(6-2의 `summary_var` 문제) ⑦ **헤더 밑줄(보라 #6553f0)과 그라데이션 오른쪽 끝
(파랑 #2a76c4)이 맞닿는 지점이 색이 안 맞아 보이지 않는지** — 밑줄은 헤더 바 바로 아래
2px이고 창이 넓어질수록 그 접점이 파란 쪽으로 옮겨갑니다. 어색하면 밑줄을 그라데이션과
같은 방향으로 칠하거나 `grad_to`로 맞춥니다(계산으로는 판단할 수 없어 보고 결정합니다).

---

## 7. 프로그램 운영의 가벼움 (요청 강조 ②)

0-5에서 **무거움이 전부 표 그리기 한 곳에 있음**을 실측으로 확인했습니다.
시작 코드는 이미 0.114초로 가볍고, `openpyxl`도 이미 지연 로딩되어 있습니다.
그래서 여기서는 **표 하나만 제대로 고칩니다.** 여러 곳을 조금씩 손대는 것보다 효과가 큽니다.

### 7-1. 행 위젯 재사용 — 핵심 개선 (현재 3.886s → **목표 1.0s 미만**)

현재는 갱신할 때마다 529개 위젯을 `destroy()`하고 다시 만듭니다.

- `self.row_widgets[no]`에 **행의 모든 위젯 참조를 등록**합니다.
  (v0.0.7이 체크박스 깜빡임을 잡으려고 이 구조를 이미 만들어 뒀습니다. 그걸 표 전체로 넓힙니다.)
- 갱신 시 **행 위젯 풀을 재사용**합니다.
  - 필요한 행 수 ≤ 있는 행 수 → 텍스트·색만 `configure`로 교체하고 남는 행은 `grid_remove()`
  - 필요한 행 수 > 있는 행 수 → 부족한 만큼만 새로 만든다
- 행 하나가 담당하는 항목 번호(`no`)가 바뀌므로, 클릭·hover 처리는 **행에 붙은 현재 `no`를 보고**
  동작하게 바꿉니다(지금은 만들 때의 `no`를 클로저에 가둬 둬서 재사용이 불가능합니다).
  → `table.py`의 `render_row` 안 `lambda ... card_no=no`와 `_bind_row_behavior`의 `no` 캡처
  **두 곳 모두**가 대상입니다. 하나만 고치면 클릭은 맞고 hover만 엉키는 식이 됩니다.
- 표를 완전히 비워야 하는 경우(검색 결과 없음/데이터 없음)는 지금처럼 안내 문구를 냅니다.
- 재사용 시 함께 갱신해야 하는 것들(빠뜨리면 앞 항목의 표시가 남습니다):
  **NEW 배지 표시/제거 · 가능여부 배지 3색 · 줄무늬 배경(홀짝) · 선택 표시줄 · hover 대상 목록.**

**이 하나로 v0.0.8.md의 "검색 시 카드가 하나하나 깜빡임"(검색 1번)도 같이 사라집니다.**
깜빡임은 곧 위젯을 지웠다 다시 만드는 그 과정이기 때문입니다.

### 7-2. 행당 위젯 수 줄이기 (현재 16개 — **줄인 결과는 실측해 보고**)

- 품번 칸이 `Frame` 2개 + 라벨 2개 + 배지 1개로 5개를 씁니다.
  배지가 없는 행(저장 완료 항목)에서는 배지를 만들지 않고, 프레임 한 겹을 줄입니다. (−2)
- 위·아래 선 2개를 행 프레임의 `highlightthickness`로 대체할 수 있는지 검토합니다. (−2, 되면)
- **숫자를 약속하지 않습니다.** 둘 다 되면 12개, 아래쪽이 안 되면 14개입니다.
  `highlightthickness`로 선택 표시줄의 위/아래 색을 따로 줄 수 있는지가 관건이고,
  이건 **실제로 해 봐야 알 수 있습니다.** 7-1이 실제 개선의 대부분이라
  이 항목이 덜 줄어도 전체 결과에는 큰 영향이 없습니다.

### 7-3. 그 밖에

- **세션 파일**: 33건에 25.7KB. 값이 0인 공수 필드를 저장하지 않으면 절반 이하로 줄어듭니다.
  읽을 때 기본값 0으로 채우므로 기존 파일과 호환됩니다.
- **배포 규모 유지**: 22MB / 947 파일. 새 의존성은 **하나도 추가하지 않습니다.**
  그라데이션은 Canvas(6-2), 아이콘은 직접 생성(5번) — **둘 다 0MB**입니다.
- `spec`의 `EXCLUDES`는 **손대지 않습니다.** 주석에 *"확인 후 동작을 검증한 항목만"* 이라고
  적혀 있고, 이번 작업에 네트워크·이미지 기능이 없어 되돌릴 이유가 없습니다.

**검증 방법** — 0-5와 **완전히 같은 측정 코드**로 전후를 비교해 표로 보고합니다.
① 첫 표 그리기 ② 검색 1회 ③ 체크 토글 1회 ④ 위젯 수 ⑤ 세션 파일 크기 ⑥ dist 용량.
33건뿐 아니라 **200건 이상**을 넣은 세션으로도 재서, 항목이 늘 때 느려지는 정도를 함께 확인합니다.

---

## 8. 버전 / 빌드

- `estimate_app/__init__.py` → `APP_VERSION = "0.0.8"`
- `installer/Setup.iss` → `MyAppVersion "0.0.8"` (**양쪽 모두** — v0.0.6 전례)
- `Machine_Estimate.spec` → `EXE(icon='estimate_app\\assets\\estimate.ico')`,
  `datas`에 `.ico` 포함
- `build.bat setup` → `dist\Estimate\` + `installer\Output\MachineEstimate_Setup_v0.0.8.exe`
- 작업 종료 후 `ver_plan.md`에 v0.0.8 항목 추가(요청/반영/검증/미실행 4단락 — 기존 문서 관례)

---

## 9. 작업 순서

1. **아이콘 생성** (`estimate.ico`, 4개 크기) → spec과 `iconbitmap` 양쪽 지정
2. **명칭 변경** (`APP_TITLE` + `Setup.iss` `MyAppName`) 및 화면 내 옛 문구 정리
3. **밝은 팔레트 + 신규 키 4개**를 `theme.json`·`DEFAULT_COLORS` 양쪽에 반영, 다크 프리셋 보존
4. **헤더 그라데이션**(Canvas) + 글꼴 교체(`table_family`/`num_family`) + 카드 테두리
5. **행 위젯 재사용**으로 표 갱신 구조 교체 → 깜빡임 제거 (7-1, 검색 1번)
6. **스크롤 되돌림**(`yview_moveto(0)`) (검색 2·3번)
7. **ESC 바인딩** 2종 (팝업 닫기 / 선택 취소)
8. **Size 열 병합 + 가운데 정렬** 3개 호출 지점 (1번)
9. **견적서 `0` 행 제거** — 실제로 쓴 행만 참조 (2-4)
10. **PDF 진단 로그 + 실패 시 엑셀 저장 제안 + `-EncodedCommand`·COM 옵션 보강** (2-1~2-3)
11. **검증**
    - 엑셀/PDF: 3 / 7 / 8 / 12 / 43건을 내보내 **Excel COM으로 재계산**한 뒤
      Size 병합·정렬, 견적서 `0` 행 없음, 합계 일치, PDF 1페이지를 매 경우 직접 읽어 대조
    - **PDF 회귀**: 0-2의 5가지 재현(소스 / `pythonw` / 완전분리 / **frozen exe 재빌드** /
      한글경로+공백폴더)을 변경 후 전부 다시 실행해 되던 것이 깨지지 않았는지 확인
    - 성능: 0-5와 같은 측정 코드로 전후 비교표 (33건 + 200건 이상)
    - 화면: 본 화면·팝업 캡처로 육안 확인(그라데이션 이음새, 요약줄 갱신, 배지 3종, 줄무늬·hover)
    - 행 재사용 오동작 확인: 검색 / 더보기 / 체크 / 업로드 / 저장 **다섯 경로에서 행을 클릭해
      맞는 항목의 팝업이 열리는지** 확인
12. 버전 0.0.8 반영 → 빌드 → 설치 파일 생성
13. `ver_plan.md` 기록 및 보고

---

## 10. 위험 요소 (미리 알려 두는 것)

- **PDF 실패 원인은 이번에도 못 찾을 수 있습니다.** 이 PC에서 5가지 방법으로 재현이 안 됩니다.
  이번 작업은 "고쳤다"가 아니라 **"다음에 실패하면 원인이 로그에 남고, 실패해도 엑셀은 건진다"**
  까지입니다. 오류창이 다시 뜨면 `%LOCALAPPDATA%\MachineEstimate\pdf_error.log`를 보내 주시면
  그때 원인을 특정할 수 있습니다. 이 점을 분명히 해 두겠습니다.
- **다크 → 밝은 테마는 v0.0.7에서 방금 승인받은 작업을 되돌리는 방향입니다.**
  다크 팔레트를 `presets.dark`로 남겨 두지만, 실제로 보시고 다크가 낫다고 판단하실 수 있습니다.
  그 경우 `theme.json`만 바꾸면 되돌아가며 **재빌드는 필요 없습니다.**
- **표 갱신 구조 교체(7-1)는 이번 작업에서 가장 손이 많이 가는 부분입니다.**
  행 위젯을 재사용하면 "이 행이 지금 어느 항목인지"가 바뀌므로, 클릭했을 때 **다른 항목의
  팝업이 열리는 실수**가 나기 쉽습니다. 검색·더보기·체크·업로드·저장 다섯 경로 모두에서
  행을 클릭해 실제로 맞는 항목이 열리는지 확인하겠습니다.
- **양식 구조는 이번에 바꾸지 않습니다.** Size 병합은 산출되는 파일에만 적용하고,
  `견적_산정` 폴더의 과거 파일들은 건드리지 않습니다.
- 관리자 권한 실제 설치를 통한 업그레이드 검증은 이전 버전들과 마찬가지로 이 세션에서
  수행할 수 없습니다. **빌드·설치 파일 생성까지만** 확인합니다.

---

## 11. 확인이 필요한 사항

없습니다. 아래 3건은 답변을 받아 확정했습니다.

| 항목 | 확정 내용 |
|---|---|
| PDF 증상 | "PDF로 만들지 못했습니다" **오류창이 떴다** → COM 호출이 실제로 실패. 2번을 그에 맞춰 구성 |
| UI 방향 | **밝은 그라데이션 테마로 전환** (다크는 프리셋으로 보존) |
| 아이콘 | **직접 제작** (외부 라이브러리 없이, 배포 용량 증가 0MB) |
