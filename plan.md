# 실행 파일 생성 작업 기록

## 2026-08-03 승인된 작업

- 대상 파일: `machine_estimate_app.py`
- 목표: 더블클릭으로 실행할 수 있는 Windows 실행 파일 생성
- 방식: PyInstaller를 사용해 GUI용 단일 EXE 생성
- 포함 파일: `견적_산정\양식\견적용.xlsx` 양식을 EXE 내부에 포함
- 코드 보강:
  - 일반 실행과 EXE 실행 모두에서 양식 파일을 찾도록 경로 처리 추가
  - 저장 결과 `견적용_입력완료.xlsx`는 실행 파일이 있는 폴더에 생성되도록 변경
- 검증 예정:
  - 파이썬 문법 검사
  - PyInstaller 빌드 성공 확인
  - 생성된 EXE 실행 시작 확인

## 완료 결과

- 생성 파일: `exe_release\Machine_Estimate.exe`
- 빌드 방식: PyInstaller `--onefile --windowed`
- 양식 포함: `견적_산정\양식\견적용.xlsx`를 실행 파일 내부에 포함
- 실행 확인: EXE를 5초간 실행해 즉시 종료되지 않는 것을 확인
- 결과 저장 위치: EXE와 같은 폴더에 `견적용_입력완료.xlsx` 생성
- 최종 위치의 `exe_release\Machine_Estimate.exe`도 실행 시작 확인 완료.

## 2026-08-03 공정 시간 입력창 미표시 수정 계획

### 현상

- 항목 입력 팝업에서 기본 정보와 Size 입력 영역은 보이지만, 각 공정별 시간 입력 영역이 보이지 않는다.
- 코드에는 `설비 및 작업별 공수 시간` 입력 프레임이 존재하지만, 현재 팝업 배치/스크롤 구조에서는 실제 화면에서 사용자가 바로 확인하기 어렵다.

### 원인 판단

- 공정 시간 입력 영역이 Size 영역 아래쪽에 세로로 배치되어 있어, 창 크기/배율/스크롤 상태에 따라 화면에서 사라진 것처럼 보인다.
- 사용자는 공정 시간 입력을 반드시 입력해야 하므로, 숨겨질 수 있는 아래쪽 배치보다 항상 보이는 고정 영역 배치가 적합하다.

### 수정 계획

1. 항목 입력 팝업을 좌우 2단 구조로 변경한다.
2. 왼쪽에는 기존 `기본 정보`, `Size 입력`을 유지한다.
3. 오른쪽에는 `각 공정별 시간 입력` 영역을 항상 보이도록 고정 배치한다.
4. 기존 공정 항목과 계산 로직은 그대로 유지한다.
5. 저장/저장 후 다음 항목 버튼은 기존처럼 하단에 유지한다.
6. 수정 후 파이썬 문법 검사를 진행한다.
7. 실행 파일도 다시 빌드해서 `exe_release\Machine_Estimate.exe`를 갱신한다.

### 승인 대기

- 위 방향으로 수정 진행 승인을 기다린다.

## 2026-08-03 공정 시간 입력창 미표시 수정 완료

- 승인 후 `machine_estimate_app.py` 팝업 구조를 좌우 2단으로 변경했다.
- 왼쪽에는 `기본 정보`와 `Size 입력`을 유지했다.
- 오른쪽에는 `각 공정별 시간 입력` 영역을 항상 보이도록 배치했다.
- 기존 공정 항목, 계산 로직, 저장 로직은 유지했다.
- 다음 단계로 문법 검사와 EXE 재빌드를 진행한다.

## 재빌드 완료

- `exe_release\Machine_Estimate.exe`를 새 UI가 반영된 실행 파일로 갱신했다.
- 공정 시간 입력 영역이 오른쪽에 항상 보이도록 팝업 구조를 변경한 상태로 빌드했다.
- 최종 EXE 실행 시작 확인 완료: 5초 동안 즉시 종료되지 않음.

## 2026-08-03 공정 시간 입력 영역 재점검 및 추가 수정

- 문제: 오른쪽에 공정 시간 입력 영역이 여전히 나타나지 않음.
- 재점검 결과: 오른쪽 프레임은 코드에 있었지만, 바깥 스크롤 캔버스 구조가 폭 배치를 꼬이게 만들 가능성이 있음.
- 조치: 팝업 내부의 스크롤 캔버스 구조를 제거하고, 본문을 직접 좌우 2단 그리드로 고정 배치했다.
- 왼쪽 영역 최소 폭: 560px, 오른쪽 공정 시간 입력 영역 최소 폭: 420px로 지정했다.
- 팝업 기본 크기: 1080x560, 최소 크기: 980x520으로 조정했다.

## 2026-08-03 공정 시간 입력 영역 재빌드 완료

- 스크롤 캔버스를 제거한 팝업 구조로 EXE를 다시 빌드했다.
- `exe_release\Machine_Estimate.exe`를 새 빌드로 교체했다.
- 기존 `exe_release\Machine_Estimate.exe`가 실행 중이라 파일이 잠겨 즉시 덮어쓰기 불가.
- 같은 폴더에 새 수정본 `exe_release\Machine_Estimate_fixed.exe`로 생성한다.

## 2026-08-03 견적용.xlsx 로드/날짜별 누적 저장/UI 130% 확대 계획

### 요청 내용

- `견적용.xlsx` 양식을 기준으로 로드해서 사용한다.
- 입력 결과가 한 번 저장하고 끝나는 방식이 아니라 누적 입력 가능해야 한다.
- 작성 날짜 기준으로 구분할 수 있어야 한다.
- 창 UI의 폰트 크기를 현재의 130% 수준으로 키운다.

### 확인한 현재 구조

- 양식 파일 위치: `견적_산정\양식\견적용.xlsx`
- 양식 시트: `견적서`, `기계`, `Sheet1`
- 실제 입력 시트: `기계`
- 입력 시작 행: 7행
- 입력 주요 열:
  - B: 품번
  - C: 품명
  - D: Coment
  - E: 가능여부
  - F: Qty
  - G: Material
  - H: Size
  - K:T: 각 공정별 시간
  - U: SUM 수식
  - V: 단가 수식
  - W: 최종단가 수식
- 현재 저장 방식은 `견적용_입력완료.xlsx` 하나에 덮어쓰는 구조라 누적 저장에 부적합하다.

### 수정 계획

1. 프로그램 실행 시 `견적_산정\양식\견적용.xlsx`를 기준 양식으로 사용한다.
2. 저장 시 작성 날짜 기준 폴더를 만든다.
   - 예: `견적_산정\2026년도\08월`
3. 저장 파일명은 날짜 기준으로 생성한다.
   - 예: `견적누적_2026-08-03.xlsx`
4. 해당 날짜 파일이 없으면 `견적용.xlsx` 양식을 복사해서 새 날짜 파일을 만든다.
5. 해당 날짜 파일이 이미 있으면 `기계` 시트의 마지막 입력 행 다음에 새 항목을 이어 쓴다.
6. 입력된 항목만 저장하고, 빈 항목은 누적 저장 대상에서 제외한다.
7. 새로 추가되는 행에는 기존 양식 행의 수식/서식을 복사해 `SUM`, `단가`, `최종단가` 계산 구조를 유지한다.
8. UI 폰트는 기존 크기 대비 약 130%로 키운다.
   - 기본 글자: 10 → 13 수준
   - 팝업/버튼/표 글자도 함께 확대
   - 표 행 높이도 글자 크기에 맞게 증가
9. 수정 후 파이썬 문법 검사를 진행한다.
10. EXE를 다시 빌드해 실행 파일을 갱신한다.

### 승인 대기

- 위 계획대로 진행하려면 사용자 승인을 받은 뒤 수정한다.

## 2026-08-03 날짜별 누적 저장 및 UI 130% 확대 구현 완료

- `견적_산정\양식\견적용.xlsx`를 기준 양식으로 로드하도록 유지했다.
- 저장 시 `견적_산정\YYYY년도\MM월\견적누적_YYYY-MM-DD.xlsx` 파일을 대상으로 사용하도록 변경했다.
- 날짜 파일이 없으면 양식에서 새로 만들고, 이미 있으면 기존 입력 아래에 누적 저장하도록 변경했다.
- 빈 항목은 저장하지 않도록 했다.
- 새 입력 행에는 기존 양식의 서식과 수식을 복사/재생성하도록 했다.
- UI 기본 폰트, 버튼, 입력창, 표, 팝업 글자 크기를 약 130% 수준으로 확대했다.

## 2026-08-03 날짜별 누적 저장/UI 확대 EXE 재빌드 완료

- PyInstaller 빌드 완료.
- 최신 실행 파일을 `exe_release\Machine_Estimate_latest.exe`로 생성했다.
- 기존 `Machine_Estimate.exe`가 실행 중일 수 있어, 덮어쓰기 전에 프로세스 잠금 여부를 확인한다.
- 실행 중인 이전 프로세스가 없어 `exe_release\Machine_Estimate.exe`도 최신 빌드로 교체했다.
- 문법 검사 통과.
- 임시 파일 기준 누적 저장 검증 완료: 1차/2차 저장이 7행, 8행으로 이어짐.
- 8개 항목 저장 검증 완료: 합계 행 앞에 행 삽입, 합계 수식 `=SUM(W7:W14)` 확인.
- 최종 EXE 실행 시작 확인 완료: 5초 동안 즉시 종료되지 않음.

## 2026-08-04 카드형 견적 현황판 전환 계획

### 요청 내용

- `견적용.xlsx` 양식을 기준으로 입력한다.
- 입력된 견적 항목을 표 형태가 아니라 카드 형태로 저장/표시한다.
- TSERP에서 만든 현황판처럼 한눈에 볼 수 있는 카드형 화면으로 구성한다.
- 기존 날짜별 누적 저장 방식은 유지한다.
- UI는 현재 130% 확대 기준을 유지한다.

### 현재 상태

- 현재 메인 화면은 `Treeview` 표 형태로 10개 기본 행을 표시한다.
- 상세 입력은 항목 더블클릭 팝업에서 처리한다.
- 저장은 `견적_산정\YYYY년도\MM월\견적누적_YYYY-MM-DD.xlsx`에 날짜별로 누적 저장한다.
- `견적용.xlsx`의 `기계` 시트 구조는 유지 중이다.

### 수정 방향

1. 메인 화면을 카드형 현황판 구조로 변경한다.
   - 상단: 저장 버튼, 새 카드 추가 버튼, 오늘 날짜/총 항목/총 견적금액 요약
   - 본문: 입력된 견적 항목을 카드 형태로 나열
2. 카드에는 핵심 정보를 표시한다.
   - NO
   - 작성일
   - 품번
   - 품명
   - 가능여부
   - Qty
   - Material
   - Size
   - 공정 시간 합계
   - 최종단가
3. 카드 색상은 상태별로 구분한다.
   - 가능: 정상/진행 느낌
   - 검토필요: 주의 느낌
   - 불가: 경고 느낌
4. 카드 클릭 또는 `수정` 버튼으로 기존 상세 입력 팝업을 연다.
5. `새 카드 추가` 버튼으로 빈 견적 카드와 입력 팝업을 만든다.
6. 카드 저장 시 기존 날짜별 누적 Excel 저장 로직을 그대로 사용한다.
7. 카드 화면은 스크롤 가능하게 만들어 항목이 늘어나도 현황판처럼 볼 수 있게 한다.
8. 기존 `견적용.xlsx` 양식 구조와 계산/저장 수식은 변경하지 않는다.
9. 수정 후 검증한다.
   - 파이썬 문법 검사
   - 카드 추가/수정 로직 코드 확인
   - 날짜별 누적 저장 로직 유지 확인
   - EXE 재빌드 및 실행 시작 확인

### 작업 범위에서 제외

- 기존 `04.CSS_조건 산출기.hta`, `05.견적.hta`는 수정하지 않는다.
- 엑셀 양식 자체의 시트 구조는 변경하지 않는다.
- 별도 데이터베이스는 추가하지 않는다.

### 승인 대기

- 사용자 승인 후 `machine_estimate_app.py`를 카드형 현황판 UI로 수정하고 EXE를 다시 빌드한다.

## 2026-08-04 카드형 견적 현황판 구현 진행

- 기존 메인 `Treeview` 표 화면을 제거했다.
- 메인 화면을 상단 요약 영역과 스크롤 가능한 카드 목록으로 변경했다.
- 오늘 날짜 누적 파일이 있으면 `기계` 시트 내용을 읽어 카드로 표시하도록 했다.
- 카드에는 작성일, 품번, 품명, 가능여부, Qty, Material, Size, 시간합계, 최종단가, 메모를 표시한다.
- 상태별 카드 색상을 다르게 표시하도록 했다.
- 카드의 `수정` 버튼 또는 카드 더블클릭으로 기존 상세 입력 팝업을 열도록 했다.
- `새 카드 추가` 버튼은 빈 카드를 만들고 상세 입력 팝업을 연다.
- 기존 누적 파일에서 불러온 카드는 엑셀 행 번호를 기억하도록 했다.
- 불러온 카드를 다시 저장하면 중복 추가하지 않고 해당 행을 갱신하도록 했다.
- 신규 카드는 마지막 입력 행 다음에 추가되도록 검증했다.
- 임시 검증 결과: P1/P2 불러오기 후 P1 수정 + P3 추가 시 엑셀 행은 P1 수정본, P2, P3 총 3건으로 유지됨.

## 2026-08-04 카드형 현황판 점검 기록

- GUI 앱을 직접 실행하면 창이 떠 있는 동안 명령이 종료되지 않아 작업이 멈춘 것처럼 보일 수 있음을 확인했다.
- `python -m py_compile machine_estimate_app.py` 문법 검사는 통과했다.
- 시작 시 날짜 폴더를 만들지 않도록 `get_dated_output_path(create_dir=False)`를 로드 경로에 적용했다.
- 짧은 실행 확인 결과 앱이 5초 동안 즉시 종료되지 않아 정상 시작으로 확인했다.

## 2026-08-04 카드형 현황판 EXE 재빌드 완료

- PyInstaller 빌드 완료.
- `exe_release\Machine_Estimate.exe`와 `exe_release\Machine_Estimate_latest.exe`를 최신 카드형 현황판 버전으로 교체했다.
- 최종 EXE 실행 시작 확인 완료: 5초 동안 즉시 종료되지 않음.
- 카드형 현황판 UI, 기존 카드 갱신/신규 카드 추가 검증, 날짜별 누적 저장 구조를 반영한 빌드다.

## 2026-08-04 양식 업로드/저장/검색 기능 추가 계획

### 요청 내용

- 사용자가 가지고 있는 `견적용.xlsx` 계열 양식 파일을 업로드해서 프로그램 카드 목록으로 불러온다.
- 업로드한 내용을 카드형 현황판에 표시한다.
- 불러온 내용을 수정하거나 신규 카드와 함께 날짜별 누적 파일로 저장할 수 있게 한다.
- Search/검색 기능으로 카드 목록에서 원하는 항목을 찾을 수 있게 한다.

### 현재 상태

- 메인 화면은 카드형 현황판으로 구성되어 있다.
- 오늘 날짜 누적 파일이 있으면 `기계` 시트 내용을 카드로 불러온다.
- 날짜별 누적 저장은 유지되어 있다.
- 기존 카드는 엑셀 행 번호를 기억해서 다시 저장할 때 중복 저장하지 않고 갱신한다.

### 수정 계획

1. 상단 버튼 영역에 `양식 업로드` 버튼을 추가한다.
2. `tkinter.filedialog`를 사용해 사용자가 `.xlsx` 파일을 선택할 수 있게 한다.
3. 업로드 대상 파일에서 `기계` 시트를 찾는다.
4. `기계` 시트의 7행부터 합계 행 전까지 입력된 내용을 읽어 카드 데이터로 변환한다.
5. 업로드 방식은 안전하게 처리한다.
   - 기존 화면 카드와 합치기 전에 사용자 확인을 받는다.
   - 기본 동작은 현재 화면 카드에 추가한다.
   - 중복 품번/품명은 카드가 중복될 수 있음을 안내한다.
6. 업로드된 카드는 `excel_row=None`으로 두어 저장 시 오늘 날짜 누적 파일의 새 행으로 추가되게 한다.
7. 상단에 검색창과 검색 초기화 버튼을 추가한다.
8. 검색 대상은 다음 필드로 한다.
   - 품번
   - 품명
   - 가능여부
   - Material
   - Size
   - Coment
   - 작성일
9. 검색어가 있으면 해당 카드만 표시하고, 요약 영역에는 검색 결과 수를 함께 보여준다.
10. 수정 후 검증한다.
   - 파이썬 문법 검사
   - 업로드 파서 임시 검증
   - 검색 필터 검증
   - EXE 재빌드 및 실행 시작 확인

### 작업 범위에서 제외

- 엑셀 양식 자체의 시트 구조 변경은 하지 않는다.
- 별도 데이터베이스는 추가하지 않는다.
- 기존 HTA 파일은 수정하지 않는다.

### 승인 대기

- 사용자 승인 후 `machine_estimate_app.py`에 업로드/검색 기능을 구현하고 EXE를 다시 빌드한다.

## 2026-08-04 양식 업로드/검색 기능 구현 진행

- 상단에 `양식 업로드` 버튼을 추가했다.
- 상단에 Search 검색창과 `검색 초기화` 버튼을 추가했다.
- 선택한 `.xlsx` 파일의 `기계` 시트를 읽어 카드 데이터로 변환하는 공통 파서를 추가했다.
- 오늘 날짜 누적 파일 로드도 같은 파서를 사용하도록 정리했다.
- 업로드된 카드는 저장 시 오늘 날짜 누적 파일의 새 행으로 추가되도록 `excel_row=None`으로 처리한다.
- 검색 대상은 작성일, 품번, 품명, 가능여부, Material, Size, Coment, Qty, 시간합계, 최종단가로 구성했다.
- 문법 검사 통과.
- 테스트 파일 기준 업로드 파서 검증 완료: 저장된 `.xlsx`에서 카드 1건을 다시 읽음.
- 검색 검증 완료: 품번 `SEARCH`, Material `S45C`는 검색됨, 없는 단어는 검색되지 않음.
- 실행 오류 수정: `tk.Frame`의 `pady=(0, 10)` 옵션을 `pack(..., pady=(0, 10))`로 옮겨 Tkinter 화면 거리 오류를 해결했다.

## 2026-08-04 양식 업로드/검색 기능 EXE 재빌드 완료

- PyInstaller 빌드 완료.
- `exe_release\Machine_Estimate.exe`와 `exe_release\Machine_Estimate_latest.exe`를 업로드/검색 기능 포함 최신 버전으로 교체했다.
- 최종 EXE 실행 시작 확인 완료: 5초 동안 즉시 종료되지 않음.
- 업로드/검색/카드형 현황판/날짜별 누적 저장 기능이 포함된 빌드다.

## 2026-08-04 연도별 전체 견적 파일 자동 업로드 및 전체 화면 실행 계획

### 요청 내용

- `C:\Users\SumH\Codex\Estimate\견적_산정\2026년도` 경로 아래 각 월별 폴더에 있는 입력분을 프로그램 실행 시 자동 업로드한다.
- 업로드된 전체 내용을 카드 현황판에서 검색할 수 있게 한다.
- 프로그램 실행 시 창을 전체 화면 또는 최대화 상태로 연다.

### 확인한 현재 파일 구조

- 자동 업로드 대상 폴더: `견적_산정\2026년도`
- 확인된 월별 엑셀 파일: 총 18개
  - `04월`: 1개
  - `05월`: 4개
  - `06월`: 8개
  - `07월`: 5개
- 현재 프로그램은 실행 시 오늘 날짜 누적 파일 하나만 자동 로드한다.
- 수동 `양식 업로드` 기능은 이미 있다.
- 검색 기능은 이미 카드 목록에 적용되어 있다.

### 수정 계획

1. 실행 시 자동 로드 대상을 오늘 날짜 파일 1개에서 `견적_산정\2026년도` 전체 `.xlsx` 파일로 확장한다.
2. 월별 폴더 안의 모든 `.xlsx` 파일을 순회한다.
3. `견적_산정\양식\견적용.xlsx`는 기준 양식이므로 자동 업로드 대상에서 제외한다.
4. 각 파일에서 `기계` 시트의 7행부터 합계 행 전까지 입력 항목을 읽어 카드로 변환한다.
5. 카드에는 원본 파일명/원본 경로/월 정보를 함께 보관한다.
6. 검색 대상에 원본 파일명과 월 정보를 추가한다.
7. 같은 파일을 읽어온 카드는 저장 시 기존 원본 파일을 직접 수정하지 않고, 현재 날짜 누적 파일에 새 카드로 저장되도록 한다.
8. 기존 수동 `양식 업로드` 기능은 유지한다.
9. 실행 시 창은 Windows 최대화 상태로 시작한다.
   - `root.state("zoomed")` 우선 적용
   - 실패 시 기존 큰 창 크기 유지
10. 수정 후 검증한다.
   - 파이썬 문법 검사
   - `견적_산정\2026년도` 스캔 파일 수 확인
   - 실제 월별 파일에서 카드 로드 수 확인
   - 검색 대상에 파일명/월 포함 확인
   - EXE 재빌드 및 실행 시작 확인

### 주의 사항

- 월별 기존 파일 원본은 자동으로 수정하지 않는다.
- 저장은 기존 방식대로 오늘 날짜 누적 파일에 반영한다.
- 기존 HTA 파일과 엑셀 양식 구조는 변경하지 않는다.

### 승인 대기

- 사용자 승인 후 `machine_estimate_app.py` 수정 및 EXE 재빌드를 진행한다.

## 2026-08-04 연도별 전체 자동 업로드 및 최대화 실행 구현 완료

- 실행 시 창이 Windows 최대화 상태로 열리도록 `root.state("zoomed")`를 적용했다.
- `견적_산정\2026년도` 아래 월별 `.xlsx` 파일 전체를 자동 스캔하도록 변경했다.
- 실제 확인 결과 자동 업로드 대상 파일은 18개이며, 18개 파일 모두에서 카드 데이터가 확인되었다.
- 자동 업로드 카드 수: 319건.
- 오래된 양식의 Size가 H~J에 나뉜 경우 `19 x 820 x 1200` 형태로 합쳐 표시하도록 했다.
- 오래된 양식의 공정 시간 영역에 수식 셀이 섞인 경우 숫자 변환 오류가 나지 않도록 수식/문자값은 0으로 처리하는 안전 변환을 추가했다.
- 카드에 원본 월/파일명을 표시하고, 검색 대상에도 포함했다.
- 과거 원본 파일에서 자동 업로드된 카드는 기본적으로 저장 대기 대상이 아니며, 수정한 경우 오늘 날짜 누적 파일에 저장되도록 했다.

## 2026-08-04 연도별 전체 자동 업로드/최대화 EXE 재빌드 완료

- PyInstaller 빌드 완료.
- `exe_release\Machine_Estimate.exe`와 `exe_release\Machine_Estimate_latest.exe`를 최신 버전으로 교체했다.
- 기존 `Machine_Estimate.exe`가 2개 실행 중이라 기본 실행 파일명은 Windows 파일 잠금으로 교체되지 않았다.
- `exe_release\Machine_Estimate_latest.exe`는 최신 빌드로 교체 완료했다.
- 최신 EXE 실행 시작 확인 완료: 7초 동안 즉시 종료되지 않음.
- 기본 파일명 교체는 실행 중인 기존 `Machine_Estimate.exe` 창을 모두 닫은 뒤 가능하다.

## 2026-08-04 20:50:21 기계 시트 카드 업로드 재점검 및 실행파일 갱신
- 사용자 지정 파일 견적_산정\2026년도\04월\PLK972304 외.xlsx의 기계 시트만 읽어 카드로 변환되도록 재확인함.
- 해당 파일의 기계 시트에서 27개 카드가 추출됨을 확인함.
- 카드 UI에 K:T 공정 시간 표시 영역을 추가함: 5축, 4축, 3축, 선반, 범용, 사상, 3차원, 연마, 지그, 프로그래밍.
- 상단 업로드 버튼 문구를 기계 시트 업로드로 변경하여 업로드 대상이 명확히 보이게 함.
- 연도 폴더 견적_산정\2026년도 자동 로드 시 18개 파일에서 319개 행을 읽고, 실제 카드 표시 데이터 196건을 확인함.
- Tkinter 실제 위젯 검사 결과 전체화면 상태와 공정 시간 라벨/값 표시를 확인함.
- PyInstaller로 실행 파일을 재빌드하고 exe_release\Machine_Estimate.exe, exe_release\Machine_Estimate_latest.exe를 최신본으로 갱신함.
- 기존 실행 중이던 구버전 Machine_Estimate.exe 프로세스 2개는 파일 교체를 위해 종료함.
- 최신 Machine_Estimate.exe 7초 실행 검증 결과 정상 실행됨.

## 2026-08-05 07:31:45 멈춤 버그 및 딥블루 UI 개선 계획

### 현재 점검 결과
- 실행 중인 Machine_Estimate.exe가 2개 확인됨. 중복 실행 상태에서 파일 로드/저장/교체가 겹치면 멈춤이나 잠금이 자주 발생할 수 있음.
- load_existing_cards()가 시작 시 견적_산정\2026년도의 모든 xlsx를 UI 스레드에서 즉시 읽음. 이 동안 창이 멈춘 것처럼 보일 수 있음.
- efresh_table()이 검색/저장/수정 때마다 표시 대상 카드 전체를 파괴 후 재생성함. 현재 카드 300건 내외 기준 라벨 위젯이 1만 개 이상 생성되어 멈춤 원인이 됨.
- 팝업 입력창은 1080x560, 좌/우 컬럼 고정 폭에 130% 폰트가 적용되어 Size 입력/공정 입력 영역이 짤릴 수 있음.
- 현재 색상은 밝은 카드형 UI라 사용자가 요청한 TSERP와 동일한 딥블루 모드가 적용되어 있지 않음.

### 수정 계획
1. 멈춤 완화
   - 시작 시 자동 업로드는 백그라운드 스레드에서 수행하고, UI에는 로드 중 상태를 먼저 표시함.
   - 검색 입력은 키 입력마다 즉시 전체 렌더링하지 않고 짧은 지연 후 1회만 반영하도록 디바운스 처리함.
   - 카드 렌더링은 한 번에 전체를 만들지 않고 기본 표시 개수를 제한하고 더 보기 방식으로 추가 표시함.

2. 중복 실행/파일 잠금 방지
   - 실행 파일 교체 전 프로세스를 확인하고 종료 후 교체함.
   - 앱 내부에서는 저장 중 버튼 중복 클릭을 막아 같은 파일을 동시에 저장하지 않도록 함.

3. TSERP 딥블루 UI 적용
   - 배경, 헤더, 카드, 검색 영역, 공정 시간 칩을 딥블루 계열로 통일함.
   - 글자 대비는 밝게 조정하고 상태 색상은 딥블루 배경에서도 보이도록 재설정함.

4. 입력창 짤림 수정
   - 팝업 기본 크기를 확대하고 최소 크기를 올림.
   - 팝업 내부에 스크롤 가능한 입력 영역을 적용하여 작은 화면에서도 하단 버튼/공정 입력이 잘리지 않게 함.
   - 기본정보/Size/공정별 시간 Entry 폭과 컬럼 weight를 조정함.

5. 검증 및 배포
   - python -m py_compile machine_estimate_app.py 실행.
   - Tkinter 위젯 크기 검사로 공정 입력 Entry 10개가 표시되는지 확인.
   - PyInstaller로 EXE 재빌드 후 exe_release\Machine_Estimate.exe와 Machine_Estimate_latest.exe 갱신.
   - 작업 완료 후 er_plan.md에 실제 수정/검증 결과를 한글로 기록.

### 승인 필요
- 위 범위대로 수정 진행하려면 사용자 승인 후 코드 수정 및 실행 파일 재빌드를 진행함.

## 2026-08-05 07:51:46 멈춤 버그 개선, TSERP 딥블루 UI, 입력창 짤림 수정 완료

### 수정 내용
- 시작 시 견적_산정\2026년도 엑셀 전체 로드를 UI 스레드에서 직접 수행하지 않고 백그라운드 스레드 + Queue 폴링 방식으로 변경함.
- Tkinter를 백그라운드 스레드에서 직접 호출하지 않도록 수정하여 main thread is not in main loop 예외 가능성을 제거함.
- 검색 입력은 250ms 디바운스 방식으로 변경하여 키 입력마다 전체 카드가 재생성되지 않게 함.
- 카드 렌더링은 기본 40개만 표시하고 더 보기 버튼으로 40개씩 추가 표시하도록 변경함.
- 저장 중복 클릭 방지를 위해 저장 진행 중 재진입을 차단함.
- 메인 UI를 TSERP 계열 딥블루 모드로 변경함: 배경 #07111f, 패널 #0d1b2e, 카드 #132b46.
- 카드/공정 시간 칩/상태 배지를 딥블루 배경에서 읽기 쉬운 색상으로 조정함.
- 팝업 입력창을 1380x820, 최소 1240x720으로 확대하고 내부 스크롤 Canvas를 적용함.
- 공정별 시간 입력을 2열 압축 배치에서 1공정 1행 배치로 변경하여 130% 폰트에서도 Entry가 짤리지 않게 함.
- 새로 작성된 한국어 UI 문구가 깨진 부분을 유니코드 기준으로 복구함.

### 검증 결과
- python -m py_compile machine_estimate_app.py 통과.
- Tkinter 실제 위젯 검사 결과 견적_산정\2026년도 데이터 319건 로드 완료.
- 첫 화면 렌더링은 40개 카드 + 더보기로 제한되어 card_container 직접 자식 41개 확인.
- 팝업 크기 1380x820, Entry 20개, 최소 Entry 폭 104px 확인.
- 딥블루 배경 색상 #07111f 확인.
- exe_release\Machine_Estimate.exe 실행 확인 완료.
- exe_release\Machine_Estimate.exe, exe_release\Machine_Estimate_latest.exe 최신 빌드로 갱신함.

## 2026-08-05 설치 파일 제작 및 업데이트 방식 도입 계획

### 요청 내용 (nextup v.0.0.1MD)

- Inno Setup Compiler를 이용해 설치 파일을 제작한다.
- 실행 폴더에 업데이트 폴더를 만들고, 이후 기능/개선 사항은 그 폴더를 통한 업데이트 방식으로 배포한다.
- 버전은 v0.0.1로 시작한다.

### 확인한 현재 상태

- `Inno Setup 7`이 `C:\Program Files\Inno Setup 7\ISCC.exe`에 설치되어 있음.
- `machine_estimate_app.py`에는 버전 정보가 없음.
- 빌드 산출물은 `exe_release\Machine_Estimate.exe` (PyInstaller onefile, windowed).

### 사용자 결정 사항

- 설치 위치: Program Files (표준 Windows 설치 경로).
- 업데이트 방식: 수동 교체 방식. 설치 폴더 안에 `update` 폴더를 두고, 새 버전 설치 파일(Inno Setup installer)을 그 폴더에 넣으면 프로그램이 실행 시 감지해서 교체를 안내한다.

### 수정 계획

1. `machine_estimate_app.py`
   - `APP_VERSION = "0.0.1"` 상수를 추가하고 창 제목에 버전을 표시한다.
   - 앱 시작 시 `{실행파일 폴더}\update` 폴더를 확인해 `.exe` 설치 파일이 있으면 사용자에게 실행 여부를 확인하는 안내창을 띄운다.
   - 사용자가 승인하면 해당 설치 파일을 실행한다. 실제 파일 교체/재시작은 Inno Setup의 `CloseApplications`/`RestartApplications` 기능에 위임한다.
2. Inno Setup 스크립트 신규 작성: `installer\Setup.iss`
   - `AppId`는 고정 GUID로 지정해 이후 버전에서도 동일 설치로 인식되게 한다.
   - `AppVersion 0.0.1`, 설치 대상 `Program Files\기계 시트 표준 견적 입력 시스템`.
   - `exe_release\Machine_Estimate.exe`를 설치 파일에 포함한다.
   - 설치 시 `update` 빈 폴더를 함께 생성한다.
   - 시작 메뉴 바로가기를 생성하고, 바탕화면 아이콘 생성 여부는 설치 중 선택하게 한다.
   - `CloseApplications=yes`, `RestartApplications=yes`로 설정해 실행 중인 앱을 감지해 종료 후 재시작하도록 한다.
3. `ISCC.exe`로 `Setup.iss`를 컴파일해 설치 파일(`installer\Output\...`)을 생성한다.
4. 실제 설치(Program Files에 설치)는 관리자 권한이 필요하므로 이번 단계에서 자동 실행하지 않고, 생성된 설치 파일만 검증한다.
5. 수정 후 검증한다.
   - 파이썬 문법 검사
   - `ISCC.exe` 컴파일 성공 확인
   - 생성된 설치 파일 존재 확인
6. `ver_plan.md`에 구현/검증 결과를 기록한다.

### 작업 범위에서 제외

- 자동 설치 실행(Program Files 쓰기)은 사용자 승인 없이 진행하지 않는다.
- 기존 카드형 현황판 기능/저장 로직은 변경하지 않는다.

## 2026-08-05 업데이트 v0.0.2 선택 다운로드 계획

### 요청 내용 (`nextup v.0.0.2.MD`)

- 선택 다운로드 기능을 추가한다.
- 다운로드 양식은 기존 설정 양식인 `견적_산정\양식\견적용.xlsx` 구조를 따른다.
- 조임쇠 영역 기준 다중 검색 후 항목을 선택하고 다운로드할 수 있게 한다.
- 신규 입력 시에는 별도 창에서 신규분만 먼저 저장한 뒤 다운로드할 수 있게 한다.

### 현재 상태

- 앱/설치 스크립트 버전은 `0.0.1`이다.
- 현재 화면에는 업로드, 새 카드 추가, 날짜별 누적 저장, 단일 검색 기능이 있다.
- 현재 저장은 전체 저장 대상을 날짜별 누적 파일에 저장하는 방식이며, 선택 항목만 별도 양식으로 내보내는 기능은 없다.

### 수정 계획

1. `APP_VERSION`과 `Setup.iss` 버전을 `0.0.2`로 올린다.
2. 카드별 체크박스와 선택 상태 관리 기능을 추가한다.
3. 상단에 `전체 선택`, `선택 해제`, `선택 다운로드`, `신규 입력 다운로드` 버튼을 추가한다.
4. 검색어를 공백/쉼표 기준으로 나누어 다중 검색이 가능하게 한다.
5. `조임쇠 영역`은 별도 컬럼 확인 전까지 품번, 품명, Coment, Material, Size, 원본 파일명 검색 대상으로 적용한다.
6. 선택 다운로드는 기존 `견적용.xlsx` 양식을 복사해 선택 카드만 `기계` 시트 구조에 맞춰 기록한다.
7. 저장 위치는 파일 저장 창으로 사용자가 지정하고 기본 파일명은 `선택견적_YYYY-MM-DD.xlsx`로 제안한다.
8. 신규 입력 다운로드는 별도 입력 창에서 신규 항목만 작성한 뒤 그 항목만 먼저 다운로드 파일로 저장한다.
9. 기존 날짜별 누적 저장, 카드형 현황판, 자동 업로드, 업데이트 감지 방식은 유지한다.
10. 문법 검사, 선택/검색/다운로드 검증, EXE 재빌드, Inno Setup 설치 파일 생성을 진행한다.

### 승인 대기

- 사용자 승인 후 코드 수정, EXE 재빌드, `MachineEstimate_Setup_v0.0.2.exe` 생성을 진행한다.

## 2026-08-09 v0.0.9 저장형태·현황판·설정창 개편 계획

### 요청 내용 (`v0.0.9.md`)

1. 저장형태 변경 및 현황판 수정 - 누적 표시를 업로드 시간 내림차순(먼저 올린 카드가 아래)으로,
   현황판 맨 앞에 No 부여, 품명과 소재 사이에 Comment 표기.
2. Shift/Ctrl 키를 윈도우 파일 선택처럼 사용, 카드는 원클릭이 아니라 더블클릭으로 열기.
3. PDF 출력 제외(엑셀만), 견적서 시트 내용 전체 표기, 단가 합계 `####` 해소,
   회사/직위/작성자를 설정창에서 입력해 양식에 반영(회사명 `(주)텍스타`, 작성자 `생산부장 황성문`),
   `yyyy-mm-dd` 출력 날짜 기재.
4. 기능 제거 - 보조기능의 `신규 입력 다운로드`, `날짜별 누적 저장`, 카드의 기계 시트 파일 표기, 엑셀 행 표기.
   No는 엑셀 행이 아니라 1번의 화면 순번으로 기재.
5. 카드 입력 - Comment를 수동으로 늘려 2줄 이상 작성, Material에 열처리 체크 시 HRC 경도
   입력창을 아래에 활성화, HRC는 Min/Max를 각각 입력.
6. 기능키 추가 - 설정창을 별도로 만들고 저장하면 전체 적용. 공정별 단가(카드에서는 고정값),
   Material 목록 선택/삭제, 회사명·작성자 기재.
7. 양식 수정 - 견적서(시트 1번) 맨 위 작성 부분을 설정창에서 입력해 지정.

추가 요청(대화): 바탕화면 바로가기 아이콘이 아직 파이썬 아이콘이다. 업데이트 플랜은 개별 파일로
만들지 말고 이 `plan.md`에 통합해서 기록한다.

### 확인한 현재 구조 (직접 재현해 확인한 사실)

- **3-2 `견적서 내용이 전체 표기 안됨`의 실제 원인**: 현재 코드로 3 / 7 / 20 / 45건을
  `excel_io.export_items()`로 내보내 덤프한 결과, 견적서 항목 참조(C/E/G열)는 45건까지 전부 정상으로
  채워졌다(견적서 15행~합계행 직전, 기계 시트 합계행도 14→27→52로 정상 확장). 즉 항목 누락이 아니라
  **견적서 상단 블록이 양식의 플레이스홀더 그대로**인 것이 문제다.
  `B4='회사명㈜ 귀중'`, `B5='  담당자 : 대상업체 담당자 직위'`, `B6='  부   서 : 구매팀 '`,
  `B8='  사업명 : 사업명'`, `F5~F9`(대표자/사업자번호/주소/전화/담당자)가 한 번도 채워지지 않는다.
  이는 요청 7-1과 같은 지점이다.
- **3-6 날짜**: 견적서 `B7`은 `=기계!Q18` 참조인데 기계 `Q18`이 문자열 `'YYYY-MM-DD'`라 날짜가 안 나온다.
  기계 `Q18`에 실제 날짜를 쓰면 견적서 날짜는 참조로 자동 반영된다.
- **3-3 `####`의 실제 원인**: 기계 시트 **X열(최종단가) 폭 9.0**, W열(단가) 폭 12.0인데 서식이
  회계 서식(`_-* #,##0_-;...`)이라 좌우 여백까지 필요하다. 백만 단위부터 잘리고 합계는 확실히 깨진다.
  견적서 G/H는 각 13.0(병합 26)이라 여유가 있다.
- **푸터 위치는 고정이 아니다**: 회사명/날짜/작성자는 기계 `Q17/Q18/Q19`에 있는데, 데이터 행이 늘면
  합계행과 함께 밀린다(20건→Q30/31/32, 45건→Q55/56/57). 항상 **합계행 +3 / +4 / +5**였다.
- **선택 체크박스 기존 버그**: `ui/widgets.py`의 `make_checkbox`가 생성 시점의 `checked`(항상 False)를
  클로저에 고정해 `on_toggle(not checked)`가 언제나 `True`다. 슬롯 재사용 구조에서 체크 해제가 안 된다.
- **아이콘**: `dist`와 설치본(`C:\Program Files (x86)\Estimate\Estimate.exe`) 양쪽에서 아이콘 리소스를
  추출해 확인한 결과 **정상적으로 파란 `E` 아이콘이 박혀 있다**. 바탕화면 바로가기
  (`C:\Users\Public\Desktop\Estimate(견적).lnk`)도 대상 경로가 맞고 `IconLocation=",0"`이다.
  즉 남은 원인은 **셸 아이콘 캐시(v0.0.2~v0.0.4 시절 파이썬 아이콘이 같은 경로로 캐시된 것)**이며,
  보조 요인으로 `estimate.ico`의 4개 엔트리가 전부 PNG 압축(DIB 아님)이라는 점이 있다.
- **단가의 주인 문제**: `core/session.py`가 `rates`를 저장하고 `restore_session()`이
  `self.rates.update(...)`로 덮어쓴다. 설정창이 단가의 주인이 되면 세션 복원이 설정을 무효화한다.

### 수정 계획

1. **설정 저장소 신설** (`core/settings.py`) - `%LOCALAPPDATA%\MachineEstimate\settings.json`에 저장한다
   (`{app}`은 Program Files라 일반 권한으로 못 쓴다). 항목은 공정별 단가, Material 목록,
   회사명·직위·작성자, 견적서 상단 9칸이다. 파일이 없거나 깨져도 코드 기본값으로 뜬다.
   `core/session.py`에서 `rates` 저장/복원을 제거해 설정창을 단가의 유일한 주인으로 만든다.
2. **설정창** (`ui/settings_dialog.py`) - 공정별 단가 입력, Material 목록 추가/삭제,
   회사·작성자, 견적서 상단 입력을 한 창에 담는다. 저장하면 즉시 전체에 적용한다.
3. **현황판** (`ui/table.py`, `ui/dashboard.py`) - 맨 앞에 화면 순번 `No` 열을 추가하고,
   품명과 소재 사이에 `Comment` 열을 넣는다. 표시용 순번은 `display_no`로 따로 두고
   내부 키 `item["no"]`(선택·팝업·슬롯 매핑)는 그대로 둔다. 정렬은 `added_at` 내림차순에
   `no` 내림차순을 2차 키로 더해 같은 초에 올라온 카드의 순서를 고정한다.
4. **선택 동작** - `make_checkbox`의 클로저 버그를 먼저 고친다(클릭 시점의 상태를 읽게 한다).
   행 클릭은 단일 선택, `Ctrl+클릭`은 토글, `Shift+클릭`은 기준점부터 범위 선택으로 바꾸고,
   카드 열기는 **더블클릭**으로 옮긴다. 범위 계산은 `self.data` 순서가 아니라 화면에 보이는
   순서를 기준으로 한다.
5. **카드 입력창** (`ui/popup.py`) - Comment를 `tk.Text` 여러 줄로 바꾸고 아래에 드래그 핸들을 붙여
   높이를 수동으로 늘릴 수 있게 한다. Material은 설정창 목록에서 고르는 콤보박스로 바꾸고,
   그 아래 `열처리` 체크박스를 둔다. 체크하면 `HRC [Min] ~ [Max]` 입력칸이 활성화된다.
   공정별 단가는 입력칸에서 읽기 전용 표시로 바꾼다(설정창 값 고정). 정보 타일에서
   `기계 시트 폴더`/`기계 시트 파일`/`엑셀 행`을 빼고 `NO`는 화면 순번을 보여 준다.
6. **엑셀 출력** (`core/excel_io.py`) - 저장할 때 견적서 상단 9칸과 기계 시트 푸터
   (합계행 +3/+4/+5 = 회사명 / `yyyy-mm-dd` / 직위+작성자)를 설정값으로 채운다.
   기계 W·X열 폭을 넓혀 `####`을 없앤다(서식은 그대로 둔다). 열처리를 체크한 항목은
   Material 값 뒤에 `HRC00~00`을 붙여 기록한다(기계 시트에 열처리 전용 열이 없다).
7. **기능 제거** - 보조기능의 `신규 입력 다운로드`, 헤더의 `날짜별 누적 저장`, PDF 출력 전체
   (`core/pdf_export.py`, 출력 형식 선택창)를 걷어낸다. 누적 저장이 사라지면 `save_pending`(NEW 배지)을
   해제할 곳이 없어지므로, **선택 다운로드 성공 시 해제**로 옮겨 영구히 남는 배지를 만들지 않는다.
8. **아이콘** - `estimate.ico`를 16/32/48은 DIB, 256은 PNG인 표준 조합으로 다시 만든다.
   `installer/Setup.iss`는 `estimate.ico`를 `{app}`에 설치하고 바로가기에
   `IconFilename`을 직접 지정해 exe 단위 캐시를 우회한다. 기존 바로가기를 `InstallDelete`로 지우고
   다시 만들되, 그때 `desktopicon` 작업의 `unchecked`를 없앤다(체크를 안 하면 지우기만 하고
   새로 안 만들어 아이콘이 아예 사라진다). `ChangesAssociations=yes`와 `ie4uinit.exe -show`로
   셸 아이콘 캐시 갱신을 유도한다.
9. 버전을 `estimate_app/__init__.py`와 `installer/Setup.iss` 양쪽 모두 `0.0.9`로 올리고,
   `MachineEstimate_Setup_v0.0.9.exe`를 만든다(파일명 버전이 `updater`의 비교 기준이다).

### 작업 범위에서 제외

- `v0.0.7_plan.md` / `v0.0.8_plan.md`는 과거 기록이라 그대로 두고, 이번 계획만 이 파일에 통합한다.
- 견적 양식(`견적_산정\양식\견적용.xlsx`) 원본과 과거 저장 파일은 손대지 않는다.
  열 폭·상단 문구는 저장할 때 코드가 출력 파일에만 적용한다.
- `Machine_Estimate.spec`의 `_ssl`/`ssl`/`_hashlib` 제외는 유지한다(네트워크 기능이 없다).

## 2026-08-09 v0.0.9 구현 완료 및 설치 파일 생성

### 반영 내용

1. **설정 저장소 신설** (`core/settings.py`) — `%LOCALAPPDATA%\MachineEstimate\settings.json`.
   공정 단가 / Material 목록 / 회사·직위·작성자 / 견적서 상단 9칸을 담는다. 파일이 없거나
   깨져도 코드 기본값으로 뜬다. Material 기본 목록은 기존 견적 파일 18개의 Material 열을
   실제로 읽어(헤더 문구로 열을 찾아) 많이 쓰인 순서로 17개를 추렸다.
   `core/session.py`에서 `rates` 저장·복원을 **제거**했다 — 설정창이 단가의 주인인데 세션에도
   담아 두면 프로그램을 켤 때 옛 세션 값이 새 설정을 덮어쓴다.
2. **설정창** (`ui/settings_dialog.py`, 요청 6번) — 탭 3개(공정 단가 / Material 목록 /
   회사·견적서 양식). Material은 목록에서 여러 개를 골라 한 번에 지울 수 있고, 입력칸에서
   Enter로 바로 추가된다. 저장하면 `app.apply_settings()`가 단가·목록·회사 정보를 즉시
   전체에 적용하고 표의 금액을 다시 계산한다. 견적서 상단은 라벨(`담당자 :` 등)을 뺀
   알맹이만 입력받고 양식 문구는 저장할 때 코드가 붙인다.
3. **현황판** (`ui/table.py`, `ui/dashboard.py`, 요청 1번) — 맨 앞에 화면 순번 `No` 열,
   품명과 소재 사이에 `Comment` 열을 넣었다. 표시 순번(`display_no`)은 화면 차례이고
   내부 키 `item["no"]`(선택·팝업·슬롯 매핑)는 그대로 둔다. 정렬은 `added_at` 내림차순에
   카드 번호 내림차순을 2차 키로 더해, 한 번에 여러 건을 올려 시각이 같아도 순서가 고정된다.
   Comment의 줄바꿈은 표에서 한 줄로 접어(`one_line`) 행 높이가 깨지지 않게 했다.
4. **선택 동작** (요청 2번) — 클릭은 그 행만 선택, `Ctrl+클릭`은 더하고 빼기,
   `Shift+클릭`은 기준 행부터 범위 선택, 카드 열기는 **더블클릭**으로 옮겼다. 범위는
   저장 순서가 아니라 화면에 보이는 순서(`visible_nos`) 기준이다.
   **기존 버그도 함께 고쳤다**: `ui/widgets.py`의 체크박스가 만들 때의 상태(`checked`, 항상
   False)를 클로저에 담고 있어 어느 행을 눌러도 늘 "켜기"만 전달됐다(슬롯 재사용 구조라
   체크 해제가 안 됐다). 상태 판단을 현재 선택을 아는 대시보드로 옮겼다.
5. **카드 입력창** (`ui/popup.py`, 요청 5번) — Comment를 `tk.Text` 여러 줄로 바꾸고 아래
   손잡이를 끌어 높이를 늘릴 수 있게 했다(2~20줄). Material은 설정창 목록 콤보박스(목록에
   없는 소재는 직접 입력 가능)로 바꾸고, 그 아래 `열처리` 체크박스를 뒀다. 체크하면 바로
   아래 `HRC [Min] ~ [Max]` 칸이 활성화된다. 공정 단가는 입력칸에서 읽기 전용 표시로 바꿨다.
   정보 타일에서 기계 시트 폴더·파일과 엑셀 행을 뺐고 `NO`는 화면 순번을 보여 준다.
6. **엑셀 출력** (`core/excel_io.py`, 요청 3·7번) — 저장할 때마다 기계 시트 푸터
   (합계행 +3/+4/+5 = 회사명 / `yyyy-mm-dd` / 직위+작성자)와 견적서 상단 9칸을 설정값으로
   채운다. 견적서 날짜 칸(B7)은 `=기계!Q##` 참조인데, 항목이 늘면 푸터가 밀리므로 **참조도
   실제 푸터 행으로 다시 쓴다**(양식에 박혀 있던 `=기계!Q18`을 두면 20건만 넘어도 빈 칸을
   가리킨다). 단가·최종단가 열 폭을 15로 넓혀 `####`을 없앴다(서식은 그대로 뒀다).
   열처리를 체크한 항목은 Material 값 뒤에 `HRC58~62`를 붙여 적고, 다시 읽을 때
   `split_material()`이 소재와 경도로 되돌린다.
7. **기능 제거** (요청 3-1, 4번) — 보조기능의 `신규 입력 다운로드`, 헤더의 `날짜별 누적 저장`,
   PDF 출력 전체(`core/pdf_export.py`, 출력 형식 선택창)를 걷어냈다. 누적 저장이 사라져
   `save_pending`(NEW 배지)을 해제할 곳이 없어지므로 **선택 다운로드 성공 시 해제**로 옮겼다.
   `write_items_to_sheet()`의 행 재사용 로직(`excel_row`/`excel_file`)과 `paths.get_estimate_root_dir()`도
   쓰이지 않게 되어 함께 정리했다.
8. **아이콘** — `estimate.ico`를 16/32/48은 DIB, 256은 PNG인 표준 조합으로 다시 만들었다
   (외부 라이브러리 없이 zlib만으로 PNG를 풀어 DIB로 다시 담았고, 그림 자체는 그대로다).
   `installer/Setup.iss`는 `estimate.ico`를 `{app}`에 따로 설치하고 바로가기가 그 파일을
   직접 가리키게 했으며(`IconFilename`), 기존 바로가기를 지우고 새로 만들고,
   `ChangesAssociations=yes`와 `ie4uinit.exe -show`로 셸 아이콘 캐시 갱신을 유도한다.
   `desktopicon` 작업의 `unchecked`를 없앴다 — `InstallDelete`는 작업 선택과 무관하게 도는데
   체크를 안 하면 지우기만 하고 새로 안 만들어 아이콘이 아예 사라지기 때문이다.
9. **화면 배율(DPI) 대응 수정** — 고배율 화면에서 헤더 세 줄(제목/요약/안내)의 y 좌표가
   112px 안에 고정돼 있어 글꼴만 커지면서 서로 겹쳤다. 실제 글꼴 높이를 재서 줄을 쌓고
   캔버스 높이도 거기에 맞춘다. 팝업·설정창도 화면 안에서 잘리지 않도록 크기와 열 비중을
   다시 잡았다(저장 버튼 줄을 먼저 고정, 오른쪽 공정표에 최소 폭 확보, 긴 안내문에 wraplength).
10. 버전을 `estimate_app/__init__.py`와 `installer/Setup.iss` 양쪽 모두 `0.0.9`로 올렸다.

### 검증 결과

- **문법**: `python -m compileall estimate_app main.py` 통과.
- **엑셀 출력(3 / 7 / 20 / 45건)**: 실제 코드 경로(`excel_io.export_items`)로 파일을 만들어
  셀을 직접 읽어 확인했다. 기계 푸터 회사명 `(주)텍스타` / 날짜 `2026-08-09` / 작성자
  `생산부장 황성문`이 항목 수에 따라 밀린 자리(Q17→Q30→Q56)에 정확히 들어갔고, 견적서
  상단 9칸이 모두 설정값으로 채워졌으며, 날짜 참조도 `=기계!Q18 / Q31 / Q56`으로 실제
  푸터 행을 따라갔다. 단가·최종단가 열 폭 15 적용, 견적서 항목 참조 수는 3/7/20/45건 모두
  건수와 일치했다.
- **`####` 원인 재확인**: 고치기 전 상태에서 기계 X열(최종단가) 폭이 9.0, W열 12.0이고
  서식이 회계 서식이라 백만 단위부터 잘리는 것을 확인한 뒤 폭을 넓혔다.
- **HRC 왕복**: 열처리를 켠 카드를 저장한 파일을 다시 읽어 소재/열처리/Min/Max가 그대로
  복원되는 것과, 켜지 않은 카드가 영향받지 않는 것을 확인했다. `split_material()` 경계
  6가지(`HRC58~62`, `HRC 58 ~ 62`, `HRC58`, 경도 없음, 빈 값, `AL6061-T6`처럼 숫자·하이픈이
  섞인 소재명)도 확인했다.
- **정렬**: 같은 초에 올라온 5건이 카드 번호 내림차순으로 고정되고, 시각이 다른 카드가
  맨 위로 오는 것을 확인했다.
- **설정 저장/복원**: 저장 후 다시 읽어 회사명·작성자 조립(`부장 홍길동`)과 Material의
  중복·공백 제거를 확인했고, 검증 뒤 원래 값으로 되돌렸다.
- **세션에 단가가 없음**: 저장된 JSON에 `rates` 키가 없고 `session.load()` 결과에도 없음을 확인했다.
- **화면**: 실제 창을 띄워 `PrintWindow`로 창별 캡처를 떠 확인했다(백그라운드 세션이라
  화면 전체 캡처로는 창이 뒤에 있어 잡히지 않았고, DPI 인식을 안 하면 캡처가 잘린다는 것도
  같이 확인했다). 현황판(맨 앞 No, 품명↔소재 사이 Comment, 최신 카드가 맨 위, 선택 행
  틴트, 헤더 버튼이 `기계 시트 업로드 / 새 항목 추가 / 설정`으로 바뀐 것), 카드 입력창
  (NO 순번, Material 콤보, 열처리 체크 시 HRC 활성, 여러 줄 Comment, 읽기 전용 단가와
  금액, 하단 저장 버튼), 설정창 3개 탭을 모두 눈으로 확인했다. 표의 열 폭도 실측해
  합계 1809px로 창(1824px) 안에 들어가는 것을 확인했다.
- **빌드**: `python -m PyInstaller Machine_Estimate.spec` 성공. `dist/Estimate/Estimate.exe`
  2,832,274바이트, `_internal/assets`에 새 `estimate.ico`(19,341바이트)와 견적 양식이 포함된
  것을 확인했다. 빌드된 exe를 직접 실행해 창 제목이 `Estimate(견적) v0.0.9`로 뜨는 것과
  exe에 박힌 아이콘이 파란 `E`인 것을 확인했다.
- **설치 파일**: `MachineEstimate_Setup_v0.0.9.exe` 9,575,826바이트 생성(경고 없이 컴파일).

### 미실행 항목

- **관리자 권한으로 실제 설치해 바탕화면 아이콘이 바뀌는지는 확인하지 못했다.** 이번 작업은
  "exe·바로가기·아이콘 파일 쪽에서 할 수 있는 것은 다 했다"까지다. 설치 후에도 예전 아이콘이
  남아 있으면 셸 캐시가 원인이므로, 명령 프롬프트에서 `ie4uinit.exe -show`를 실행하거나
  탐색기를 다시 시작하면 즉시 갱신된다.
- `####` 해소는 열 폭을 15로 넓힌 것까지 확인했고, 엑셀이 실제로 그리는 화면은 확인하지
  못했다(openpyxl로는 렌더링을 볼 수 없다).
- 마우스로 직접 Shift/Ctrl 클릭하거나 더블클릭하는 상호작용은 이전 버전들과 같은 이유
  (백그라운드 세션·화면 배율)로 실측하지 못했다. 코드 경로와 캡처로 대신 확인했다.
- `v0.0.7_plan.md` / `v0.0.8_plan.md`는 과거 기록이라 손대지 않았다. 이번 계획과 결과만
  이 파일에 통합했다.
- 기존 `견적_산정` 폴더의 과거 파일과 양식 원본(`견적용.xlsx`)은 손대지 않았다.
  열 폭·상단 문구는 저장할 때 출력 파일에만 적용된다.

### 검토 후 추가로 고친 것

빌드까지 마친 뒤 코드를 다시 훑다가 두 가지를 더 잡았다. 둘 다 새로 생긴 `No` 열 때문에
드러나는 문제라 이번 버전에서 함께 고치고 다시 빌드했다.

1. **새 카드의 NO 표기가 어긋났다.** `새 항목 추가`로 만든 빈 카드는 내용이 없어 현황판
   목록에 잡히지 않는다(`has_item_data`). 그런데 `get_display_no()`가 화면 순번을 못 찾으면
   내부 카드 번호를 대신 돌려주고 있어서, 화면에는 1~40이 떠 있는데 입력창 제목만
   `[NO. 41]`로 뜨는 어긋남이 생겼다(요청 4-5가 바로 이 지점이다). 순번이 없으면 없다고
   알리도록 바꾸고(`None`), 입력창에서는 `신규`로 보여 준다.
2. **`저장 후 다음 항목 입력`이 남의 카드를 열 수 있었다.** 다음 번호(`no + 1`)가 이미
   쓰이고 있으면 그 카드를 그대로 열어 편집하게 되어 있었다(v0.0.9 이전부터 있던 동작).
   연속 입력 중에 기존 항목을 덮어쓰는 흐름이라, 비어 있는 번호를 새로 발급해 항상 새
   카드로 이어 가도록 고쳤다.

두 경로 모두 실제 앱을 띄워 확인했다 — 빈 카드는 순번이 붙지 않고 `신규`로 표기되며
목록 건수를 늘리지 않는 것, 내용을 채우면 곧바로 1번(가장 최근)으로 붙는 것,
`저장 후 다음 항목 입력`이 기존 번호와 겹치지 않는 빈 카드를 새로 만드는 것을 확인했다.
재빌드한 설치 파일은 `MachineEstimate_Setup_v0.0.9.exe` 9,577,963바이트다.

## 2026-08-09 v0.1.0 엑셀 출력 정리 · 현황판 구분선 · 소재 무게 계획

### 요청 내용

`v0.1.0.md` 원문 기준이다. 묶음이 넷이다.

1. **출력 엑셀 수정**
   1. 1번 시트(견적서)에서 아래 내용이 숨겨진다. 숨겨져 있는 행이지만 표기된 넘버에 맞추고
      한 행을 비운 뒤 합계가 나타나게 한다.
   2. 손봐야 할 곳을 노란색 셀로 표시해 두었다(`선택견적_2026-08-09.xlsx`).
      Comment처럼 내용이 셀 크기를 넘어서면 그에 맞게 크기를 조절한다.
   3. 견적 내용에 수식이 붙어 있어 오류가 우려된다. 수식을 제거한다.
2. **경도 입력 부분과 시간 입력 부분에는 항상 숫자만 입력**되게 한다.
3. **행에 대해 구분선**
   1. 아래 데이터 카드들에는 구분선 표기가 불필요하다.
   2. 맨 상단 제목 부분에만 행의 구분선이 연하게 필요하다.
   3. 구분선을 끌어 크기를 조절하게 하고, 대시보드의 마지막 형태를 저장해 다음에 그대로 로드한다.
   4. 소재 내용에 `…`이 생긴다. 기본 크기에서 조절이 필요하다.
4. **견적 카드 입력에 추가**
   1. 사이즈 부분에 각 소재의 비중에 따른 무게를 기록한다.
   2. 비중 데이터는 지금 입력된 소재 기준으로 하고, 추가 사항은 설정창에서 넣는다.

### 확인한 현재 구조

사용자가 준 `선택견적_2026-08-09.xlsx`(27건 출력본)와 양식 원본 `견적용.xlsx`를 openpyxl로
직접 열어 확인한 것만 적는다.

**(1) 견적서 시트에서 내용이 숨는 원인 — 요청 1-1**

양식 원본 `견적서` 시트는 처음부터 **22~56행이 hidden 상태**다(원본에서 확인). 항목 칸은
15행부터, 합계행은 57행(`B57 = ' 합 계 '`)이라 실제 칸 수는 42개인데, 눈에 보이는 것은
15~21행 7칸뿐이다.

출력본을 열어 보니 그대로였다.

| 행 | hidden | B(No) | C(품번 참조) |
|---|---|---|---|
| 15~21 | False | `=ROW()-14` | `=기계!$C7` ~ `$C13` |
| 22~41 | **True** | `=ROW()-14` | `=기계!$C14` ~ `$C33` ← **데이터가 있는데 숨어 있다** |
| 42~56 | True | `=ROW()-14` | 비어 있음 |
| 57 | False | ` 합 계 ` | `=SUM(G15:G56)` |

즉 27건을 출력했는데 화면에는 7건만 보인다. 합계 금액은 숨은 행까지 더하므로 맞지만,
견적서로 인쇄하면 20건이 사라진다.

원인은 `excel_io.py`가 **행의 hidden 속성을 한 번도 건드리지 않는 것**이다.
`sync_estimate_sheet()`는 참조 수식만 채우고, 행을 밀어 내리는 `shift_block_down()`은
값·서식·병합·행 높이만 옮기고 `hidden`은 옮기지도 지우지도 않는다(619줄 중 220~221행).

부수적으로 42~56행의 B열에는 `=ROW()-14`가 남아 있어, 숨김만 풀면 **번호만 찍힌 빈 행**이
드러난다. 요청의 "표기된 넘버에 맞추고"가 이 지점이다.

**(2) 노란색으로 표시된 셀 — 요청 1-2**

출력본 전체에서 사용자가 칠한 노란색(`FFFFFF00`)은 **`기계!E29` 한 칸**이다. 나머지 색은
양식 원본에도 있는 서식이다(V열 SUM 칸의 주황 `FFFFC000`, 5·6행 헤더 색).

`E29` 값은 줄바꿈이 든 Comment 7줄
(`M12*1.25 탭 / M14*1.5 탭 / sdlgnsld / sdgnlsdf / snlgskd / sndglsn / sdf`)이고,
그 행의 높이가 **112.0**이다. 다른 데이터 행은 전부 30.0이다. 사용자가 엑셀에서 직접 늘려
"이렇게 되어야 한다"를 표시해 둔 것으로 읽힌다(7줄 × 16pt ≒ 112).

`기계!E7`의 정렬을 확인해 보니 **`wrap_text`가 이미 True**다. 그러니 자동 줄바꿈은 이미
되고 있고, 모자란 것은 행 높이뿐이다. 줄바꿈 설정을 새로 켤 필요는 없다.

그런데 왜 높이가 안 늘어나는지 한 겹 더 들어가 보니, **v0.0.9가 엑셀의 자동 높이 맞춤을
꺼 버리고 있었다.** 엑셀은 행마다 `customHeight`(사용자가 높이를 직접 정했다는 표시)를
들고 있고, 이 표시가 켜져 있으면 자동 맞춤을 하지 않는다.

| 행 | 양식 원본 | 출력본 |
|---|---|---|
| 7~14 (양식 기본 8칸) | height 30.0, `customHeight=True` | 같음 |
| 28~30 (양식에 없던 행) | **높이 항목 자체가 없음 = 엑셀 자동** | height 30.0, `customHeight=True` |

8칸을 넘어가는 행은 양식 원본에서 높이가 지정돼 있지 않아 엑셀이 알아서 맞출 수 있는
상태였는데, `copy_row_style()`이 7행의 `30.0 + customHeight=True`를 그대로 복사해 넣으면서
그 여지를 없앴다. 사용자가 `E29`를 손으로 112까지 늘려야 했던 까닭이 이것이다.

또 하나 확인한 것: 데이터 행마다 Size 열에 `I16:K16` 같은 **병합**이 있다
(`merge_size_cell()`이 만든다). 엑셀은 병합된 칸을 자동 높이 계산에서 빼므로, 자동
맞춤에 맡기더라도 Size 칸이 길어지는 경우는 늘어나지 않는다. Comment가 든 E열은
병합이 아니라 영향을 받지 않는다.

**(3) 붙어 있는 수식 — 요청 1-3**

출력 파일에 남는 수식은 네 종류다. 전부 `excel_io.py`가 넣는다.

| 위치 | 수식 | 넣는 곳 |
|---|---|---|
| 기계 A열 | `=ROW()-6` | `apply_row_formulas()` |
| 기계 V열(SUM) | `=L7+M7+…+U7` | 〃 |
| 기계 W열(단가) | `=(L7*$L$5+…)*$G7` | 〃 |
| 기계 X열(최종단가) | `=ROUNDDOWN($W7,-3)` | 〃 |
| 기계 X 합계 | `=SUM(X7:X33)` | `update_summary_formula()` |
| 견적서 B열 | `=ROW()-14` | `sync_estimate_sheet()` |
| 견적서 C·E·G열 | `=기계!$C7` `=기계!$D7` `=기계!$X7` | 〃 |
| 견적서 G 합계 | `=SUM(G15:G56)` | 〃 |
| 견적서 B7(날짜) | `=기계!Q##` | `write_estimate_header()` |

금액 규칙은 이미 `core/pricing.py`가 화면용으로 같은 계산을 하고 있어(시간×단가 합 × 수량,
1,000원 단위 내림) 값으로 바꿔 적을 재료는 다 있다.

**(4) 현황판 표 — 요청 3**

`ui/table.py`는 헤더(`build_header`)와 각 행 슬롯이 `configure_columns()`로 **같은 grid
열 설정을 각자 적용**하는 구조다. 열 폭은 `assets/theme.json`의 `layout`에서 읽는다.
지금 화면에 그려지는 구분선은 헤더 아래 2px 파란 줄(`build_header_underline`) 하나뿐이고,
행 사이 가로선은 없다(v0.0.8에서 선 대신 배경 틴트로 바꿨다). 열과 열 사이 세로선은 없다.

행 슬롯은 `dashboard.py:47`의 `self.row_slots` 리스트에 담기고, 한 번 만든 슬롯은
지우지 않고 `hide_row_slot()`(grid_remove)으로 숨겼다가 다시 쓴다. 열 폭을 바꿀 때
이 리스트를 돌면 살아 있는 슬롯 전부에 닿을 수 있다(확인함).

창은 `dashboard.py:25`에서 `theme.json`의 `window_size`로 크기를 정한 뒤
`:56`의 `maximize_window()`가 **항상 `state("zoomed")`로 최대화**한다. 즉 지금은
시작할 때 언제나 최대화된 창으로 뜬다.

소재 칸의 `…`은 `table.py:228`의 `shorten(item["material"], 14)`이다. 기본 소재 목록의
길이를 세어 보면 `KS D 3752, SM45C` 16자, `AL6061P-T62, T651` 17자,
`Ti-6AL-4V SAE-AMS4911` 21자로 **17개 중 대부분이 14자를 넘는다**. 상수 14가 원인이다.

**(5) 카드 입력창 — 요청 2, 4**

- 시간 칸: 저장할 때만 `float()` 검사를 한다(`popup.py:418~425`). 입력 도중에는 글자가
  그대로 들어가고, 금액 칸에 `입력오류`만 뜬다.
- HRC 칸: 마찬가지로 저장 시점에만 검사한다(`popup.py:426~432`).
- Size: `_build_size_section()`이 블록(T×W×L) / 로드(Ø D×L) / 직접입력 세 가지를 받아
  `100T * 50W * 30L` 같은 **문자열**로만 저장한다. 치수 숫자를 따로 들고 있지 않다.
- 소재: `core/settings.py`의 `DEFAULT_MATERIALS` 17개 목록이 있고 비중 정보는 없다.
- 설정창(`ui/settings_dialog.py`)은 `ttk.Notebook`에 탭 3개를 `notebook.add()`로 붙이는
  구조다. Material 탭은 `tk.Listbox(selectmode=EXTENDED)` 한 개에 추가·선택삭제 버튼을
  달아 두었고, 저장할 때 `settings_store.save(payload)` → `app.apply_settings()` 순으로
  전체에 반영한다. 탭을 하나 더 붙이는 것은 같은 방식으로 된다(확인함).

### 수정 계획

**A. 견적서 숨은 행 (요청 1-1)** — `core/excel_io.py`

`sync_estimate_sheet()` 끝에 행 표시 여부를 정리하는 단계를 새로 넣는다. 행을 밀어 내린
뒤에 돌려야 한다 — 45건처럼 칸이 늘어나는 경우 옛 hidden 표시가 내용이 바뀐 행에 그대로
붙어 있기 때문이다.

    항목이 든 행(15 ~ 15+건수-1)   → hidden 해제
    그 다음 한 행                   → hidden 해제 + B열 번호를 비운다 (요청의 "한 행을 비우고")
    그 아래 남는 빈 칸              → hidden 유지, B열 번호도 비운다
    합계행                          → 항상 보이게

빈 칸의 `=ROW()-14`를 지우는 것은 숨김을 풀었을 때 번호만 찍힌 행이 보이지 않게 하려는
것이다. 값을 지울 때는 `ws.cell(...).value = None`으로 직접 대입한다
(`ws.cell(row, col, value=None)`은 openpyxl이 "인자를 안 준 것"으로 취급해 기존 값이 남는다).

합계행은 지금 위치(양식의 57행)에 그대로 둔다. 항목과 합계 사이의 숨은 빈 칸은 인쇄될 때
높이를 차지하지 않으므로, 화면·인쇄 모두 "항목 → 빈 한 줄 → 합계"로 보인다.

**B. Comment 행 높이 (요청 1-2)** — `core/excel_io.py`

위에서 본 대로 지금은 자동 조정이 아니다. 방법이 둘 있어 함께 적는다.

**(A) 프로그램이 높이를 계산해 넣는다 — 이쪽을 권한다**

항목을 다 적은 뒤 각 행의 높이를 다시 잡는다.

    필요한 줄 수 = max(각 셀마다,  줄바꿈으로 나눈 조각들을 그 열 폭(문자 수)으로 나눠 올림)
    행 높이 = max(양식 기본 30.0, 줄 수 × 16.0)

검사 대상은 Comment·품명·Material처럼 글이 긴 열이다. 한 줄이면 30.0 그대로라 기존 출력과
달라지지 않는다. 노란 셀 `E29`(7줄, 열 폭 25.33)를 이 규칙에 넣으면 112가 나와 사용자가
직접 맞춰 둔 높이와 같아진다 — 이것을 검증 기준으로 삼는다.

결과가 늘 같고 openpyxl만으로 검증할 수 있다는 것이 장점이다. 대신 글꼴과 열 폭으로
미루어 짐작하는 계산이라, 어긋나면 여전히 손으로 고쳐야 한다.

**(B) 높이 지정을 지우고 엑셀 자동 맞춤에 맡긴다**

`copy_row_style()`이 데이터 행에 높이를 복사하지 않게 하고 `customHeight`를 끈다. 즉
양식 원본의 28행 이후와 같은 상태로 되돌리는 것이다. 코드는 훨씬 단순하다.

다만 (1) Size 열 병합 때문에 그 칸이 길어지는 경우는 자동으로 늘어나지 않고,
(2) openpyxl로 만든 파일을 엑셀이 **열자마자** 자동으로 맞춰 그리는지는 openpyxl로는
확인할 수 없다. 실제로 엑셀에서 열어 봐야 알 수 있는 부분이다.

→ 기본은 (A)로 진행하되, (B)를 원하시면 그렇게 하고 실제 엑셀로 확인하는 절차를 넣겠다.
어느 쪽을 택하든 "양식에 없던 행까지 고정 높이로 만들어 버리던" 지금 동작은 고친다.

**C. 수식 제거 (요청 1-3)** — `core/excel_io.py`

계산 결과를 값으로 적는다. `apply_row_formulas()`는 지금 두 가지 일을 겸하고 있어
(항목을 채우기 **전에** 빈 행을 만들 때도 불린다) 둘로 나눈다.

    prepare_blank_row(ws, row, cols)      서식만 갖춘 빈 행. A·V·W·X를 None으로 비운다
    write_row_values(ws, row, item, ...)  A=순번, V=시간합, W=단가합, X=최종단가를 숫자로

견적서 쪽도 `=기계!$C7` 참조 대신 품번·품명·최종단가 값을 직접 적고, `=ROW()-14`는 순번
숫자로, 두 `=SUM(...)`은 합계 숫자로, 날짜 칸 `=기계!Q##`은 날짜 문자열로 바꾼다.
금액은 `core/pricing.py`의 `calc_row()`를 그대로 쓴다 — 화면과 엑셀이 어긋나지 않게
하려고 계산을 한 곳에만 두는 지금 규칙을 유지한다.

> 알아 두실 점: 수식을 없애면 **엑셀에서 5행 단가나 시간을 고쳐도 금액이 다시 계산되지
> 않는다.** 지금은 양식이 스스로 계산하지만, 앞으로는 프로그램이 계산한 값이 박혀서 나간다.
> 요청하신 대로 진행하되, 금액을 엑셀에서 직접 만지실 일이 있다면 알려 주십시오.

**D. 숫자만 입력 (요청 2)** — `ui/popup.py`

시간 10칸과 HRC Min/Max 2칸에 Tk의 입력 검증(`register` + `validatecommand`)을 건다.
숫자와 소수점만 통과시키고 그 외 글자는 아예 들어가지 않게 한다. 붙여넣기도 같은 검사를
거친다. 수량(Qty)은 정수만 받게 같은 방식으로 묶는다. 저장 시점 검사는 그대로 둔다
(입력 경로가 하나 더 생겨도 마지막 방어선은 남겨 둔다).

**E. 표 구분선과 열 폭 (요청 3)** — `ui/table.py`, `ui/dashboard.py`, `core/settings.py`

1. 데이터 행에는 선을 넣지 않는다(지금과 같다 — 요청 3-1).
2. 헤더의 열과 열 사이에만 연한 세로 구분선을 넣는다(요청 3-2). 색은 `theme.json`의
   `line`을 옅게 쓴다.
3. 그 구분선을 끌면 좌우 열 폭이 바뀌게 한다(요청 3-3). 마우스가 올라가면 `sb_h_double_arrow`
   커서로 바뀌고, 끄는 동안 실시간으로 폭이 바뀐다.
   - 헤더와 행 슬롯이 각자 `configure_columns()`를 적용하므로, 폭을 바꿀 때
     **헤더와 `dashboard.row_slots`의 모든 슬롯을 함께** 다시 설정한다. 헤더만 고치면
     표가 어긋난다. 숨겨 둔 슬롯도 나중에 다시 쓰이므로 함께 고친다.
   - 지금 품명(weight 3)과 Comment(weight 2)는 남는 폭을 나눠 갖는다. 사용자가 직접 끈
     열은 `weight=0`으로 고정해 끈 폭을 지킨다.
4. 바뀐 열 폭은 `settings.json`에 담는다(요청 3-3-1). `theme.json`은 프로그램에 같이
   묶여 나가는 자원이라 설치 폴더에 있고 일반 권한으로 못 쓴다. 사용자가 고치는 값은
   전부 `%LOCALAPPDATA%`로 보낸다는 지금 규칙을 따른다.
   **저장하는 것은 열 폭뿐이다** — 창 크기는 담지 않는다. 지금 프로그램은 켤 때마다
   `maximize_window()`로 항상 최대화하므로, 창 크기를 저장해 봐야 그 위에 덮어써진다.
   "마지막 형태"를 창 크기까지 포함해서 원하신다면 시작 시 최대화를 조건부로 바꿔야
   하는데, 그건 지금 동작을 바꾸는 일이라 따로 여쭙고 진행하겠다.
5. 소재 칸의 `…`(요청 3-4)은 상수 14를 없애고 **그 열의 실제 폭에서 글자 수를 역산**해
   자르도록 바꾼다. 열 폭을 조절할 수 있게 되므로 상수로 두면 폭을 넓혀도 `…`이 남는다.
   기본 소재 목록에서 가장 긴 `Ti-6AL-4V SAE-AMS4911`(21자)이 기본 폭에서 다 보이도록
   `theme.json`의 소재 열 기본 폭도 함께 넓힌다.

**F. 소재 비중과 무게 (요청 4)** — `core/model.py`, `core/settings.py`,
`ui/settings_dialog.py`, `ui/popup.py`, `ui/table.py`

1. Size 입력에서 이미 받고 있는 치수(T·W·L 또는 D·L)로 부피를 낸다.
   - 블록: `T × W × L`
   - 로드: `π × (D/2)² × L`
   - 직접입력: 치수를 알 수 없으므로 **무게를 비워 둔다**(추측하지 않는다)
2. 무게 = 부피(mm³) × 비중(g/cm³) ÷ 1000 → g, 다시 ÷1000 → kg. 소수 둘째 자리까지 보여 준다.
   수량과는 곱하지 않는다(1개 기준). 필요하시면 총중량도 함께 보여 드릴 수 있다.
3. 비중 표를 `settings.json`에 새로 담고 설정창에 **`소재 비중` 탭**을 만든다(요청 4-2).
   기존 세 탭과 같이 `notebook.add()`로 붙인다. 다만 비중은 (이름, 값) 두 칸짜리라
   Material 탭의 `Listbox` 대신 두 열짜리 `ttk.Treeview`를 쓰고, 아래에 이름·비중
   입력칸과 추가·수정·삭제 버튼을 둔다. 저장 경로와 반영 방식(`settings_store.save`
   → `app.apply_settings`)은 기존 탭과 같다.
4. 소재 이름과 비중을 맞추는 방법: 목록에 있는 이름이 `AL6061P-T62, T651` /
   `AL6061P-T651` / `AL6061-T651` / `AL6061-T6`처럼 **같은 재질을 네 가지로 적고** 있어,
   이름을 통째로 열쇠로 쓰면 하나만 고쳐도 나머지가 빠진다. 그래서
   **키워드 우선 매칭**(`AL6061` → 2.70 같은 규칙)으로 찾고, 특정 이름에만 다른 값을 주고
   싶으면 설정창에서 그 이름을 따로 등록해 덮어쓰게 한다. 어느 규칙에도 걸리지 않으면
   무게를 비우고 카드에 "비중 미등록"으로 알린다.
5. 무게는 카드 입력창의 Size 영역에 함께 표시하고, 카드 자료(`item["weight"]`)에 담아
   세션에 저장한다. 현황판 표에 무게 열을 넣을지는 아래에서 여쭙는다.

### 승인 전에 확인이 필요한 것

**(1) 비중 기본값** — 아래 값으로 시작해도 되겠습니까. 일반적으로 통용되는 물성값이지만
실제 견적 금액에 들어가는 값이라 확인을 받고 넣겠습니다.

| 키워드 | 비중 (g/cm³) | 해당하는 목록의 소재 |
|---|---|---|
| SM45C / SCM440 / STD61 / SS275 / A516 | 7.85 | 탄소강·합금강 계열 5종 |
| STS304 | 7.93 | STS304 |
| STS630 | 7.78 | STS630, KS D 3706의 STS630 |
| AL6061 | 2.70 | AL6061 계열 4종 |
| AL7075 | 2.81 | AL7075-T651, AL7075-T6 |
| AL2024 | 2.78 | AL2024-T3 |
| Ti-6AL-4V | 4.43 | Ti-6AL-4V SAE-AMS4911 |
| PEEK | 1.30 | PEEK |

**(2) 무게를 엑셀에도 적을지.** 기본은 "적지 않는다"로 잡았습니다. 기계 시트 6행 헤더에
무게 열이 없어서 넣으려면 양식 자체를 고쳐야 하는데, 이 프로그램은 양식 구조를 도메인
규칙으로 삼고 있어 함부로 열을 늘리지 않는 편이 안전합니다. 화면(카드·현황판)에만
보여 드리고 견적 파일에는 넣지 않겠습니다. 엑셀에도 필요하시면 Size 칸 뒤에 덧붙이는
방식(예: `100T * 50W * 30L  (2.85kg)`)으로 양식을 건드리지 않고 넣을 수 있습니다.

**(3) 현황판에 무게 열을 추가할지.** 열이 이미 10개라 좁아지는 것이 걱정입니다.
기본은 "카드 입력창에만 표시"로 잡았습니다.

### 작업 순서와 검증 계획

승인해 주시면 아래 순서로 진행하겠습니다.

1. `excel_io.py` (A·B·C) — 엑셀 출력이 가장 위험한 부분이라 먼저 하고, **3 / 7 / 20 / 45건**
   네 가지로 파일을 만들어 셀을 직접 읽어 확인한다(HANDOVER 7절 규약).
   - 견적서에서 데이터 행이 전부 보이는지(hidden 해제), 그 다음 한 행이 비어 있는지,
     합계행이 보이는지
   - 45건은 행이 밀려 내려가는 경로라 옛 hidden 표시가 남지 않는지 특히 확인한다
   - 수식이 하나도 남지 않았는지(모든 셀의 값이 `=`로 시작하지 않는지) 전수 검사
   - 수식을 없앤 뒤 금액이 이전 버전과 같은지 대조
   - `E29` 사례(7줄 Comment)의 행 높이가 112가 되는지

   수식을 값으로 바꿀 때 걸리는 호출 순서가 두 군데 있다. 중간 상태가 아니라
   **저장된 파일의 최종 상태**로 검사한다.
   - `write_items_to_sheet()`는 `update_summary_formula()`를 항목을 채우기 **전에**
     한 번(533행), 채운 **뒤에** 또 한 번(543행) 부른다. 지금은 수식이라 어느 쪽이든
     같은 결과지만, 값으로 바꾸면 앞선 호출이 합계 0을 적는다. 뒤 호출이 덮어쓰긴 해도
     앞 호출에서는 합계를 적지 않도록 정리한다.
   - 행 높이는 `copy_row_style()`이 항목마다 7행 높이 30.0을 덮어쓰므로, 계산한 높이는
     **항목을 다 채운 뒤에** 적용해야 한다.
2. `popup.py` (D) + Size·무게 (F)
3. `table.py`·`settings.py`·`settings_dialog.py` (E·F)
4. 문법 검사 → 창을 직접 띄워 캡처 확인 → 버전을 `estimate_app/__init__.py`와
   `installer/Setup.iss` **양쪽 모두** `0.1.0`으로 올림 → 빌드 → 설치 파일 생성

### 미실행 항목

- 아직 아무 코드도 고치지 않았다. 이 문서는 계획이며 승인 전이다.
- v0.0.9는 `Estimate` 브랜치에 아직 병합되지 않았다. v0.1.0을 그 위에 얹으면 병합하지 않은
  변경이 두 버전 쌓인다. 먼저 v0.0.9를 병합할지 함께 결정해 주시면 좋겠다.
- 노란색 표시는 `기계!E29` 한 칸만 확인했다. 다른 곳도 손봐야 한다면 알려 주시기 바란다.
- 엑셀의 자동 높이 맞춤(위 B안의 (B))이 openpyxl로 만든 파일에서 열자마자 도는지는
  확인하지 못했다. `customHeight` 값까지만 확인했고, 실제 렌더링은 엑셀로 열어야 안다.
- "항목 → 빈 한 줄 → 합계"가 엑셀 화면과 인쇄에서 실제로 그렇게 보이는지는 openpyxl로
  확인할 수 없다. 숨긴 행은 높이를 차지하지 않는다는 규칙에 기댄 판단이며, 실제 모양은
  구현 후 엑셀로 열어 확인해 주셔야 한다(`####`와 같은 성격의 미확인 항목이다).

## 2026-08-09 v0.1.0 구현 완료 및 설치 파일 생성

### 반영 내용

1. **견적서 숨은 행 해제** (`core/excel_io.py`, 요청 1-1) — `sync_estimate_sheet()`가
   항목이 든 행은 `hidden=False`, 그 다음 한 행은 보이되 번호를 비우고, 나머지는
   `hidden=True`로 정리한다. 45건처럼 행이 밀려 내려간 경우에도 새로 옮긴 위치 기준으로
   다시 계산해 옛 hidden 표시가 남지 않는다.
2. **Comment 행 높이 계산** (`core/excel_io.py`, 요청 1-2) — `fit_row_height()`가
   `wrap_text`가 켜진 칸(지금 양식은 Comment만)의 줄 수를 세어
   `max(30, 줄수 x 16)`으로 행 높이를 정한다. B안(엑셀 자동 맞춤)이 아니라 A안(계산해서
   넣기, 계획서에서 안내한 대로)으로 갔다 — 사용자가 `E29`를 손으로 늘려 뒀던 112가
   그대로 재현되는 것으로 검증했다.
3. **수식 제거** (`core/excel_io.py`, 요청 1-3) — 기계 시트 A/V/W/X, 견적서 B/C/E/G,
   두 시트의 합계, 견적서 날짜 칸까지 전부 계산된 값으로 적는다. 계산은
   `core/pricing.calc_row()` 하나만 쓴다(화면과 어긋나지 않게 하려는 지금 규칙 유지).
   `apply_row_formulas()`를 `prepare_blank_row()`(빈 칸 정리) /
   `write_row_values()`(항목 값 기재)로 나눴고, 합계도
   `write_summary_label()`(라벨) / `write_summary_total()`(항목을 다 쓴 뒤 값)로 나눠
   "항목 쓰기 전에 합계 0을 적었다가 나중에 덮어쓰는" 순서 문제를 없앴다.
   엑셀에서 단가·시간을 직접 고쳐도 더 이상 재계산되지 않는다(계획서에서 미리 안내).
4. **숫자만 입력** (`ui/popup.py`, 요청 2) — 시간 10칸, HRC Min/Max, Qty, Size의
   T/W/L/D칸에 `validate='key'` 검증을 걸었다. 숫자와 소수점(Qty는 정수만) 외의
   글자는 입력 자체가 안 먹는다. 저장 시점 검사는 마지막 방어선으로 남겼다.
5. **소재 비중과 무게** (`core/model.py`, `core/settings.py`, `ui/popup.py`,
   `ui/settings_dialog.py`, 요청 4) — `model.calc_weight_kg()`가 치수(블록:
   T×W×L, 로드: π×(D/2)²×L)와 비중으로 무게(kg)를 낸다. 직접입력이거나 비중을
   못 찾으면 계산하지 않는다(추측하지 않는다). `settings.resolve_density()`는
   정확히 같은 이름을 먼저 찾고, 없으면 소재 문자열에 포함되는 가장 긴 키워드를
   고른다 — `AL6061`(키워드)과 `AL6061P-T651`(특정 표기)이 둘 다 있어도 더 구체적인
   쪽이 이긴다. 기본 비중 8종(승인받은 값)을 `DEFAULT_DENSITIES`로 넣었다.
   설정창에 **소재 비중** 탭을 새로 붙여 Treeview로 추가·수정·삭제한다.
   카드 입력창 Size 영역에 "무게(1개)"를 실시간으로 보여 주고 저장 시
   `item["weight"]`에 담는다. **엑셀 출력에는 적지 않는다**(승인받은 대로) —
   기계 시트 헤더에 무게 열이 없어 양식을 고쳐야 하는데, 이 프로그램은 양식 구조를
   도메인 규칙으로 삼고 있어 함부로 늘리지 않았다.
6. **현황판 열 구분선·드래그 리사이즈** (`ui/table.py`, `ui/dashboard.py`,
   `ui/theme.py`, 요청 3-1~3-3) — 헤더 열 사이에만 연한 세로 구분선을 두고(데이터
   행에는 없음), 끌면 좌우 열 폭이 바뀐다. 폭은 `app.col_widths`에 살아 있고 끄는
   즉시 헤더와 `dashboard.row_slots`의 모든 슬롯에 같이 반영되며, 손을 떼면
   `settings.json`의 `col_widths`에 남아 다음에 켤 때도 그대로 열린다. 한 번이라도
   사용자가 끈 열은 `weight=0`으로 고정해 창 크기가 바뀌어도 그 폭을 지킨다(그
   전까지는 품명 3 : Comment 2 비율로 남는 폭을 나눠 갖는 지금 동작 그대로).
   **창 크기 자체는 저장하지 않는다** — 계획서에서 안내한 대로, 지금 프로그램은
   시작할 때 항상 최대화(`maximize_window()`)해서 창 크기를 저장해도 의미가 없다.
7. **소재 칸 `…` 해소** (`ui/table.py`, `ui/theme.py`, `core/config.py`, 요청 3-4) —
   상수 글자 수(14) 대신 `Theme.table_cell_font.measure()`로 실제 글꼴 폭을 재
   `fit_text()`가 칸에 맞게 자른다. 열 폭을 조절할 수 있게 되면서 상수로는 계속
   `…`이 남기 때문이다. 기본 소재 열 폭도 120 -> 150으로 넓히고, 여유가 있던
   금액 칸(150 -> 120, 억 단위까지 실측 확인)에서 같은 만큼 덜어 전체 폭 합계는
   그대로 뒀다.
8. 버전을 `estimate_app/__init__.py`와 `installer/Setup.iss` 양쪽 모두 `0.1.0`으로
   올렸다.

### 구현 중 잡은 버그

`Tk`의 `grid_bbox(column=idx)`를 `row` 없이 부르면 그 열 하나가 아니라 **그리드
전체의 bbox를 돌려준다**(문서에 명시돼 있지 않아 직접 겪었다). 그 탓에 구분선 9개가
전부 같은 자리(x=1306)에 겹쳐, 소재 헤더 글자 왼쪽 절반을 가리는 것을 화면 캡처로
발견했다. `grid_bbox(row=0, column=idx)`로 고쳐 해결했다(`table.py`의
`_reposition_resizers()`에 이유를 주석으로 남겼다).

### 검증 결과

- **문법**: `python -m compileall estimate_app main.py` 통과.
- **엑셀 출력(3 / 7 / 20 / 45건)**: `excel_io.export_items()`로 실제 파일을 만들어
  검사했다.
  - 두 시트 어디에도 `=`로 시작하는 값(수식)이 하나도 없음을 전수 검사로 확인.
  - 견적서에서 항목 행이 전부 보이고(hidden 해제), 그 다음 한 행이 보이되 번호가
    비어 있고, 나머지가 숨어 있고, 합계행은 항상 보이는 것을 4가지 건수 모두 확인.
  - 견적서·기계 시트의 합계 금액이 `pricing.calc_row()`로 낸 기댓값과 정확히 같고,
    항목별 최종단가도 계산값=기계 시트 값=견적서 값 셋이 모두 일치.
  - 남는 빈 칸(예: 3건 저장 시 원본 양식의 4~13행)은 예전처럼 수식이 남아 0원이
    찍히던 것과 달리 완전히 빈 칸임을 확인.
  - 7줄 Comment(`기계!E29` 재현 사례)의 행 높이가 정확히 112로 계산되고, 1줄인
    행은 기존과 같은 30을 유지하는 것을 4가지 건수 모두 확인.
- **숫자만 입력**: `_numeric_entry`에 실제 `Entry` 위젯을 만들어 `"abc"`,
  `"1.2.3"`, `"12a3"` 삽입을 시도해 전부 차단되고 `"123"`, `"1.5"`는 그대로
  들어가는 것을 확인.
- **무게 계산**: `calc_weight_kg('block', t=100, w=50, l=30, density=2.70)` = 0.405kg,
  `calc_weight_kg('rod', d=20, l=100, density=7.85)` = 0.2466kg으로 계산이 맞고,
  `resolve_density()`가 `AL6061P-T651`/`AL6061-T6`/`AL7075-T6`/
  `Ti-6AL-4V SAE-AMS4911`을 각각 맞는 비중으로 찾고 등록에 없는 이름은 `None`을
  돌려주는 것을 확인.
- **화면**: 실제 창을 띄워 `PrintWindow`로 캡처했다(백그라운드 세션이라 전체 화면
  캡처 대신 창 핸들을 직접 그리게 함).
  - 현황판: 5건을 채운 표에서 헤더 구분선이 얇게 보이고 소재 열 텍스트가 잘리지
    않는 것, No/Comment/소재 순서가 맞는 것을 확인. (버그 발견 전 캡처에서는 소재
    헤더 글자가 가려져 있었고, 고친 뒤 캡처에서는 정상.)
  - 카드 입력창: T=100/W=50/L=30을 입력하자 "무게(1개) 0.41 kg"이 바로 뜨는 것,
    여러 줄 Comment가 스크롤 가능한 칸에 들어가는 것, 시간·HRC 칸이 읽기 전용
    단가와 함께 보이는 것을 확인.
  - 설정창: 새 **소재 비중** 탭에 기본값 12개(키워드 8종, 강 계열이 5개 이름으로
    갈라져 총 12행)가 Treeview로 뜨고, 이름/비중 입력칸과 추가·수정·삭제 버튼이
    보이는 것을 확인.
- **빌드**: `python -m PyInstaller Machine_Estimate.spec` 성공.
  `dist/Estimate/Estimate.exe`를 직접 실행해 창 제목이
  `Estimate(견적) v0.1.0`으로 뜨는 것을 확인했다(사용자가 그 사이 이미 실행해 둔
  v0.0.9 인스턴스는 건드리지 않고 새로 띄운 것만 종료했다).
- **설치 파일**: `ISCC.exe`로 경고 없이 컴파일. `MachineEstimate_Setup_v0.1.0.exe`
  9,589,780바이트. 워크트리 삭제 시 사라지지 않도록 사용자 배포 폴더
  (`...\orca\workspaces\Estimate\Estimate\installer\Output\`)에도 복사해 뒀다.

### 미실행 항목

- **관리자 권한으로 실제 설치해 확인하는 것은 이번에도 하지 못했다.** exe를 직접
  실행해 창이 뜨는 것까지만 확인했다(v0.0.9 때와 같은 한계).
- **엑셀을 직접 열어 눈으로 보는 것은 하지 못했다.** openpyxl로 hidden 속성·행
  높이·수식 유무는 확인했지만, 실제 엑셀 프로그램이 "항목 → 빈 한 줄 → 합계"를
  그 모양대로 그리는지, Comment 칸이 112pt에서 7줄을 다 보여 주는지는 openpyxl로
  볼 수 없다. 계획서에서 미리 밝힌 한계 그대로다.
- **마우스로 직접 헤더 구분선을 끄는 상호작용은 실측하지 못했다** — 코드 경로
  (`app.col_widths` 갱신 → `_apply_column_widths()` → `_persist_column_widths()`)를
  직접 호출해 같은 효과(소재 열을 250px로 넓히자 "Ti-6AL-4V SAE-AMS4911"이
  잘리지 않고 다 보이는 것, `settings.json`에 남는 것)를 확인했다. 이전 버전들과
  같은 이유(백그라운드 세션)로 실제 드래그는 못 해 봤다.
- v0.0.9는 여전히 `Estimate` 브랜치에 병합되지 않았다. 이번 v0.1.0도 같은
  `worktree-v0.0.9` 브랜치 위에 이어 커밋했다 — 두 버전이 함께 병합 대기 중이다.

## 2026-08-09 v0.1.0 추가 검증 — 인쇄 범위·재업로드 왕복

구현 완료 보고 뒤 다시 점검하다가 놓칠 뻔한 두 가지를 추가로 확인했다.

### 확인한 것

**(1) 견적서 인쇄 범위(`print_area`).** 요청 1-1의 원래 문제가 "인쇄하면 사라진다"였는데,
`sync_estimate_sheet()`는 항목이 42건을 넘어 행이 늘어나는 경우에만 `print_area`를
넓힌다. 8~42건 구간(hidden만 풀고 행은 늘리지 않는 경우)에 인쇄 범위가 그대로인 채라면
화면에서는 보여도 인쇄에서는 잘릴 수 있어 다시 열어 확인했다.

양식 원본의 `견적서!print_area`가 처음부터 `$B$1:$H$57` — 즉 항목 칸 42개(15~56행)와
합계행(57행)을 이미 전부 포함하고 있었다. 그래서 hidden만 풀어도 인쇄 범위 안이다.
3/7/20/45건 모두 `print_area`의 마지막 행이 합계행 이상인 것을 직접 확인했다(45건은
기존 코드가 이미 처리하던 확장 경로라 `60`까지 넓어졌다).

**(2) 재업로드 왕복.** 수식을 값으로 바꾸면서 기계 시트에 남는 값의 성질이 바뀌었으므로
(`read_cards_from_workbook()`이 그 값을 다시 읽는 다른 주요 흐름이다), 20건을 저장한 뒤
다시 업로드해 품번·품명·기종·Comment·가능여부·Qty·Size·시간·소재·열처리·HRC 전부가
원래 값과 정확히 같은 것을 확인했다.

### 검증 결과

- print_area: 3/7/20/45건 모두 합계행까지 포함(PASS).
- 재업로드 왕복: 20건 전 필드 일치(PASS).

두 가지 모두 별도 코드 수정 없이 통과했다 — 기존 구조가 이미 감당하고 있었다.

## 2026-08-09 v0.1.1 — Mill/Lathe 가공조건 산출기 보조 기능 (구현 완료)

### 요청 내용

`v0.1.1.md` 원문(사용자 원본은 `C:\Users\SumH\orca\workspaces\Estimate\Estimate\v0.1.1.md`,
이번에 저장소 루트로 복사해 둔다).

**기능 추가**

1. `01.견적_산출_입력.hta`
2. `04.CSS_조건 산출기.hta`
3. 위 2가지 hta 프로그램의 내용을 보조 기능으로 넣어서 사용
4. `01.견적_산출_입력`은 **Mill 계열**, `04.CSS 조건 산출기`는 **Lathe**로 명명

**각 기능의 사용 효과**

1. mill 계열 조건 산출기는 블록 및 로드에서 mill 계열 선택했을 때 사이즈 조건을 넣고
   내부 조건대로 계산할 수 있게
2. 로드 계열은 계산식이 없음. 계산 방식은 mill 계열 참조
3. Lathe로 선택 시 Lathe의 가공 시간 조건 계산으로 산출

**추가 지시 (계획 검토 중 받은 것)**

> 반대로 지금 프로그램을 산출용으로 쓰고, 견적표 카드를 선택하면 해당 소재 사이즈를
> 계산기 프로그램으로 전달하는 방식

계산기가 견적을 끌고 가는 구조가 아니라 **견적 프로그램이 주인이고 계산기가 카드에서
값을 받아 도는 구조**로 잡는다. 아래 B절을 그에 맞춰 다시 썼다.

> 결과는 카드로 되돌릴 필요 없음. UI만 이질감 없게 설정

계산 결과를 카드에 써 넣는 길은 넣지 않는다. 대신 **산출기 창이 지금 화면과 같은 색·글꼴·
위젯으로 보이게 하는 것**을 별도 항목으로 잡았다(아래 C절).

### 확인한 현재 구조

두 hta 파일과 지금 앱을 실제로 열어 확인한 것만 적는다.

**(1) `04.CSS_조건 산출기.hta` — 계산 로직이 완전히 들어 있다.**

`doCalc()` 하나에 선삭 계산이 전부 들어 있고, 이식에 필요한 값이 모두 파일 안에 있다.

| 항목 | 확인한 내용 |
|---|---|
| 장비 상수 | `BASE_POWER = 22.0` kW, `MACHINE_MAX_RPM = 3500` (DN솔루션 PUMA 2600Y) |
| 인서트 | 대구텍 CNMG 120408 · 100% 기준 |
| 재질 | 9종. `<option value>`가 곧 비절삭저항 kc(N/mm²) — Al 700 / 주철 1350 / S45C 1800 / Ti6Al4V 1950 / SCM 2200 / SUS304 2400 / 열처리강 2750 / 인코넬 3100 / 청동 780 |
| 재질별 권장값 | `recommendations{v, f, ap, maxR}` 9행 |
| 인서트 재종표 | `insertGrades{grade, coat, iso, desc}` 9행 |
| SFM 변환 | `SFM_FACTOR = 3.28084`, V↔SFM 양방향 동기화 |
| 입력 | D_max, D_min, SFM, V, G50 제한 RPM, F(mm/rev), Ap(mm), L(mm), 허용동력(kW) |

계산식(파일에 적힌 그대로):

- `minRPM = 1000·V / (π·Dmax)`, `maxRPM = 1000·V / (π·Dmin)` — 둘 다 G50 제한값으로 상한 처리
- `feedMin = minRPM × f` (Dmax 기준 분당 이송)
- `MRR = V × ap × f` (cm³/min)
- `Fc = (ap × f × kc) / 9.81` (kgf)
- `totalVol = π(Dmax² − Dmin²)·L / 4000` (cm³)
- 패스 수 = `ceil((Dmax−Dmin)/2 / ap)`, **단 L ≤ 1.6이면 단면 특수공정으로 패스 1회·Z이동 없음**
- 가공시간 `totalSec = (L / feedMin) × 60 × passes` (L ≤ 1.6이면 `(반경여유/feedMin)×60`)
- `Pc = kc × V × f × ap / 60000` (kW), 설비 부하율 = Pc/허용동력, 스핀들 부하율 = Pc/22kW
- 동력 스케일: `ps = 입력동력/22` (0.2~1.5로 제한), `ap ×= ps`, `f ×= √ps`
- `Dmax < Dmin`이면 두 입력칸을 오류 표시

설정값은 레지스트리(`HKCU\Software\CNCLatheCalcV2\`)에 저장한다 — 이식할 때는 앱의
`settings.json`으로 옮긴다.

**(2) `01.견적_산출_입력.hta` — 계산식이 하나도 없다. 이번 계획의 가장 큰 변수다.**

파일을 끝까지 읽었다. 스크립트에 있는 함수는 `buildRows` / `refreshPreview` /
`clearItemRows` / `clearAllInputs` / `bindFocusSelect`뿐이고, **가공 조건이나 시간을
계산하는 코드가 전혀 없다.** 근거:

- 공정 합계·산출금액·견적단가 칸은 전부 `readonly`이고, 그 값을 채우는 코드가 없다
  (`refreshPreview()`는 이미 들어 있는 `quoteUnit` 값을 그대로 표에 옮겨 적을 뿐이다).
- 파일 안에 직접 적혀 있다 — `"산출 방식과 계산 방식은 다음 단계에서 연결 예정"` (339행).

그리고 이 파일이 실제로 담고 있는 것(견적 기본정보, 공정별 단가 10종, 품목 7행 입력,
견적서 미리보기)은 **이미 지금 앱에 다 들어와 있다** — 설정창(단가·회사정보) + 카드
입력 팝업(품목) + 현황판(미리보기)이 그 자리를 대신한다.

즉 "01.hta의 내용을 Mill 계열 산출기로 넣는다"는 그대로는 성립하지 않는다.
**Mill 계열 산출기는 04.hta(선삭)의 구조를 본떠 새로 설계해야 한다.**

**(3) 지금 앱 — 형상은 있지만 '가공 계열'은 없다.**

- `ui/popup.py`의 Size 섹션에 블록(T×W×L) / 로드(D×L) / 직접입력 라디오가 있다.
  다만 이건 v0.1.0에서 **무게 계산용 형상**으로 넣은 것이고, Mill/Lathe 같은 가공 계열
  구분은 아니다. `core/model.calc_weight_kg()`도 부피 계산까지만 한다.
- 팝업에는 산출기가 필요로 하는 값이 **없다**: 목표 치수(Lathe의 D_min에 해당),
  공구경, 날수, Vc, 이송, 절입량, 허용동력. 즉 산출기는 Size 값을 **미리 채워 받되**
  자기 입력칸을 따로 갖는 별도 화면이어야 한다.
- 공정은 `MACHINE_KEYS` 10종(`m_5axis, m_4axis, m_3axis, m_lathe, m_general, m_finish,
  m_cmm, m_grind, m_jig, m_prog`). `core/pricing.calc_row()`는
  `금액 = Σ(시간 × 단가) × 수량`이므로 **카드의 시간은 1개 기준·시간(h) 단위**다.
- 현황판 검색줄 오른쪽에 이미 **"보조 기능"** 영역이 있다(전체 선택/선택 해제/선택 다운로드).
  요청의 "보조 기능으로 넣어서"와 자리가 맞는다.
- 설정창에 v0.1.0의 **소재 비중** 탭이 있다 — 재질별 표를 사용자가 늘려 쓰게 하는
  선례가 이미 있고, 사용자도 그때 "추가 사항은 설정창에서 넣을 것임"이라고 했다.

### 수정 계획

#### A. `core/machining.py` 신설 — 화면과 분리된 순수 계산

`pricing.py`가 금액 규칙의 단일 출처인 것과 같은 이유로, 가공 조건 계산도 UI에서 떼어
낸다. 함수는 값만 받아 값만 돌려주고 tkinter를 import 하지 않는다.

- `calc_lathe(...)` — 04.hta `doCalc()`를 그대로 옮긴다. **식·상수·분기(L ≤ 1.6 단면
  특수공정, G50 상한, Dmax<Dmin 오류, 동력 스케일 ps/√ps)를 하나도 바꾸지 않는다.**
  옮기면서 값이 달라지면 그건 이식이 아니라 개조다.
- `calc_mill(...)` — 새로 만든다. 선삭과 같은 계열의 표준 밀링 식을 쓴다.

  | 항목 | 식 |
  |---|---|
  | 주축 회전수 | `n = 1000·Vc / (π·D_tool)` (최고 RPM 상한 적용) |
  | 분당 이송 | `Vf = n · fz · z` (fz=날당 이송, z=날수) |
  | 칩 배출량 | `Q = ae · ap · Vf / 1000` (cm³/min) |
  | 절삭 동력 | `Pc = Q · kc / 60000` (kW) |
  | 제거 체적 | 블록 `(T·W·L − t·w·l)/1000`, 로드 `π(D²−d²)·L/4000` (cm³) |
  | 가공 시간 | `제거체적 / Q` (분) |
  | 부하율 | Pc/허용동력, Pc/장비 정격 — 04.hta와 같은 판정 구간(80/60/30%) |

  선삭이 D_max→D_min으로 깎는 것과 같은 구조로, 밀링도 **소재 치수 + 목표 치수** 두
  벌을 받는다. 요청 2("로드 계열은 계산식이 없음, mill 계열 참조")는 **로드 소재를 밀링할
  때도 위 식을 그대로 쓰고 제거 체적만 원통 식으로 바꾼다**는 뜻으로 해석했다 — 04.hta의
  `totalVol` 식과 같은 모양이다. (해석이 다르면 알려 주기 바란다.)

#### B. `ui/condition_dialog.py` 신설 — **카드가 값을 보내 주는 독립 산출기 창**

주종을 뒤집는다. 계산기가 견적을 끌고 가는 게 아니라, **견적 프로그램이 주인이고 계산기는
선택한 카드의 소재·사이즈를 받아 도는 보조 도구**다(사용자 요청). 요청 원문의 "보조 기능으로
넣어서 사용"과도 이 쪽이 맞다.

- **진입점: 현황판 "보조 기능"의 `가공조건 산출기` 버튼.** 카드를 1건 골라 두고 누르면
  그 카드의 Material·Size가 채워진 채 열린다. 아무것도 안 골랐으면 빈 채로 열려 단독
  계산기로 쓸 수 있다.
- **2건 이상 골랐으면 열지 않고 "1건만 선택해 주세요"라고 알린다.** `selected_nos`가
  집합(다중 선택)이라 어느 카드인지 정할 수 없다. 임의로 첫 번째를 쓰면 조용히 엉뚱한
  소재로 계산하게 된다.
- **창은 하나만 뜬다.** 띄워 둔 채로 현황판에서 다른 카드를 고르고 다시 누르면 그 카드
  값으로 갈아 끼운다(`app.open_popups`와 같은 방식으로 중복 생성을 막는다). 카드를
  번갈아 계산해 보는 흐름이 이 구조에서 자연스럽다.
- 카드 입력 팝업 안 Size 섹션 옆에도 같은 버튼을 둔다 — 값을 넣던 중에 바로 열 수 있다.

**카드에서 계산기로 넘어가는 값과 그 한계**

| 카드에 있는 것 | 계산기에서 쓰는 곳 |
|---|---|
| Material 문자열 (`KS D 3752, SM45C` 등) | 재질 9종 중 하나로 매칭 → kc·권장값 자동 적용 |
| Size 문자열 (`100T * 100W * 100L`, `Ø100 * 100L`) | 블록이면 소재 T/W/L, 로드면 D/L. Lathe면 D→D_max, L→L |

- Material 매칭은 v0.1.0의 `settings.resolve_density()`와 **같은 키워드 방식**을 쓴다.
  카드의 Material은 `KS D 3867, SCM440`처럼 규격명이 섞인 긴 문자열이라 이름 전체를
  열쇠로 못 쓰는 것이 이미 확인된 사실이다. 못 찾으면 비워 두고 사용자가 고르게 한다 —
  추측해서 채우지 않는다.
- **Size는 문자열로만 저장돼 있다.** `item`에 형상·치수 필드가 따로 없고 `size` 한 칸뿐이다.
  `ui/popup.py` 191~208행에 이미 그 문자열을 되짚는 역파서가 있으므로, 그것을
  **`core/model.parse_size()`로 빼내 팝업과 계산기가 같은 것을 쓴다.** 두 군데가 서로
  다르게 읽는 일이 생기지 않게 하려는 것이다.
- **넘길 수 없는 값이 있다.** 계산기가 필요로 하는 **목표 치수**(Lathe의 D_min,
  Mill의 목표 t/w/l)와 **공구 조건**(공구경·날수·Vc·이송·절입량·허용동력)은 카드에 아예
  없다. 즉 전달은 "소재 쪽 절반을 미리 채워 주는 것"이고, 나머지는 계산기에서 입력한다.
  이건 방향을 어느 쪽으로 잡든 똑같다.

**창 구성**

- 맨 위에 **가공 계열: Mill / Lathe** 선택. 형상(블록/로드)과는 별개인 새 축이다.
  카드에서 열었으면 형상은 이미 정해져 오고, 계열만 고르면 된다.
  - **Mill** → 블록이면 소재 T/W/L + 목표 t/w/l, 로드면 소재 D/L + 목표 d/L.
    공구경·날수·Vc·fz·ap·ae·허용동력을 함께 받는다.
  - **Lathe** → 04.hta와 같은 입력(D_max, D_min, SFM, V, G50 RPM, F, Ap, L, 허용동력).
- 결과 배치는 04.hta를 따른다: 회전수/이송/부하율/MRR/패스/절삭력/제거체적 + 아래 띠에
  **총 예측 가공 시간**. Lathe는 인서트 재종 추천도 그대로 보여 준다.
- 입력칸은 **v0.1.0 요청 2의 규칙(숫자·소수점만)** 을 따른다. 04.hta의 `ev()`/`fixF()`는
  `eval()`로 `100*2` 같은 식을 받지만 이식하지 않는다 — 같은 프로그램 안에서 칸마다 입력
  규칙이 다르면 헷갈리고, `eval`을 남길 이유도 없다.

#### C. 기존 화면과 겉돌지 않게 — UI 규칙

**결과를 카드로 되돌리지 않기로 했다(사용자 결정).** 산출기는 카드 데이터를 **읽기만
한다** — 적용 버튼도, 카드 저장도, 현황판 갱신도 없다. 화면에 보여 주고 끝난다. 덕분에
"팝업이 열려 있는지 / 저장 전 상태인지 / 이미 값이 있는지"를 따질 일이 통째로 사라져
구조가 단순해지고, 계산기가 견적 데이터를 망칠 경로 자체가 없어진다.

남은 문제는 **화면이 겉도는 것**이다. 04.hta는 어두운 배경(`#111827`)에 형광 노랑·시안으로
짠 화면인데, 지금 앱은 v0.0.8에서 밝은 그라데이션 테마로 바꿨고 색·글꼴이 전부
`theme.json`에서 나온다. 04.hta 화면을 그대로 옮기면 프로그램 안에서 **그 창만 검게 뜬다.**

그래서 **레이아웃(무엇을 어디에 보여 줄지)만 가져오고, 색·글꼴·위젯은 전부 지금 앱 것을 쓴다.**

| 항목 | 규칙 |
|---|---|
| 위젯 | tk 직접 그리기 대신 ttk. 입력 `ttk.Entry`, 선택 `ttk.Combobox(state="readonly")`, 묶음 `ttk.LabelFrame` — 카드 팝업과 같은 구성 |
| 색 | `theme.color(...)`에서만 가져온다. 코드에 색값(`#111827` 같은 것)을 적지 않는다 |
| 글꼴 | `theme.normal / bold / small / value_num`. 결과 숫자는 자릿수가 흔들리지 않게 `value_num`(고정폭) |
| 스타일 이름 | 이미 있는 것을 재사용한다 — 항목 제목 `Head.TLabel`, 결과값 `Value.TLabel`, 설명 `Muted.TLabel`, 묶음 테두리 `TLabelframe` |
| 부하율 3단계 | 04.hta의 형광 초록/노랑/빨강 대신 **`theme.get_status_colors()`** 가 주는 (배경, 글자, 테두리) 쌍을 쓴다. 현황판 가능여부 배지와 같은 색이라 눈에 익다 |
| 숫자 입력 | `popup._numeric_entry()`를 재사용한다(v0.1.0 요청 2 규칙). 두 파일이 같이 쓰게 되므로 `ui/widgets.py`로 옮긴다 |
| 창 | `Toplevel` + `transient(app.root)`, 화면 배율을 보고 크기 계산 — 설정창(`settings_dialog.py` 64~75행)·카드 팝업(`popup.py` 329~343행)과 같은 방식. **`grab_set()`은 조건부다 — 바로 아래 참고** |
| 배치 | 04.hta의 좌(입력) / 우(결과) 2단은 유지하되, 양쪽 최소폭을 `columnconfigure(minsize=)`로 못박는다. 비율만 주었다가 팝업 금액 칸이 잘렸던 일이 있다 |
| 이모지 | 04.hta의 `⏱️ ⚡ 🔧 📊 ★`는 쓰지 않는다. 지금 앱은 구역 제목의 `■` 정도만 쓴다 |
| 단위 표기 | 값과 단위를 한 칸에 붙여 쓴다(`630 RPM`, `104.7 cm³/min`) — 04.hta와 같고 팝업 금액 표기와도 어긋나지 않는다 |

**총 가공시간은 `7분 35초 (0.13 h)`처럼 시간(h) 환산을 나란히 적는다.** 카드의 공정
시간칸이 h 단위이므로, 되돌리기 버튼이 없어도 눈으로 보고 그대로 옮겨 적을 수 있다.
초 단위만 보여 주면 사용자가 매번 3600으로 나눠야 한다.

**모달 여부는 어디서 열었는지에 따라 갈린다.** 한 가지로 통일할 수 없다.

- **현황판에서 연 경우 — 모달로 만들지 않는다**(`grab_set()` 없음). 창을 띄워 둔 채
  현황판에서 다른 카드를 골라야 하기 때문이다(B절). 모달로 잡으면 그 흐름이 막힌다.
  산출기가 카드를 고치지 않으므로 모달로 묶어 둘 이유도 없다.
- **카드 팝업에서 연 경우 — 산출기가 자기 `grab_set()`을 건다.** 팝업이 이미
  `grab_set()`을 잡고 있어서(`popup.py` 342행) 뒤에 뜬 창이 이벤트를 못 받는다.
  산출기를 닫을 때 팝업이 다시 잡게 돌려준다. 이걸 빠뜨리면 팝업 입력이 먹통이 된다.

#### D. 설정창에 **"가공 조건"** 탭 추가 — 장비·재질 표를 사용자가 고친다

v0.1.0의 `densities`와 똑같은 방식(코드에 기본값, `settings.json`이 덮어씀)으로 넣는다.
특정 장비를 코드에 박으면 두 번째 장비가 들어올 때 못 쓴다.

- 장비: 정격 동력(기본 22.0kW), 최고 회전수(기본 3500), 이름(기본 `PUMA 2600Y`)
- 선삭 재질표 9행: 이름 / kc / Vc / F / Ap / 최고RPM / 추천 인서트 — 04.hta 값이 기본값
- 밀링 재질표 9행: 이름 / kc / Vc / fz / ap / ae — **아래 잠정값을 기본으로 넣되,
  사용자 확인이 필요한 항목이다(뒤의 "확인이 필요한 것" 참고).**

`core/settings.load()`는 키마다 `payload.get(키, 기본값)`으로 읽어 새 키가 없는 옛
`settings.json`에도 기본값이 그대로 들어간다 — **직접 확인했다. 마이그레이션 코드가 따로
필요 없다.**

#### E. 손대지 않는 것

- `*.hta` 원본은 읽기만 한다(인수인계서: 레거시, 수정하지 말 것). 04.hta의 수식을
  파이썬으로 옮겨 적는 것이지 파일을 고치는 게 아니다.
- 엑셀 출력(`excel_io.py`)과 견적 양식은 건드리지 않는다.
- **카드 데이터(`item`)도 건드리지 않는다.** 산출기는 Material·Size를 읽기만 하고
  어떤 경우에도 카드 값을 고치지 않는다. 세션 저장 경로에도 영향이 없다.
- 버전 올리기(`estimate_app/__init__.py`, `installer/Setup.iss` 두 곳)와 빌드는
  **승인 후** 구현이 끝나고 나서 한다.

### 확인이 필요한 것 (승인과 함께 알려 주기 바란다)

1. **밀링 재질별 권장 절삭조건.** 01.hta에 계산식이 없어서(위 (2) 참고) 밀링 쪽 권장값의
   출처가 없다. 04.hta의 kc 9종은 **선삭 기준**이라 그대로 쓸 수 없다. 실제 쓰는 값이
   있으면 그대로 넣겠다. 없으면 아래 **잠정 기본값**으로 넣고 설정창에서 고쳐 쓰게 한다
   (초경 엔드밀 Ø10 4날 기준으로 잡은 값이며, **실측이 아니라 잠정치임을 화면에도 적는다**).

   | 재질 | kc | Vc(m/min) | fz(mm/날) | ap(mm) | ae(mm) |
   |---|---|---|---|---|---|
   | 알루미늄 | 700 | 300 | 0.10 | 5.0 | 5.0 |
   | 청동 | 780 | 180 | 0.08 | 3.0 | 5.0 |
   | 주철 FC/FCD | 1350 | 150 | 0.10 | 3.0 | 5.0 |
   | 탄소강 S45C | 1800 | 120 | 0.08 | 2.0 | 5.0 |
   | Ti6Al4V | 1950 | 50 | 0.05 | 1.0 | 3.0 |
   | 합금강 SCM | 2200 | 100 | 0.07 | 2.0 | 4.0 |
   | STS304 | 2400 | 80 | 0.06 | 1.5 | 4.0 |
   | 열처리강 HRC40~ | 2750 | 45 | 0.04 | 1.0 | 2.0 |
   | 인코넬 | 3100 | 25 | 0.03 | 0.8 | 2.0 |

2. **밀링 장비.** 선삭은 PUMA 2600Y(22kW/3500RPM)로 04.hta에 적혀 있는데 밀링 장비 정보는
   어디에도 없다. 알려 주면 기본값으로 넣고, 없으면 동력 15kW / 12000RPM으로 두고
   설정창에서 고치게 한다.

### 검증 계획 — 이식이 정확한지 숫자로 대조한다

이 기능은 엑셀 출력을 건드리지 않으므로 3/7/20/45건 엑셀 회귀는 대상이 아니다.
대신 **04.hta의 식을 그대로 옮겨 기대값을 미리 계산해 뒀다.** 구현 뒤에는 파이썬
`calc_lathe()`가 아래 값을 그대로 내는지만 보면 된다 — HTA를 띄워 눈으로 읽는 방식은
쓰지 않는다(백그라운드 세션에서 mshta/IE 렌더링은 v0.0.9·v0.1.0 때도 못 했고, 그렇게
잡으면 또 "미실행 항목"이 될 뿐이다).

공통 입력: Dmax 100 / Dmin 50 / L 100 (별도 표기한 경우 제외)

**[1] 기본값** — S45C(kc 1800), V 198, G50 2500, F 0.23, Ap 2.3, 22kW

| 항목 | 기대값 |
|---|---|
| 주축 RPM (min~max) | 630 ~ 1261 |
| 분당 이송 Vf | 145 mm/min |
| MRR | 104.7 cm³/min |
| 주절삭력 Fc | 97 kgf |
| 제거 체적 | 589.0 cm³ |
| 패스 수 | 11회 |
| Pc / 설비부하 / 스핀들부하 | 3.14 kW / 14.3% / 14.3% |
| 소비전력 | 0.397 kWh |
| SFM | 650 ft/min |
| **총 가공시간** | **7분 35초 (0.1265 h)** |

**[2] 난삭재 + G50 상한** — 인코넬 선택 시 권장값 자동 적용(V 35 / F 0.12 / Ap 1.2 /
G50 500), 22kW

| 항목 | 기대값 |
|---|---|
| 주축 RPM | 111 ~ 223 |
| Vf / MRR / Fc | 13 mm/min / 5.0 cm³/min / 46 kgf |
| 패스 수 | 21회 |
| Pc / 설비부하 | 0.26 kW / 1.2% |
| **총 가공시간** | **2시간 37분 5초 (2.6180 h)** |

**[3] L = 1.5 — 단면 특수공정 분기** (그 외 [1]과 동일)

패스 **1회**, 제거체적 8.8 cm³, **총 가공시간 10초 (0.0029 h)**.
Z이동이 없으므로 `(반경여유/Vf)×60`으로 계산된다. 이 분기를 놓치면 시간이 11배로 나온다.

**[4] Dmax 50 < Dmin 100 — 오류 분기** (그 외 [1]과 동일)

두 입력칸에 오류 표시. 제거체적 0 cm³, 패스 0회, **총 가공시간 0초**.
RPM은 `1261 ~ 630`으로 뒤집혀 나오는데 **04.hta의 동작 그대로다** — 고치지 않는다.

**[5] 동력 스케일 상·하한** — S45C 권장값(V 220 / F 0.28 / Ap 2.8) 기준

| 입력 동력 | ps (원값 → 제한 후) | 적용된 F / Ap | 패스 | 총 가공시간 |
|---|---|---|---|---|
| 40 kW | 1.818 → **1.500** | 0.343 / 4.2 | 6회 | 2분 30초 (0.0416 h) |
| 5 kW | 0.227 → 0.227 (하한 0.2 미달 아님) | 0.133 / 0.64 | 40회 | 42분 57초 (0.7158 h) |

**[6] 밀링(대조할 원본이 없으므로 식으로 직접)** — S45C(kc 1800), 엔드밀 Ø10 4날,
Vc 120, fz 0.08, ap 2.0, ae 5.0, 15kW / 12000RPM

| 항목 | 기대값 |
|---|---|
| n | 3819.7 RPM |
| Vf | 1222.3 mm/min |
| Q | 12.22 cm³/min |
| Pc / 설비부하 | 0.367 kW / 2.4% |
| 블록 100×100×100 → 90×90×100 | 제거 190.0 cm³ → **15.54분 (0.2591 h)** |
| 로드 Ø100×100 → Ø90 | 제거 149.2 cm³ → **12.21분 (0.2035 h)** |

로드 쪽 제거체적 식이 04.hta의 `totalVol`과 같은 모양인 것을 여기서 확인할 수 있다
(요청 2 "로드는 mill 계열 참조"의 근거).

**UI 확인**: 산출기 창을 실제로 띄워 캡처하고(인수인계서 7절 방식), 카드 팝업·설정창
캡처와 나란히 놓아 배경·글자·입력칸 색이 같은지 본다. 코드에 색값을 직접 적은 곳이
없는지 `grep '#[0-9a-fA-F]\{6\}' estimate_app/ui/condition_dialog.py`로 함께 확인한다
(비면 통과).

그 외 매번 하는 것: `python -m compileall -q estimate_app main.py`.

### 반영 내용

**신설**

- `estimate_app/core/machining.py` — `calc_lathe()`(04.hta `doCalc()` 이식, 상수·분기 하나도
  안 바꿈), `apply_lathe_recommendation()`(04.hta `applyRecommendation()` 이식, 동력
  스케일 0.2~1.5 제한·ap 선형/f 제곱근 비례), `calc_mill()`(신규, 표준 밀링 식
  n/Vf/Q/Pc), `seconds_to_text()`/`seconds_to_hours()`(공통 시간 표시).
- `estimate_app/ui/condition_dialog.py` — 산출기 창. 진입점 둘: 현황판 "보조 기능"의
  "가공조건 산출기" 버튼(비모달, 선택 0/1/2+건 분기), 카드 팝업 Size 섹션의 버튼(팝업
  grab을 넘겨받았다가 닫을 때 돌려줌). 카드 데이터는 읽기만 하고 어떤 경우에도 고치지
  않는다(사용자 결정).

**수정**

- `core/settings.py` — `DEFAULT_LATHE_MACHINE`/`DEFAULT_MILL_MACHINE`,
  `DEFAULT_LATHE_MATERIALS`/`DEFAULT_MILL_MATERIALS`(9행씩), `_clean_machine()`/
  `_clean_material_rows()`/`resolve_machining_material()` 추가. `load()`/`save()`에
  `lathe_machine`/`mill_machine`/`lathe_materials`/`mill_materials` 4개 키 반영(옛
  `settings.json`에 이 키가 없어도 기본값으로 채워지는 것 확인 — 마이그레이션 코드 불필요).
- `core/model.py` — `parse_size()` 신설. `ui/popup.py`의 Size 문자열 역파서를 그대로
  옮겼다(동작이 바뀌지 않은 것을 직접 확인). 카드 팝업과 산출기가 이제 이 하나를 같이 쓴다.
- `ui/widgets.py` — `numeric_entry()` 신설(`popup.py`의 `_numeric_entry`를 그대로 옮김).
  `popup.py`는 이제 여기서 import(`_numeric_entry` 별칭 유지, 기존 호출부 안 건드림).
- `ui/popup.py` — Size 섹션에 "가공조건 산출기 열기" 버튼. Size 파서를 `parse_size()`
  호출로 교체. 더 이상 안 쓰는 `import re` 정리(`math`는 이전부터 이미 안 쓰이고 있었고
  이번 작업과 무관해 손대지 않았다).
- `ui/dashboard.py` — `self.condition_window` 슬롯, "가공조건 산출기" 버튼(보조 기능
  줄), `open_condition_dialog()` 메서드(선택 2건 이상이면 안내만 하고 열지 않음).
- `ui/settings_dialog.py` — "가공 조건" 탭 신설(Lathe/Mill 서브탭). 각 서브탭은 장비
  스펙 3칸(이름/정격 동력/최고 회전수) + 재질표 트리뷰(불러오기/추가·수정/삭제 폼).
  저장 시 4개 키 모두 반영.
- `estimate_app/__init__.py`, `installer/Setup.iss` — 버전 0.1.0 -> 0.1.1.

**계획을 구현하며 조정한 것**

1. Mill의 로드 형상은 목표 길이를 따로 안 받는다. 계획 B절 표에는 "로드면 소재
   D/L + 목표 d/L"라고 적었지만, 실제 제거 체적 식 `π(D²−d²)·L/4000`은 Lathe의 로드
   계산(04.hta)과 같은 모양이라 L이 하나뿐이다 — 목표 길이가 들어갈 자리가 없었다.
   목표는 d 하나만 받도록 통일했고, 화면에도 "로드는 목표 지름만 받습니다(길이는
   소재와 같다고 봅니다)"라고 적어 뒀다.
2. Mill 재질 선택은 동력 스케일링 없이 재질표 값을 그대로 채운다. Lathe의
   `apply_lathe_recommendation()` 스케일 공식은 04.hta 고유의 것이라 Mill에 대응하는
   원본이 없다 — 억지로 만들지 않고, 필요하면 사용자가 직접 조정하는 쪽을 택했다.
3. 부하율 배지 색은 04.hta의 4단계(여유/적정/주의/과부하)를 현황판과 같은 3단계
   (가능/검토필요/불가, `theme.get_status_colors()`)로 접었다. 04.hta의 "설비 부하율
   판정" 80% 기준을 위험 경계로 그대로 쓰고 중간에 50%를 뒀다 — 04.hta 그대로 4단계를
   쓰면 이 앱의 배지 체계(3단계)와 안 맞는다.
4. Lathe 산출기를 카드 없이(빈 채로) 열면 04.hta의 정적 기본값(V198/F0.23/Ap2.3/G50
   2500)을 그대로 보여 준다 — 04.hta의 `initApp()`이 `doCalc()`만 부르고
   `applyRecommendation()`은 안 부르는 것과 같다. 다만 **카드에서 재질이 자동
   매칭돼 열렸을 때는 그 재질의 추천값을 곧바로 적용한다** — 카드 없이 연 첫 화면과
   카드가 매칭돼 연 화면의 동작을 다르게 잡은 것으로, 계획에는 명시하지 않았던
   판단이다(그렇지 않으면 인코넬 카드를 열어도 G50 2500 같은 안 맞는 값이 뜬다).
5. Mill/Lathe 패널 전환을 처음엔 `tkraise()`로 짰다가, 실제로 창을 띄워 캡처해 보니
   두 패널의 세로 길이가 달라(Mill이 목표 치수 줄까지 있어 더 길다) 짧은 쪽(Lathe)
   아래로 긴 쪽(Mill)의 나머지 행이 그대로 비쳐 보이는 문제를 발견했다.
   `grid_remove()`로 아예 빼는 방식으로 고쳤다(아래 검증 결과의 캡처 비교 참고).
6. 입력 항목이 창 높이보다 길어질 수 있어(특히 Mill: 장비+재질+공구 8칸+형상+소재
   3칸+목표 3칸) 세로 스크롤(Canvas+Scrollbar, `dashboard.py`의 `row_canvas`와 같은
   구조)을 추가했다 — 계획에는 없던 항목이다. 저장 버튼 줄(닫기)은 스크롤 영역
   밖에 먼저 고정해 창이 작아도 항상 보이게 했다(`settings_dialog.py`가 이미 쓰던
   이유와 같다).

### 검증 결과

- **수치 대조**: `plan.md`의 검증표 [1]~[6]을 `core/machining.py`로 직접 재현해
  소수점까지 일치 확인(예: [1] 7분 35초/0.13h, [2] 2시간 37분 5초, [3] L=1.5 단면
  특수공정 10초, [4] Dmax<Dmin 오류 플래그, [5] 동력 스케일 상한 1.5/하한 0.2 클램프,
  [6] Mill 블록 0.26h·로드 0.20h). 전부 PASS.
- **compileall**: `python -m compileall -q estimate_app main.py` 통과.
- **settings 마이그레이션**: 새 키(`lathe_machine` 등)가 없는 옛 payload에서도
  `_clean_material_rows()`가 기본값 9행으로 채우는 것을 직접 확인.
- **실제 Tk 창 스모크 테스트(13개 시나리오, 전부 PASS)**: 선택 0/1/2건 진입 분기,
  Mill의 SM45C→S45C 자동 매칭, Lathe의 AL6061P-T62→알루미늄 자동 매칭, 계열
  Mill/Lathe 전환, 창을 띄운 채 다른 카드로 갈아 끼우기, 팝업↔산출기 grab 인계·복귀
  (닫으면 팝업이 다시 grab을 잡는 것까지), 설정창(가공 조건 탭 포함) 열고 닫기.
- **화면 캡처(PrintWindow, HANDOVER.md 7(3) 방식, pywin32 없이 순수 ctypes로 구현)**:
  산출기(Mill/Lathe), 카드 팝업, 설정창의 가공 조건 탭(Lathe/Mill 서브탭) 총 5장을
  실제로 렌더링해 확인. 배경·글자·입력칸 색이 카드 팝업·설정창과 같은 밝은 테마로
  뜨는 것을 눈으로 확인했다. `grep -E '#[0-9a-fA-F]{6}' estimate_app/ui/condition_dialog.py`
  결과 없음(하드코딩 색값 없음).
  캡처 과정에서 위 "계획을 구현하며 조정한 것" 5, 6번 문제(패널 잔상, 스크롤 없어 목표
  치수·닫기 버튼이 창 밖으로 잘림)를 실제로 발견했고, 고친 뒤 재캡처로 사라진 것을
  확인했다 — 계획서만으로는 못 잡았을 문제라 이 검증 단계가 실제로 의미가 있었다.
- **엑셀 출력 회귀(7/20건)**: `settings.py` 변경이 기존 `rates`/`materials`/
  `densities`/`company`/`client`/`supplier` 흐름을 깨지 않는 것과, `export_items()`가
  예외 없이 도는 것을 확인했다(산출기는 카드를 안 건드리므로 이 이상의 회귀 대상은
  없다).
- **빌드(사용자 요청으로 추가 진행)**: `python -m PyInstaller --noconfirm Machine_Estimate.spec` 성공. `dist/Estimate/Estimate.exe`를 직접 실행해 창 제목이
  `Estimate(견적) v0.1.1`로 뜨는 것을 확인했다(그 사이 사용자가 띄워 둔 v0.1.0
  인스턴스는 건드리지 않고 새로 띄운 것만 종료했다 -- v0.1.0 때와 같은 방식).
  `ISCC.exe`로 경고 없이 컴파일. `MachineEstimate_Setup_v0.1.1.exe` 9,624,874바이트.
  워크트리 삭제 시 사라지지 않도록 사용자 배포 폴더
  (`...\orca\workspaces\Estimate\Estimate\installer\Output\`)에도 복사해 뒀다.

### 미실행 항목

- 마우스로 직접 버튼을 눌러 보는 상호작용(클릭·드래그·스크롤 휠)은 이전 버전들과
  같은 이유(백그라운드 세션)로 실측하지 못했다. 코드 경로를 직접 호출하고
  PrintWindow로 렌더링 결과만 확인했다.
- 밀링 재질별 권장 조건표·밀링 장비 스펙은 사용자 확인 없이 계획서의 잠정값으로
  진행했다(계획 단계에서 이미 밝힌 항목). 화면·설정창 모두 "잠정치"라고 표기했다.
- 설정창의 가공 조건 탭 트리뷰는 열이 많아(Lathe 8열) 창 너비에 따라 오른쪽 한두
  열이 잘릴 수 있다(실제 캡처에서 확인, Ae(mm)·최고RPM 등). 트리뷰 자체에 가로
  스크롤바를 추가하진 않았다 — ttk.Treeview 기본 동작(마우스 드래그로 열 경계 조절)에
  맡겼다. 급하지 않은 개선 여지로 남겨 둔다.
- v0.0.9·v0.1.0과 마찬가지로 이번 v0.1.1도 `Estimate` 브랜치에 아직 병합하지 않았다.

## 2026-08-09 v0.1.2 — 카드 삭제(선택 삭제 / 우클릭 삭제) + 되돌리기 (구현 완료)

### 요청 내용

> 선택 삭제 또는 우클릭 삭제 기능 첨부

"또는"은 둘 중 하나만 만들라는 뜻으로도 읽히지만, 두 경로가 같은 동작을 부르는 것이라
따로 만들 일이 거의 없다(진입점만 둘, 실제 삭제 코드는 하나다). **둘 다 넣는 것으로
잡았다.** 하나만 원하시면 그 진입점만 빼면 된다.

### 확인한 현재 구조

실제로 코드를 열어 확인한 것만 적는다.

**(1) 삭제 기능은 지금 아예 없다.** `estimate_app` 전체에 `Button-3`(우클릭)·`tk.Menu`·
`tk_popup`을 쓰는 곳이 하나도 없다(grep으로 확인). 카드를 지우는 경로도 없다 — 한 번
만든 카드는 세션에서 사라지지 않는다.

**(2) 카드 목록과 선택.**

| 항목 | 위치 | 내용 |
|---|---|---|
| 카드 목록 | `dashboard.EstimateApp.data` | dict의 리스트. 카드 한 건이 dict 하나 |
| 내부 번호 | `item["no"]` | 선택·팝업·슬롯 매핑의 키. 화면 순번(`display_no`)과 다르다 |
| 선택 | `app.selected_nos` | `no`의 집합(다중 선택) |
| 화면 표시 | `model.filter_items()` | `has_item_data()`가 참인 카드만 목록에 오른다 |
| 다시 그리기 | `app.refresh_table()` | `selected_nos.intersection_update()`로 없어진 번호를 알아서 걷어낸다 |
| 저장 | `app.save_session()` | `session.save()`가 `app.data`를 통째로 다시 쓴다 |

즉 **삭제 자체는 `app.data`에서 빼고 `refresh_table()` + `save_session()`을 부르면
끝난다.** 표 슬롯(`row_slots`)은 줄어든 행 수를 이미 감당한다(`hide_row_slot()`으로
남는 슬롯을 숨기는 경로가 있다).

**(3) 위험한 지점 — 카드 입력창이 열려 있는 채로 그 카드를 지우면 조용히 어긋난다.**

`ui/popup.py`의 `save_and_close()`는 팝업을 **열 때 붙잡아 둔 `item` dict를 직접
고친다**(502~515행). 그 dict가 이미 `app.data`에서 빠졌다면:

- 저장을 눌러도 오류 없이 넘어가고, `refresh_table()`을 불러도 목록에 안 나타난다.
- 사용자가 보기엔 "저장했는데 사라졌다"가 된다. **오류 메시지도 안 뜬다.**

지금 구조에서 실제로 생길 수 있는 유일한 데이터 손실 경로다. 아래 계획 C에서 막는다.

**(4) 가공조건 산출기(v0.1.1)는 영향이 없다.** 카드의 Material·Size를 **열 때 한 번
읽어 자기 입력칸에 복사**할 뿐, `item`을 참조로 들고 있지 않다. 카드를 지워도 산출기
창은 이미 복사해 둔 숫자를 그대로 계산할 뿐이고, 카드에 되쓰는 길이 없으므로(v0.1.1의
사용자 결정) 어긋날 데가 없다.

**(5) `anchor_no`(Shift 범위 선택의 기준 행)가 지워진 카드를 가리킬 수 있다.**
`handle_row_click()`이 `self.anchor_no in self.visible_nos`를 먼저 확인하므로 예외는
안 난다(범위 선택이 그냥 일반 선택으로 떨어진다). 그래도 지운 뒤에는 비워 두는 게 맞다.

**(6) 번호 재사용.** `model.get_next_no()`는 `max(no) + 1`이다. 맨 끝 번호 카드를
지우면 다음 새 카드가 그 번호를 다시 받는다. `open_popups`가 `no`를 열쇠로 쓰므로
번호가 겹치면 문제가 될 수 있는데, **(3)에서 팝업이 열린 카드의 삭제를 막으면 그
경우가 사라진다** — 팝업은 닫힐 때 `open_popups`에서 스스로 빠지기 때문이다.

### 수정 계획

#### A. `dashboard.py`에 삭제 로직 하나 — 진입점 둘이 같은 함수를 부른다

```
delete_items(nos, parent=None)      실제 삭제. 아래 두 진입점이 공통으로 쓴다
  1) 팝업이 열려 있는 카드가 있으면 -> 안내만 하고 아무것도 지우지 않는다 (C 참고)
  2) 확인 대화상자 (건수 + 품번 목록)
  3) app.data에서 제거, selected_nos/anchor_no 정리
  4) save_session() -> refresh_table(True)
```

- **부분 삭제를 하지 않는다.** 대상 중 하나라도 팝업이 열려 있으면 나머지도 안 지운다.
  일부만 지우고 "3건 중 2건만 지웠습니다"라고 알리는 쪽은, 사용자가 무엇이 남았는지
  다시 확인해야 해서 오히려 번거롭다.
- `refresh_table(True)`로 부른다(`reset_limit=True`) — 목록의 내용 자체가 바뀐 경우라
  기존 규약(업로드·검색과 같은 취급)에 맞다.

#### B. 진입점 ① — 현황판 "보조 기능" 줄의 `선택 삭제` 버튼

`전체 선택 / 선택 해제 / 선택 다운로드 / 가공조건 산출기` 옆에 붙인다.

- 선택이 0건이면 "삭제할 항목을 먼저 선택하세요" 안내만 한다.
- **`선택 다운로드` 바로 옆에 두지 않는다.** 둘 다 "선택"으로 시작하는 데다 되돌릴 수
  없는 동작이라, 오클릭하면 그대로 사라진다. 맨 오른쪽 끝에 두고 앞쪽 버튼들과 간격을
  더 벌린다(`padx`를 키운다).

#### C. 진입점 ② — 행 우클릭 메뉴

`table.py`의 `_bind_row_behavior()`에 `<Button-3>`을 더한다. 지금 클릭 규칙(윈도우
탐색기 방식)과 어긋나지 않게 맞춘다.

- **우클릭한 행이 선택 밖이면 그 행만 선택하고 메뉴를 연다.** 선택 안이면 선택을
  그대로 두고 메뉴를 연다(선택한 여러 건에 한 번에 걸린다). 탐색기와 같은 규칙이다.
- 메뉴 항목: **`입력창 열기`** / 구분선 / **`삭제`**.
  (`입력창 열기`는 더블클릭과 같은 동작이다. 메뉴에 삭제 하나만 두면 오클릭이 바로
  삭제로 이어지므로, 무해한 항목을 위에 둬 커서가 삭제 위에서 시작하지 않게 한다.)
- 메뉴는 슬롯마다 만들지 않고 **앱에 하나만 만들어 재사용한다.** 행 슬롯은 재사용되는
  구조라(v0.0.8) 슬롯마다 `tk.Menu`를 달면 메뉴가 계속 쌓인다. 콜백은 다른 클릭
  핸들러와 같은 규칙으로 **`slot["no"]`를 호출 시점에 읽는다**(v0.0.9에서 체크박스가
  이 규칙을 어겨 해제가 안 됐던 전례가 있다 — 인수인계서 함정 7번).
- `tk_popup()` 뒤에 **`grab_release()`를 반드시 부른다.** 안 부르면 메뉴가 grab을 쥔
  채로 남아 다음 클릭이 먹통이 된다(Tk의 알려진 동작).

#### D. 팝업이 열린 카드 보호 — 위 (3)의 손실 경로를 막는다

`delete_items()` 맨 앞에서 `app.open_popups`를 확인해, 대상 중 입력창이 열려 있는
카드가 있으면 **지우지 않고** 어느 카드인지 알려 준다:

> "다음 항목은 입력창이 열려 있어 지울 수 없습니다: NO.3 (P-1234)
>  입력창을 닫은 뒤 다시 시도해 주세요."

팝업을 대신 닫아 주는 방법도 있지만 그러면 사용자가 입력하던 내용이 말없이 날아간다.
막고 알리는 쪽이 안전하다.

#### E. 확인 대화상자

```
선택한 3건을 삭제할까요?

  NO.1  P-1234 / 브래킷
  NO.4  P-5678 / 하우징
  NO.7  P-9012 / 커버

삭제한 항목은 '삭제 취소'로 되돌릴 수 있습니다.
```

- 5건을 넘으면 앞 5건만 적고 `...외 N건`으로 줄인다(대화상자가 화면을 넘지 않게).
- 기본 버튼은 **아니오**(`messagebox.askyesno`는 기본이 "예"라 `default="no"`를 준다).

#### F. 되돌리기(Undo) — 사용자 요청으로 포함(승인 시 추가 지시)

`save_session()`이 삭제 직후 세션 파일을 덮어쓰므로 삭제만으로는 복구 수단이 없다.
**메모리에 방금 지운 카드를 들고 있다가 되살리는 단일 단계 Undo**를 넣는다(여러 번
되돌리는 스택은 안 만든다 — 이 앱에 되돌릴 만한 동작이 삭제 하나뿐이라 스택은 과하다).

- `app.last_deleted` — `[{"item": 카드dict, "index": 지우기 전 app.data에서의 위치}, ...]`.
  **세션 파일에는 저장하지 않는다** — 메모리에만 둔다. 프로그램을 껐다 켜면 되돌릴 수
  없다(다른 모든 실행 취소 기능이 흔히 그렇듯, 세션을 넘어서까지 보장하지 않는다).
- 되살릴 때는 `index` 오름차순으로 하나씩 `list.insert(index, item)` 한다. 원래 위치
  그대로 되돌아간다(먼저 지운 것부터 꽂아야 뒤 인덱스가 어긋나지 않는다).
- **번호 재사용 충돌을 막는다.** `model.get_next_no()`는 `max(no)+1`이라, 지운 카드가
  맨 끝 번호였다면 그 사이에 `새 항목 추가`/`저장 후 다음 항목`/`기계 시트 업로드`로
  새 카드가 만들어지면 같은 번호를 다시 받을 수 있다(위 확인한 구조 (6)). 되돌리기
  버튼을 누르는 시점에 **`last_deleted`의 번호가 지금 `app.data`에 이미 있는지 검사**해,
  하나라도 겹치면 "그 사이 같은 번호의 카드가 새로 만들어져 되돌릴 수 없습니다"라고
  알리고 버퍼를 비운다. 코드 경로마다 미리 막지 않고(추가/업로드 지점을 일일이 손대지
  않고) **되돌리는 순간에만 검사**하는 쪽이 나중에 새 코드가 추가돼도 안전하다.
- **새로 삭제하면 이전 되돌리기 버퍼는 조용히 사라진다.** 되돌리지 않은 채로 다시
  삭제하면 그 전 것은 못 되돌린다 — 단일 단계이므로 당연한 동작이라 별도 확인창은
  안 띄운다.
- 되돌리면 복원한 카드들을 **다시 선택 상태로 만든다**(`set_selection`) — 방금 무엇이
  돌아왔는지 눈에 보이게.
- **입력창(팝업)이 열려 있는 카드는 되돌리기 대상에서 막을 필요가 없다.** 삭제 쪽만
  막으면 된다(D절) — 복원은 데이터를 다시 넣는 동작이라 팝업과 충돌하지 않는다.

**진입점 둘**

1. 보조 기능 줄에 `삭제 취소` 버튼. `last_deleted`가 비어 있으면 비활성화
   (`state=tk.DISABLED`) — 되돌릴 게 없을 때 눌러서 헷갈릴 일이 없게.
2. **Ctrl+Z** — `app.root`에만 건다(전역 `bind_all`이 아니다). 카드 팝업이나 산출기
   같은 다른 Toplevel에 포커스가 있을 때는 반응하지 않는다 — 팝업 안에서 텍스트를
   고치다 Ctrl+Z를 누르면 "삭제 취소"가 튀어나오는 건 사용자가 기대하는 동작이 아니다.

삭제 확인 대화상자(E절)에도 "삭제한 항목은 '삭제 취소'로 되돌릴 수 있습니다."를
한 줄 더한다 — 되돌리기가 있다는 것 자체를 확인창에서 알려 준다.

#### G. 손대지 않는 것

- 엑셀 출력·견적 양식·가공조건 산출기: 무관하다.
- `session.py` / `settings.py`: 저장 형식이 그대로다(카드가 빠진 목록을 기존 방식으로
  다시 쓸 뿐이라 새 키가 없다).
- 내용이 빈 카드(`has_item_data()`가 거짓)는 애초에 목록에 안 보여 이 기능의 대상이
  아니다. 세션 저장 때 이미 걸러지므로 다음 실행 때 저절로 사라진다 — 따로 손대지 않는다.

### 검증 계획

GUI라 자동 테스트가 없으므로 v0.1.1에서 쓴 방식(실제 Tk 창을 띄워 코드 경로를 직접
호출 + PrintWindow 캡처)을 그대로 쓴다.

1. **선택 삭제** — 3건 중 2건 선택 후 삭제 → `app.data` 길이, 남은 카드의 `no`,
   화면 순번(1,2,...)이 다시 매겨지는지, `selected_nos`가 비는지.
2. **우클릭(선택 밖)** — 선택이 없는 행에서 우클릭 → 그 행만 선택되고 메뉴가 뜨는지.
3. **우클릭(선택 안)** — 2건 선택 후 그중 한 행에서 우클릭 → 선택이 2건 그대로인지
   (1건으로 줄지 않는지).
4. **팝업 보호** — 카드 팝업을 연 채 그 카드를 삭제 시도 → 안내가 뜨고 `app.data`가
   그대로인지. **팝업을 닫은 뒤에는 삭제되는지**까지 확인한다.
5. **확인 대화상자에서 아니오** → 아무것도 안 지워지는지.
6. **세션 반영** — 삭제 후 `session.load()`로 다시 읽어 지운 카드가 없는지.
7. **전부 삭제** — 마지막 한 건까지 지웠을 때 빈 목록 안내(`_show_empty`)가 뜨는지
   (`refresh_table`의 `if not all_items` 경로).
8. **번호 재사용** — 맨 끝 번호를 지운 뒤 `새 항목 추가` → 예외 없이 열리는지.
9. **되돌리기** — 삭제 직후 `삭제 취소`(버튼/Ctrl+Z 둘 다) → `app.data`·`no`·
   `selected_nos`(복원한 항목이 다시 선택되는지)가 삭제 전과 같은지, 되돌린 뒤
   `삭제 취소` 버튼이 다시 비활성화되는지.
10. **되돌리기 번호 충돌** — 맨 끝 번호를 지운 뒤 `새 항목 추가`로 같은 번호를 다시
    받게 만들고 `삭제 취소` → 되돌리지 못한다는 안내가 뜨고 예외가 안 나는지.
11. **Ctrl+Z가 팝업에서는 안 먹는지** — 카드 팝업에 포커스를 준 채 Ctrl+Z →
    `undo_delete()`가 안 불리는지(포커스가 `app.root`에 없으므로).
12. 매번 하는 것: `python -m compileall -q estimate_app main.py`, 우클릭 메뉴 캡처.

### 반영 내용

**수정**

- `estimate_app/ui/dashboard.py` — `delete_selected_items()` / `delete_items(nos)` /
  `undo_delete()` / `_update_undo_button()` / `open_row_context_menu(no, event)` 신설.
  보조 기능 줄에 `선택 삭제`(오클릭 방지를 위해 앞쪽 버튼과 간격을 더 벌림)와
  `삭제 취소`(비활성으로 시작) 버튼 추가. `app.root`에 `<Control-z>` 바인딩(전역
  `bind_all`이 아니다 — 카드 팝업 등 다른 Toplevel에 포커스가 있을 때는 안 먹는다).
  `__init__`에 `last_deleted`/`undo_button`/`row_context_menu`/`context_menu_no` 슬롯.
- `estimate_app/ui/table.py` — `_bind_row_behavior()`에 `<Button-3>` 추가. 체크박스처럼
  `is_control` 위젯도 우클릭만은 받도록 구조를 나눴다(왼쪽 클릭 계열만 건너뛰고
  `<Button-3>`은 항상 건다).
- `estimate_app/__init__.py`, `installer/Setup.iss` — 버전 0.1.1 -> 0.1.2.

**계획대로 구현한 것 중 확인해 둘 만한 지점**

- 우클릭 메뉴는 슬롯마다 만들지 않고 `app.row_context_menu`에 한 번만 만들어 재사용한다
  (표 슬롯이 재사용되는 구조라 슬롯마다 달면 메뉴가 계속 쌓인다). 메뉴 항목은
  `app.context_menu_no`/`app.selected_nos`를 **호출 시점**에 읽는다 — v0.0.9 체크박스
  버그(위젯 생성 시점 상태를 클로저에 담아 생긴 문제)와 같은 함정을 다시 밟지 않으려는
  것이다.
- 우클릭한 행이 선택 밖이면 `handle_row_click(no, "plain")`으로 그 행만 선택한 뒤 메뉴를
  연다. 이미 선택 안에 있으면 선택을 그대로 둔다. 이 정리를 메뉴를 열기 **전에** 끝내
  두므로, 메뉴의 `삭제` 명령은 별도 분기 없이 그때그때의 `app.selected_nos`를 그대로
  쓴다 — "우클릭한 행 하나만" vs "선택된 여러 건"을 가르는 로직이 메뉴 쪽엔 안 남는다.
- `tk_popup()` 뒤에 `grab_release()`를 `finally`로 반드시 부른다(계획서에 적어 둔 대로 --
  안 부르면 메뉴가 grab을 쥔 채 남아 다음 클릭이 먹통이 된다).
- `delete_items()`는 `index_by_id`(파이썬 객체 `id()` 기반)로 삭제 전 위치를 기록한다.
  `list.index(item)`(dict 값 비교) 대신 쓴 것은, 내용이 우연히 같은 두 카드가 있을 때
  엉뚱한 위치를 가리킬 수 있어서다.

### 검증 결과

- **compileall**: `python -m compileall -q estimate_app main.py` 통과.
- **실제 Tk 창 스모크 테스트(13개 시나리오, 전부 PASS)** — 검증 계획 1~11번을 그대로
  실행했다. `tk.Menu.tk_popup`/`grab_release`를 no-op으로 바꿔치기해서 확인했다
  (`tk_popup()`은 OS 네이티브 모달 루프라 `after` 콜백으로도 자동 안 닫히는 것을 먼저
  직접 겪었다 — Tcl 이벤트 큐를 안 타는 진짜 OS 루프였다).
  - 선택 삭제(3건 중 2건) → 남은 카드·번호·선택 해제·`삭제 취소` 버튼 활성화 확인
  - 세션에 삭제가 그대로 반영됨(`session.load()`로 재확인)
  - 되돌리기(버튼) → 복원 + 복원한 카드 재선택 + 버튼 다시 비활성화, 세션에도 반영
  - 삭제 확인 대화상자에서 "아니오" → 안 지워짐
  - 팝업이 열린 카드 삭제 시도 → `showwarning` 뜨고 안 지워짐, **팝업을 닫은 뒤엔
    지워짐**까지 확인
  - 우클릭(선택 밖 행) → 그 행만 선택, 우클릭(선택 안 행, 2건 선택 상태) → 선택
    유지(1건으로 안 줄어듦)
  - 캐시된 메뉴의 `삭제` 항목을 직접 `invoke()`해 선택 2건이 한 번에 지워지는 것,
    `입력창 열기` 항목이 실제로 팝업을 여는 것 확인
  - 전부 삭제 → 빈 목록 안내(`_show_empty`) 뜸
  - **번호 재사용 + 되돌리기 충돌 방어** — 맨 끝 번호 카드 삭제 → `새 항목 추가`로
    같은 번호를 실제로 재사용하는 것까지 확인 → `삭제 취소` → `showerror`로 안내하고
    `app.data`가 그대로인 것, `last_deleted`가 비워지는 것 확인
  - **Ctrl+Z 범위** — 카드 팝업에 실제로 포커스를 준 채 팝업 위젯에 대고
    `<Control-z>`를 발생시켜도 `undo_delete()`가 안 불리는 것(대조군: `root`에 준
    같은 이벤트는 정상 작동)을 확인
- **메뉴 구성 프로그램적 확인**: `app.row_context_menu` 생성 후 항목 수(3: 입력창
  열기/구분선/삭제), 라벨, `bg`/`fg`/`activebackground`가 `theme.color("card_alt"/
  "text"/"accent")`와 정확히 같은 값인 것을 확인(하드코딩 색값 없음).
- 이번 작업은 카드의 존재 여부만 바꾸고 엑셀 출력·가공조건 산출기 구조는 건드리지
  않아 별도 회귀 대상이 없다.

### 미실행 항목

- **우클릭 메뉴가 실제로 화면에 뜬 모습은 캡처하지 못했다.** `tk_popup()`이 Tcl
  이벤트 큐를 거치지 않는 OS 네이티브 모달 루프라(위 검증 결과에서 직접 확인),
  PrintWindow 캡처를 하려면 별도 스레드에서 SendInput으로 강제로 닫는 식의 추가
  작업이 필요하다. 대신 메뉴 구성(라벨·색)을 프로그램적으로 확인하는 것으로
  대체했다 — 다른 화면들(v0.1.1의 산출기 등)과 달리 새 레이아웃이 아니라 표준
  tk.Menu 텍스트 항목 3개뿐이라 위험도가 낮다고 판단했다.
- 마우스로 직접 우클릭·버튼 클릭하는 실제 상호작용은 이전 버전들과 같은 이유
  (백그라운드 세션)로 실측하지 못했다.
- **Ctrl+Z 바인딩이 실제 마우스 클릭 후에도 정상 작동하는지는 확인했지만, 카드
  팝업을 닫은 뒤 포커스가 대시보드로 자동 복귀하는지는 확인하지 못했다.** 이
  세션에는 실제 창 관리자가 없어 `Toplevel.destroy()` 후에도 `root.focus_get()`이
  `None`으로 남는 것을 직접 확인했다(`root.focus_force()`로 강제한 뒤에야
  이벤트가 들어왔다). 실제 사용자는 아무 데나 한 번 클릭하면 포커스가 돌아오므로
  실사용에 지장은 없을 것으로 보이지만, 이 부분은 실제 환경에서 확인이 필요하다.

## 2026-08-09 v0.1.3 — 삭제 취소 버튼 정리 · 다중 파일 업로드 · 품번 중복 행 표시 (구현 완료)

### 요청 내용

사용자 원문: "선택삭제 옆에 삭제는 불필요 기계 시트 업로드에서 다중으로 파일 업로드 가능하게
설정 및 품번의 중복 번호는 행의 색변화로 표현 바로 빌드 까지 작성"

1. "선택 삭제" 옆의 삭제(취소) 버튼 제거
2. 기계 시트 업로드에서 여러 파일을 한 번에 고를 수 있게
3. 품번이 중복되는 항목은 행의 색으로 표현
4. 바로 빌드까지

`.agents/AGENTS.md`의 "계획 먼저, 승인 후 구현" 절차는 이번에 밟지 않았다 — 요청에
"바로 빌드 까지"가 명시돼 있어 승인 대기 없이 진행했다. 이 기록이 사후 보고를 겸한다.

### 확인한 현재 구조

1. 현황판 보조 기능 줄(`dashboard.build_dashboard`)의 버튼 순서는
   `전체 선택 / 선택 해제 / 선택 다운로드 / 가공조건 산출기 / 선택 삭제 / 삭제 취소`였다.
   "선택 삭제 옆"에 실제로 붙어 있던 것은 v0.1.2에서 넣은 **삭제 취소** 버튼 하나뿐이다
   (우클릭 메뉴에도 "삭제"가 있지만 그것은 버튼 옆이 아니다).
2. `upload_excel_file()`은 `filedialog.askopenfilename`(단수)로 파일을 하나만 받았고,
   읽자마자 곧바로 `self.data`에 붙였다.
3. 행 배경색은 `dashboard.row_normal_bg(slot)` 한 곳에서 결정한다(선택 > 줄무늬).
   hover/leave 콜백도 같은 함수를 부른다.
4. `core/config.py`의 `_merged()`는 **기본값에 있는 키만** 남긴다. 새 색을 theme.json에만
   적으면 무시되고 `KeyError`가 난다 — DEFAULT_COLORS에도 같이 넣어야 한다.
5. 카드 입력창의 저장(`popup.save_and_close`)은 `app.refresh_table(True)`를 부른다.
   품번을 고쳐 저장하면 중복 판정이 다시 계산된다.

### 반영 내용

**A. 삭제 취소 버튼 제거 (요청 1)**

- `dashboard.build_dashboard`에서 "삭제 취소" 버튼과 `self.undo_button`,
  `_update_undo_button()`을 없앴다.
- **되돌리기 기능 자체는 남겼다.** `undo_delete()`와 `Ctrl+Z` 바인딩은 그대로다 —
  v0.1.2에서 사용자가 직접 요청한 기능이라, 버튼(화면 요소)만 정리하고 능력은 보존했다.
- 버튼이 사라진 만큼 되돌리는 방법을 두 곳에 적었다.
  - 헤더 안내줄: "...방금 삭제한 항목은 Ctrl+Z로 되돌립니다."
  - 삭제 확인창: "방금 삭제한 항목은 현황판에서 Ctrl+Z로 되돌릴 수 있습니다."

**B. 다중 파일 업로드 (요청 2)**

- `askopenfilename` → `askopenfilenames`. Tk 구현에 따라 튜플이 아니라 Tcl 리스트
  문자열이 오는 경우가 있어 `self.root.tk.splitlist()`로 한 번 거른다.
- **전부 읽은 뒤에 한꺼번에 붙인다.** 파일마다 읽고 바로 붙이면 세 번째 파일에서 오류가
  났을 때 앞의 두 개만 들어간 채 멈춘다(업로드는 되돌리기 대상이 아니라 사용자가 직접
  지워야 한다). 한 파일이 실패해도 나머지는 살린다.
- 확인창에 파일별 건수를 적고, 읽지 못한 파일은 "[불러오지 못한 파일]" 아래에 이유와 함께
  따로 적는다. 전부 실패하면 그 목록만 경고로 띄우고 아무것도 붙이지 않는다.
- 고른 순서대로 번호를 이어 붙인다. 화면에서는 나중에 들어온 카드가 위로 오므로
  마지막에 고른 파일이 목록 맨 위에 놓인다.
- 빈 목록에 파일 하나를 무사히 읽어 온 경우는 예전처럼 확인창 없이 바로 넣는다.
- 업로드 완료창은 중복 품번이 생겼으면 그 사실을 함께 알린다(요청 3과 이어지는 안내).

**C. 품번 중복 행 색 표시 (요청 3)**

- `core/model.py`에 `normalize_part_no()` / `find_duplicate_part_nos()` 신설.
  비교 표기는 공백 제거 + 대문자 통일이다(`" a-100 "`과 `"A-100"`을 같은 것으로 본다).
  **빈 품번은 세지 않는다** — 아직 안 적은 카드끼리 물들면 새 카드를 만들 때마다 화면이
  붉어진다.
- 판정 대상은 검색·더보기로 가려진 것까지 포함한 `data` 전체다. 짝이 화면 밖에 있어도
  중복은 중복이다.
- 판정은 `refresh_table` / `update_summary_text`에서 한 번만 해서
  `app.duplicate_part_nos`에 담아 두고, 행마다 그 집합만 본다(행마다 다시 세면 카드 수의
  제곱만큼 일이 늘어난다).
- 색 우선순위는 **선택 > 중복 > 줄무늬**. 지우거나 내려받을 대상을 고르는 중에는 "지금
  고른 행이 어디인지"가 더 급한 정보이고, 중복 여부는 선택을 풀면 바로 다시 보인다.
- 새 색 3개를 `config.DEFAULT_COLORS`와 `assets/theme.json` **양쪽에** 넣었다
  (`row_dup_bg` `#fdeee4` / `row_dup_alt_bg` `#fbe6d8` / `row_dup_fg` `#a4400f`).
  danger_bg(`#fdeaec`)를 그대로 쓰지 않은 이유는 "불가" 배지와 같은 색이라 배지가 행
  배경에 묻히기 때문이다. 줄무늬를 잃지 않도록 짝/홀 두 톤을 뒀다.
- 배경 하나에만 기대지 않도록 품번 글자색도 함께 바꾼다(색 구분이 어려운 사용자 대비).
- `slot["duplicate"]`는 `create_row_slot`의 딕셔너리 리터럴에서 False로 먼저 만든다 —
  마우스가 지나가면 `<Leave>` 콜백이 `row_normal_bg(slot)`을 부르는데, 그게 첫
  `update_row_slot`보다 먼저 일어날 수 있다.
- 요약줄에 "품번 중복 N건"을 더했다. 짝이 화면 밖에 있으면 색만으로는 눈에 안 띄기 때문이다.

**D. 버전**

`estimate_app/__init__.py`와 `installer/Setup.iss` 양쪽 모두 0.1.2 → **0.1.3**.

### 검증 결과

1. **문법** — `python -m compileall -q estimate_app main.py` 통과.
2. **중복 판정 로직(헤드리스)** — `find_duplicate_part_nos`에
   `["A-100", " a-100 ", "B-200", "", None, "A 100", "a100"]`을 넣어
   `{"A-100", "A100"}`이 나오고 빈 품번은 걸리지 않는 것을 확인.
3. **다중 업로드 엔드투엔드** — `excel_io.export_items`로 실제 xlsx 2개를 만들고
   (`sheet_a` 3건 / `sheet_b` 2건, `A-1002`가 서로 겹침), 여기에 엑셀이 아닌
   깨진 파일 1개를 더해 셋을 한 번에 업로드시켰다(파일 대화상자·메시지 상자만 가짜로 대체,
   읽기·붙이기는 실제 코드 경로). 결과:
   - 확인창에 파일별 건수와 `broken.xlsx: File is not a zip file`이 함께 표시됨
   - 실패한 1개를 뺀 5건이 번호 1~5로 연속해서 들어감(부분 반영 없음)
   - `duplicate_part_nos == {"A-1002"}`, 요약줄 "품번 중복 2건"
4. **화면(실제 창 캡처)** — HANDOVER 7-(3) 방식(`paths.get_user_dir` 임시 대체 +
   `PrintWindow`)으로 카드 8건(중복 품번 5건, 그중 1건은 선택 상태)을 띄워 캡처했다.
   - 중복 행이 주황 계열로 확실히 구분되고, 줄무늬(짝/홀 두 톤)도 유지됨
   - 선택된 중복 행은 선택색(`#f0eefe`)을 지키고 품번 글자색만 주황으로 남음
   - 보조 기능 줄이 `전체 선택 / 선택 해제 / 선택 다운로드 / 가공조건 산출기 / 선택 삭제`
     로 끝나고 "삭제 취소" 버튼이 사라진 것을 확인
   - 창 제목 `Estimate(견적) v0.1.3` 확인
5. **삭제 / 되돌리기 경로(헤드리스)** — 이번에 `_update_undo_button()`과 그 호출부를
   지웠으므로 실제로 실행시켜 확인했다(문법 검사로는 남은 호출을 못 잡는다).
   중복 쌍(`A-1001` 2건) 중 한쪽을 `선택 삭제` → 남은 쪽의 중복 표시가 풀리고
   (`duplicate_part_nos == set()`, 행 배경 `#f9fbff`), `Ctrl+Z` 되돌리기 →
   카드·중복 표시·선택이 모두 복원(`#fdeee4`, 선택된 행은 `#f0eefe` 유지).
   입력창이 열린 카드는 삭제가 막히고, 닫은 뒤에는 지워지는 것도 함께 확인.
   `root.bind("<Control-z>")`가 살아 있는 것도 확인했다.
6. **빌드** — `python -m PyInstaller --noconfirm Machine_Estimate.spec` +
   `ISCC.exe installer\Setup.iss`.
   `installer\Output\MachineEstimate_Setup_v0.1.3.exe` 9,633,408바이트 생성.
   워크트리는 지워질 수 있으므로 `C:\Users\SumH\Codex\Estimate\installer\Output\`
   으로 복사해 뒀다.
7. **패키징된 exe 스모크 테스트** — `dist\Estimate\Estimate.exe`를 직접 띄워
   창 제목이 `Estimate(견적) v0.1.3`으로 뜨는 것을 확인하고 종료시켰다
   (PyInstaller 번들에서 모듈이 빠져 시작조차 못 하는 경우를 거르기 위해).

### 미실행 항목

- **실사용 확인은 못 했다.** 실제 기계 시트 여러 개를 한 번에 올리는 것은 사용자 파일로만
  가능하다. 검증에 쓴 xlsx는 프로그램이 스스로 만든 출력물이다.
- **설치 실행은 못 했다.** 설치 파일 생성까지만 확인했고 관리자 권한 설치는 안 해 봤다.
- **"선택 삭제 옆의 삭제"의 해석.** 버튼 옆에 있던 것은 "삭제 취소"뿐이라 그것을 없앴다.
  사용자가 뜻한 것이 **우클릭 메뉴의 "삭제"**였다면 `dashboard.open_row_context_menu()`의
  `menu.add_command(label="삭제", ...)`와 그 위 `add_separator()` 두 줄을 지우면 된다
  (그 경우 메뉴에는 "입력창 열기"만 남는다).
- 중복 판정은 품번 문자열만 본다. 기종·소재가 달라도 품번이 같으면 중복으로 표시한다.
  실무에서 그런 경우가 있다면 판정 기준을 사용자와 다시 정해야 한다.

## 2026-08-10 v0.1.4 — 데이터 저장 위치를 사용자가 고르게 한다(네트워크 공유)

### 요청 내용

> v0.1.4 버전업. 주요 요청 1가지 — **db 즉 데이터 저장 경로를 내가 선택해서 저장 및 로드**
> 할 수 있게. **네트워크 환경에서 사용**하려고 함.

지금은 사용자 데이터가 전부 `%LOCALAPPDATA%\MachineEstimate`에 고정돼 있어 PC를 옮기면
단가·소재·회사 정보를 다시 맞춰야 하고, 여러 사람이 같은 기준으로 견적을 낼 수 없다.

### 확인한 현재 구조 (실제로 읽고 확인한 것만)

| 파일 | 쓰는 곳 | 성격 |
|---|---|---|
| `settings.json` | `core/settings.py` (`paths.get_user_file`) | 단가·Material 목록·비중·가공조건·회사/견적서 양식·열 폭 |
| `session_state.json` | `core/session.py` | **자동 저장**. 종료 시 `dashboard.save_session()`, 시작 시 `restore_session()` |
| `update_state.txt` | `core/updater.py` | 이 PC가 이미 처리한 설치 파일 기록 |
| `theme.json` / `rates.json` 사본 | `paths.get_asset_path()` | 사용자 폴더에 같은 이름이 있으면 번들보다 우선 |

네 가지 모두 `paths.get_user_dir()` 한 곳을 본다. 그래서 **`get_user_dir()` 자체를 공유
폴더로 돌리면 안 된다** — 성격이 서로 다르기 때문이다.

- `settings.json` → **공유해야 한다.** 이번 요청의 본래 목적(같은 단가·기준으로 견적).
- `session_state.json` → **공유하면 안 된다.** 자동 저장이라 두 사람이 동시에 켜 두면
  나중에 끈 사람이 앞사람 작업을 말없이 덮어쓴다(last-write-wins). 경고도 못 띄운다.
- `update_state.txt` → **공유하면 안 된다.** 공유하면 A PC가 업데이트한 순간 B PC는
  "이미 처리함"으로 판단해 영영 업데이트 안내를 못 받는다.
- `theme.json`/`rates.json` 덮어쓰기 사본 → 로컬 유지(화면 실험용이라 PC별 값이다).

추가로 확인한 위험 두 가지 (지금은 로컬 디스크라 드러나지 않지만 네트워크에서는 실제 사고가 된다):

1. `settings.load()`가 `OSError`를 통째로 삼키고 **기본값을 돌려준다**. 공유 폴더가 잠깐
   끊긴 채로 프로그램을 켜면 사용자는 단가가 기본값으로 보이고, 뭔가 고쳐 저장하는 순간
   **공유 settings.json이 기본값으로 덮어써진다 — 전원에게.**
2. `settings.save()` / `session.save()`가 `open(path, "w")`로 바로 연다. 쓰는 중에 연결이
   끊기면 파일이 잘린 채로 남는다.

### 수정 계획

#### A. 공통 기반 — 데이터 폴더 (필수, 어느 안을 고르든 들어간다)

1. **`core/datastore.py` 신설** — 데이터 폴더의 주인.
   - 위치 지정 파일은 **로컬 고정**: `%LOCALAPPDATA%\MachineEstimate\data_location.json`
     (`{"path": "\\\\서버\\공유\\Estimate", "enabled": true}`).
     공유 위치를 가리키는 쪽지를 공유 위치에 둘 수는 없으므로 이건 반드시 로컬이다.
   - `get_data_dir()` — 지정돼 있고 실제로 쓸 수 있으면 그 폴더, 아니면 `get_user_dir()`.
   - `probe(path)` — 폴더 존재/읽기/쓰기(임시 파일 생성·삭제)를 실제로 시험해 결과를 돌려준다.
     "될 것 같다"가 아니라 실제로 써 보고 판단한다.
   - `paths.get_user_dir()`은 **그대로 둔다.** 세션·업데이트 기록·테마 사본은 지금 자리에 남는다.

2. **원자적 쓰기** — `datastore.write_json_atomic()`: 같은 폴더에 임시 파일로 쓰고
   `os.replace()`로 바꿔치기. `settings.save()`와 `session.save()` 양쪽에 적용한다.
   (같은 폴더에 써야 SMB에서도 rename이 원자적이다.)

3. **읽기 실패를 구분한다** — `settings.load()`가
   "파일 없음(정상, 기본값 사용)"과 "경로에 못 닿음(비정상)"을 나눈다.
   못 닿았으면 `load_error`를 실어 돌려주고, 프로그램은
   - 시작할 때 경고를 띄우고,
   - **그 세션에서는 설정 저장을 막는다**(공유 파일을 기본값으로 덮어쓰는 사고 차단).
   - 사용자가 "로컬로 되돌리기"를 고르면 그때부터 로컬 설정으로 정상 동작한다.

4. **설정창에 "데이터 위치" 탭 추가**(기존 Notebook 방식 그대로).
   - 현재 위치 표시 / 상태(정상·읽기전용·연결 안 됨)
   - `찾아보기…`(폴더 선택) → `probe` 통과해야 적용
   - `로컬로 되돌리기`
   - 폴더를 새로 고를 때 이관 처리:
     - 대상 폴더에 `settings.json`이 **없으면** → 지금 쓰던 설정을 복사해 간다.
     - **있으면** → 그쪽 값을 채택한다. 어느 쪽이 이겼는지 대화상자로 분명히 알린다.
       (말없이 기본값으로 시작하면 "내 단가가 초기화됐다"로 보인다.)
   - 적용 후에는 `app.apply_settings()`로 화면에 즉시 반영.

5. `Machine_Estimate.spec`은 **건드리지 않는다.** HANDOVER 함정 #10은 실제 통신
   (TLS·해시)을 붙일 때의 이야기다. UNC 경로/네트워크 드라이브는 `os`·`json`·`shutil`만
   쓰는 평범한 파일 입출력이라 `_ssl`/`_hashlib` 제외를 되돌릴 이유가 없다
   (되돌리면 설치 파일만 커진다). 구현도 이 세 모듈 범위를 넘지 않는다.

6. 버전은 `estimate_app/__init__.py`와 `installer/Setup.iss` **양쪽** 0.1.4 (함정 #9).

#### B. 카드 목록(작업 내용)을 어떻게 공유할지 — **사용자 확인 필요**

여기가 이번 요청에서 유일하게 갈리는 지점이다. "저장 및 로드"를 어느 범위로 뜻했는지에 따라
만들 것이 달라진다.

**안 1 (권장) — 견적 보관함: 이름 붙여 저장 / 목록에서 불러오기**

- 공유 폴더 안에 `estimates\` 를 두고, 카드 목록을 `사업명_날짜.json` 같은 이름으로
  **사용자가 직접 저장**한다. 보조 기능 줄에 `견적 저장` / `견적 불러오기` 두 버튼.
- 불러오면 현재 카드 목록을 대체한다(대체 전 확인 대화상자).
- 저장할 때 같은 이름이 이미 있고 **내가 불러온 뒤 다른 사람이 고쳤으면** 경고하고
  덮어쓸지 묻는다(파일 수정 시각 비교). 덮어쓰기도 원자적으로.
- 자동 저장 세션은 지금처럼 **로컬**에 그대로 둔다(각 PC의 "작업 중" 상태).
- 장점: 네트워크에서 안전하다. 누가 무엇을 언제 저장했는지 파일로 남는다.
  단점: 자동이 아니라 사용자가 저장을 눌러야 한다.

**안 2 — 세션 자동 저장까지 공유 폴더로 옮긴다**

- 요청 문구에 가장 가깝지만, **두 대 이상에서 동시에 쓰면 작업이 사라진다.**
  자동 저장이라 경고를 띄울 자리도 없다. 한 번에 한 사람만 쓴다는 전제가 확실할 때만 쓸 만하다.
- 그래도 원한다면 최소 안전장치를 붙인다: 프로그램을 켤 때 잠금 파일(`session.lock`,
  PC 이름·시각)을 만들고 이미 있으면 "다른 PC(○○)가 사용 중 — 읽기 전용으로 엽니다" 안내.

**안 3 — 기준 데이터만 공유(설정만), 카드 목록은 각자**

- A 항목만 하고 끝낸다. 가장 단순하고 사고 여지가 없다.
  "여러 사람이 같은 단가·소재 기준으로 견적을 낸다"가 목적이라면 이것으로 충분하다.

권장: **A + 안 1**. "저장 및 로드"라는 표현을 그대로 살리면서, 네트워크에서 남의 작업을
말없이 덮어쓰는 경로를 만들지 않는다.

### 검증 계획 (HANDOVER 7절 기준)

1. `python -m compileall -q estimate_app main.py`
2. **데이터 폴더 4가지 상태**를 헤드리스로 확인한다 — 지정 안 함(로컬) / 정상 폴더 /
   비어 있는 폴더(설정 이관 확인) / **닿지 않는 경로**(존재하지 않는 UNC 호스트).
   마지막 경우에 ① 프로그램이 뜨고 ② 경고가 뜨고 ③ **기본값을 되쓰지 않는지**를 확인한다.
3. 엑셀 출력 회귀 — 3 / 7 / 20 / 45건(설정 경로가 바뀌어도 출력이 그대로인지).
4. 화면 캡처 — 설정창 "데이터 위치" 탭, (안 1이면) 보조 기능 줄의 새 버튼.
5. 빌드 + 설치 파일 생성, `dist\Estimate\Estimate.exe` 스모크 테스트,
   설치 파일을 `C:\Users\SumH\orca\workspaces\Estimate\Estimate\installer\Output\`로 복사.

### 승인 대기

`.agents/AGENTS.md` 원칙에 따라 **구현·버전 변경·빌드는 승인 후에 시작한다.**
확인이 필요한 것은 위 **B의 안 1 / 안 2 / 안 3** 중 어느 것인지 하나다.

### 승인 / 결정

사용자가 **안 1(견적 보관함)** 을 골랐다. 즉 `A(데이터 폴더 선택) + 안 1(이름 붙여 저장/불러오기)`
로 구현했고, 세션 자동 저장은 계획대로 PC별 로컬에 남겼다.

### 반영 내용

#### A. `core/datastore.py` 신설 — 데이터 폴더의 주인

- 위치 쪽지는 **로컬 고정**: `%LOCALAPPDATA%\MachineEstimate\data_location.json`.
  공유 위치를 가리키는 쪽지를 공유 위치에 둘 수는 없어서다.
- `get_state()` — `{dir, shared, ok, reason}`. 실행하는 동안 한 번만 판정하고 캐시한다.
- `probe(path)` — 폴더 존재 + **임시 파일을 실제로 만들어 지워 보고** 판정한다.
  별도 스레드로 돌리고 `PROBE_TIMEOUT`(4초) 안에 답이 없으면 '못 닿음'으로 본다.
  끊긴 UNC 경로에서 SMB가 수십 초를 잡고 있는 동안 프로그램이 안 뜬 것처럼 보이는 것을 막는다.
- **공유 폴더가 죽어 있어도 `dir`은 공유 경로를 그대로 가리킨다.** 말없이 로컬로 되돌리면
  사용자는 공유 값을 쓰는 줄 알고 자기 PC에만 값을 쌓게 된다 — 조용히 갈라지는 것보다
  "지금 못 닿는다"고 알리고 멈추는 편이 낫다.
- `write_json_atomic()` — 같은 폴더에 임시 파일로 다 쓴 뒤 `os.replace()`.
- `read_json()` — **'파일 없음'과 '경로에 못 닿음'을 구분해서** 돌려준다.

#### B. `core/settings.py` — 공유 폴더 대응

- 저장 위치가 `datastore.get_data_file("settings.json")`으로 바뀌었다.
- `load()`가 못 읽었으면 `_load_error`를 세우고, **그 뒤로 `save()`가 거부한다.**
  못 읽은 상태에서 손에 든 값은 사용자의 설정이 아니라 코드 안의 기본값이고,
  그걸 공유 폴더에 쓰면 모두의 단가가 한 번에 날아간다. (`force=True`는 폴더 이관 때만)
- `relocate(new_path, enabled)` 신설 — 폴더를 옮기며 설정을 어떻게 할지 정한다.
  - 대상에 `settings.json`이 있으면 `adopted`(그쪽 값 채택)
  - 비어 있으면 `copied`(지금 값 복사)
  - 비어 있는데 직전 읽기가 실패했으면 `fresh`(기본값을 진짜 설정인 양 굳히지 않는다)
  - 대상 파일이 깨졌거나 못 닿으면 **원래 위치로 되돌리고** 실패로 알린다
  - 어느 쪽이 이겼는지 반드시 화면에 알린다(말없이 기본값이면 "내 단가가 초기화됐다"로 보인다)

#### C. `core/library.py` 신설 — 견적 보관함

- `{데이터 폴더}\estimates\{이름}.json`. 파일 하나가 견적 한 건.
- 저장할 때 이름·건수·저장 시각·**저장한 사람**(`USERNAME@COMPUTERNAME`)·버전을 함께 남긴다.
  `getpass`/`socket` 대신 환경변수를 쓴다 — 이름 한 줄 얻자고 PyInstaller 번들에
  새 의존을 들이지 않기 위해서다(spec EXCLUDES는 '확인한 것만' 규칙으로 관리된다).
- 파일 이름 정리(`sanitize_title`) — 윈도우 금지 문자 치환, 끝의 점·공백 제거
  (윈도우가 끝의 점·공백을 잘라 버려서, 그대로 두면 저장한 이름과 실제 파일 이름이 어긋나
  다음 덮어쓰기 판정이 빗나간다).
- **잠금은 걸지 않는다.** 네트워크 폴더에서 잠금은 프로그램이 비정상 종료하면 남아 버려,
  남의 작업을 막아 놓고 풀지 못하는 쪽이 더 큰 사고가 된다. 대신 저장 직전에 mtime을
  다시 재서 "불러온 뒤 다른 사람이 고쳤다"면 알리고 물어본다.

#### D. 화면

- **설정창에 "데이터 위치" 탭**(6번째). 현재 폴더·상태 표시, 경로 직접 입력 또는 찾아보기,
  `이 폴더 사용` / `다시 확인` / `이 PC에만 저장(기본값)`.
  이 탭만 아래 [저장] 버튼과 무관하게 **즉시 적용**된다(폴더를 바꾸면 다른 탭이 들고 있는
  값이 통째로 낡기 때문). 적용하면 설정창을 닫는다. 탭 맨 위에 그 사실을 적어 두었다.
- **`ui/library_dialog.py` 신설** — 상단 버튼 `견적 불러오기` / `견적 저장`이 같은 창을 연다
  (어느 쪽으로 열렸는지에 따라 포커스만 다르다). 목록 · 불러오기 · 새로고침 · 삭제 · 이름 저장.
  불러오기는 카드 목록을 통째로 바꾸므로 반드시 먼저 묻는다(Ctrl+Z로 못 되돌린다).
- 요약줄에 `공유 폴더` / `공유 폴더 연결 안 됨` / `보관함 '이름'` 표시를 더했다.
- 시작할 때 설정을 못 읽었으면 경고를 띄운다(업데이트 확인보다 **먼저**). 기본값이 보이는
  이유를 먼저 알려야 사용자가 단가를 다시 입력하는 헛수고를 안 한다.

#### E. 공유 환경에서 드러난 기존 코드의 위험 3건 (검토 중 발견해 함께 고침)

1. **열 폭 저장이 남의 단가를 덮어쓰던 문제** — `table._persist_column_widths()`가
   `dict(app.settings)`(켤 때 읽은 사본)을 통째로 저장하고 있었다. 공유 폴더에서는
   *열 구분선을 끄는 것만으로* 그 사이 다른 PC가 고친 단가가 사라진다.
   **파일을 다시 읽어 열 폭만 얹는** 방식으로 바꿨다. 화면에는 열 폭만 반영한다
   (읽어 온 단가까지 밀어 넣으면 구분선을 끌었을 뿐인데 표의 금액이 통째로 바뀐다).
2. **보관함이 빈 첫 실행에서 안내 문구 줄을 클릭하면 IndexError** — 목록이 비면
   "저장된 견적이 없습니다." 한 줄을 넣는데, 그 줄도 선택되므로 `entries`와 번호가 어긋났다.
3. **입력창이 열린 채로 불러오면 그 입력이 조용히 사라지던 문제** — `delete_items()`가
   이미 막고 있던 것과 같은 함정이다(popup이 붙잡고 있는 item dict가 `app.data`에서 빠지면
   저장을 눌러도 오류 없이 사라진다). `apply_library_entry()`에도 같은 방어를 넣었다.

#### F. 손대지 않은 것

- `session_state.json` / `update_state.txt` / `theme.json`·`rates.json` 사본은 로컬 그대로.
  (세션은 자동 저장이라 공유하면 나중에 끈 쪽이 앞사람 작업을 말없이 덮어쓴다.
  update_state는 공유하면 B PC가 업데이트 안내를 영영 못 받는다.)
  세션 저장에도 원자적 쓰기만 적용했다 — 끄는 순간에 쓰므로 잘리면 목록이 통째로 사라진다.
- `Machine_Estimate.spec`. HANDOVER 함정 #10은 실제 통신(TLS·해시)을 붙일 때의 이야기다.
  UNC 경로는 `os`·`json`·`tempfile`만 쓰는 평범한 파일 입출력이라 `_ssl`/`_hashlib` 제외를
  되돌릴 이유가 없다(되돌리면 설치 파일만 커진다). EXCLUDES에 `socket`은 없지만,
  그마저 안 쓰도록 `_who()`를 환경변수로 구현했다.
- 버전은 `estimate_app/__init__.py`와 `installer/Setup.iss` 양쪽 0.1.4.

### 검증 결과

1. **문법** — `python -m compileall -q estimate_app main.py` 통과.
2. **데이터 위치·보관함 헤드리스 (12묶음 55항목, 실패 0)** — 사용자 실제 프로필을 건드리지
   않도록 `paths.get_user_dir`을 임시 폴더로 돌리고, PC 두 대(`pcA`/`pcB`)와 공유 폴더를
   흉내 내 확인했다.
   - 지정 안 함 → 로컬 폴더, 저장·복원 정상
   - 빈 공유 폴더로 이동 → `copied`, 내 단가·소재 목록이 공유 폴더로 넘어감
   - 다른 PC가 같은 폴더 지정 → `adopted`, 공유 단가·회사명·소재 목록을 그대로 봄
   - 없는 폴더 / 끊긴 UNC(`\\no-such-host-xyz\share`) → 거부, **2.2초 만에** 돌아옴
   - **시작할 때 공유 폴더가 죽어 있는 경우** → `load_error`가 서고, 기본값이 보이고,
     `save()`가 거부되고, **공유 settings.json이 바이트 단위로 그대로**, 복구 후 다시 읽힘
   - 보관함: 저장 → 목록(건수·시각·저장자) → 다른 PC에서 불러오기 → 카드 3건 복원
   - 같은 이름 재저장 시 mtime이 달라짐(덮어쓰기 충돌 판정의 근거)
   - 이름 정리(금지문자·끝의 점/공백·빈 이름), 삭제
   - **세션이 공유 폴더로 새지 않음**(로컬에만 생성)
   - 공유 폴더에 임시 파일(`.tmp_*`, `.estimate_probe_*`) 잔여물 없음
   - 로컬로 되돌리기 → 공유 파일은 남고 로컬 설정이 되살아남
   - **열 폭 저장 회귀**(위 E-1): PC_B가 단가를 고친 뒤 PC_A가 열 폭을 저장해도
     PC_B의 단가 수정이 살아 있고, PC_A 화면의 단가는 바뀌지 않음
3. **엑셀 출력 회귀** — 3 / 7 / 20 / 45건 모두 정상.
   합계행(14/14/27/52), 푸터 3줄(회사명·날짜·직위+작성자), 견적서 항목 수 일치.
4. **화면(실제 창 캡처)** — HANDOVER 7-(3) 방식(`PrintWindow`)으로 확인.
   - 상단 버튼 줄: `기계 시트 업로드 / 견적 불러오기 / 견적 저장 / 새 항목 추가 / 설정`
   - 요약줄 끝에 `| 공유 폴더` 표시, 창 제목 `Estimate(견적) v0.1.4`
   - 설정창 탭 6개, 마지막이 `데이터 위치` — 폴더·상태·경로칸·버튼 3개가 모두 보임
   - 보관함 창: 저장자까지 포함한 목록 2건이 정상 표시
   - **처음 캡처에서 보관함 창의 `불러오기`·`새로고침` 버튼이 폭에 눌려 0px로 사라진 것을
     발견**해 하단을 두 줄(목록 조작 / 이름 저장)로 나눴다. 다시 캡처해 모두 보이는 것 확인.
   - 빈 보관함에서 안내 문구 줄을 클릭 → Tk 콜백 예외 없음(위 E-2 수정 확인)
5. **빌드** — `python -m PyInstaller --noconfirm Machine_Estimate.spec` +
   `ISCC.exe installer\Setup.iss`.
   `installer\Output\MachineEstimate_Setup_v0.1.4.exe` **9,653,015바이트**.
   배포 폴더 `C:\Users\SumH\orca\workspaces\Estimate\Estimate\installer\Output\`로 복사 완료.
6. **패키징된 exe 스모크 테스트** — `dist\Estimate\Estimate.exe`를 띄워 창 제목
   `Estimate(견적) v0.1.4` 확인 후 강제 종료(정상 종료를 태우면 사용자의 실제 세션 파일을
   덮어쓰므로 일부러 `taskkill /F`로 끝냈다). `dashboard`가 `library_dialog`를, 그것이
   `core.library`·`core.datastore`를 **모듈 수준에서** import 하므로, 창이 떴다는 것은
   새 모듈이 번들에 제대로 들어갔다는 뜻이다.

### 미실행 항목

- **진짜 네트워크 공유 폴더에서는 확인하지 못했다.** 검증에 쓴 '공유 폴더'는 전부 로컬
  디렉터리다. 따라서 SMB에서의 `os.replace` 원자성, 공유 폴더에 임시 파일 만들기(권한),
  연결이 끊겼을 때 실제 대기 시간이 `PROBE_TIMEOUT`(4초)로 잘리는지는 실측하지 못했다.
  **사용자가 실제 공유 폴더를 지정해 보는 것이 첫 확인 항목이다.**
- **두 대에서 동시에 같은 견적을 저장해 보지 못했다.** "다른 사람이 고쳤습니다" 경고는
  파일 mtime 비교로만 확인했고, 실제 두 PC 동시 사용은 못 해 봤다.
- **관리자 권한 실제 설치는 하지 않았다.** 설치 파일 생성까지만 확인했다.
- **설정창 [저장]은 여전히 통째 저장이다.** A가 설정창을 열어 둔 사이 B가 단가를 고치면
  A가 저장할 때 B의 수정이 사라진다. 사용자가 직접 누른 저장이라 이번에는 그대로 뒀다
  (열 폭 저장은 UI 제스처라 고쳤다). 고치려면 저장 직전에 파일을 다시 읽어 바뀐 항목만
  비교해 알리는 방식이 필요하다.
- **공유 폴더 상태는 켤 때 한 번만 판정한다.** 실행 중 네트워크가 끊기거나 다시 붙어도
  자동으로 알아채지 못한다. [설정 > 데이터 위치 > 다시 확인]을 누르거나 다시 켜야 한다.

### 추가 수정 (같은 날, 검토 중 발견)

**재시작하면 덮어쓰기 충돌 경고가 조용히 죽던 문제.** `app.library_current`(지금 화면이
보관함의 어느 견적인지 + 불러온 시각의 mtime)를 메모리에만 두고 있었다. 그래서:

> PC A가 아침에 「2026 A사」를 불러오고 프로그램을 끈다 → PC B가 낮에 같은 견적을 고쳐
> 저장한다 → PC A가 저녁에 다시 켜서(세션이 카드를 그대로 되살림) 같은 이름으로 저장한다
> → 기준점이 없어 **"다른 사람이 고쳤습니다"가 안 뜨고** 평범한 "이미 있습니다"만 뜬다.
> 사용자가 무심코 [예]를 누르면 PC B의 낮 작업이 사라진다.

고친 내용:

- `session.save()`에 `library_current`를 함께 담고 `session.load()`가 되돌려준다.
  옛 세션 파일에 키가 없으면 `None`(v0.1.4 이전과 같은 동작으로 내려갈 뿐 오동작하지 않는다).
- 보관함에 저장한 직후에도 `save_session()`을 불러 참조를 바로 남긴다(저장하자마자 끄는 경우).
- 데이터 폴더를 옮기면 참조를 지우고 세션에도 반영한다(옛 폴더의 견적을 가리키지 않게).
- 판정을 `library.is_changed_by_other()`로 빼냈다 — 화면 코드 안에 있으면 헤드리스로
  검증할 수 없기 때문이다.

검증(헤드리스, 실패 0): 세션이 참조를 되살리는지 / 재시작 상황을 만들어 다른 PC가 고친 뒤
경고가 실제로 뜨는지 / 참조가 없거나 다른 파일이거나 옛 세션(키 없음)이면 일반 확인창으로
안전하게 내려가는지. 이후 문법·엑셀(3·7·20·45건)·창 캡처를 다시 돌리고 재빌드했다.
`MachineEstimate_Setup_v0.1.4.exe`를 다시 만들어 배포 폴더에 덮어썼다.

부수적으로 HANDOVER.md 버전 요약 표의 행 사이에 빈 줄이 껴 있어 표가 끊겨 보이던 것도
고쳤다(v0.1.1~v0.1.2 사이에 이미 있던 것).

## 2026-08-10 v0.1.4 기능 보존을 위한 v0.1.5 통합 복구 기록

### 승인 및 구현 범위

- 사용자가 v0.1.4 기능 대부분이 사라졌다고 보고했고, 원인 점검 후 복구 계획을 승인했다.
- v0.1.4 기준선의 데이터 보관함, 삭제/되돌리기, 다중 엑셀 업로드, 가공조건, 설정 저장 보호, 엑셀 양식 입출력 기능을 v0.1.5 소스에 다시 통합했다.
- v0.1.5의 열처리 열, 사용자 지정 화면 열 이름, 열 너비 저장, 설정창 화면 열 탭, 팝업 공통 닫기 동작과 기존 v0.1.4 기능이 함께 동작하도록 충돌을 조정했다.

### 검증 결과

- `python -m compileall -q estimate_app main.py` 통과.
- 버전/열처리 열/삭제·되돌리기/다중 업로드/보관함/가공조건/팝업 진입점 스모크 테스트 통과.
- 설정 스키마 왕복, 가공조건 계산, 사용자 지정 엑셀 헤더 입출력 왕복 테스트 통과.
- Tkinter GUI 무인 시작 검증은 사용자가 중단하여 통과로 판정하지 않았다.
- 통합 후 v0.1.5 설치 파일은 아직 재빌드하지 않았다. 재빌드 후 결과를 별도 검증 기록에 남긴다.

## 2026-08-10 입력창 종료 및 대시보드 열 표시 복구 계획 (승인 대기)

### 사용자 보고 증상

1. 견적 항목 입력창에서 `Esc`를 눌러도 닫히지 않는다.
2. 입력창 우측 상단의 `X`를 눌러도 닫히지 않으며, `저장`해야만 나갈 수 있다.
3. 대시보드 제목 열이 칸별로 분리되어 보이지 않고 데이터 열과 배치가 다르다.
4. 대시보드의 `열처리` 열 내용이 표시되지 않는다.

### 현재 코드에서 확인한 원인 범위

- `estimate_app/ui/popup.py`에는 `Esc`와 `WM_DELETE_WINDOW(X)`가 모두 `close_popup()`에
  연결되어 있다. 따라서 단순 바인딩 누락이 아니라 공통 종료 경로, 미저장 확인창,
  `grab_set()` 모달 상태 또는 콜백 실행 중 오류를 실제 창에서 확인해야 한다.
- 입력창 하단에는 `저장 후 다음 항목 입력`, `저장`만 있고 명시적인 `취소/닫기` 버튼이 없다.
- `estimate_app/ui/table.py`에는 `열처리` 헤더와 `heat_label` 위젯이 모두 만들어져 있지만,
  `update_row_slot()`이 행을 갱신할 때 `heat_label`에 값을 넣는 처리가 빠져 있다.
  현재 열처리 내용이 빈칸으로 보이는 직접 원인이다.
- 헤더와 데이터 행은 같은 열 폭 설정을 사용하지만 서로 독립된 Tk `grid` 컨테이너라,
  셀 내용의 요구 폭과 사용자 저장 폭에 따라 실제 열 경계가 달라질 수 있다.
- 헤더 구분선은 2px 드래그 핸들로만 구현되어 있어 실제 화면에서 사라졌거나 데이터 열 경계와
  다른 위치에 놓였는지, `grid_bbox()`와 화면 캡처로 확인해야 한다.

### 승인 후 수정 계획

#### A. 견적 항목 입력창 종료 복구

1. 기존 카드와 신규 카드 입력창에서 `Esc`·`X`를 재현하고 다음을 확인한다.
   - `close_popup()` 진입 여부와 Tk 콜백 오류
   - 미저장 확인창이 팝업 앞에 정상 표시되는지
   - 닫기 전후 `grab_current()`와 `app.open_popups` 상태
2. 종료 처리를 하나의 안전한 `request_close()` 경로로 정리한다.
   - 변경이 없으면 즉시 닫는다.
   - 변경이 있으면 저장하지 않고 닫을지 확인한다.
   - 확인 취소 시 현재 창과 입력 중 값을 그대로 유지한다.
   - 폐기 승인 시 입력값 검증이나 저장을 요구하지 않고 닫는다.
   - 중복 호출과 이미 파괴 중인 창에서도 오류가 나지 않도록 하고 modal grab을 안전하게 해제한다.
3. `Esc`, 창 `X`, 새 하단 `취소/닫기` 버튼을 모두 동일한 종료 함수에 연결한다.
4. 신규 미저장 빈 카드는 닫을 때 목록·선택·세션에서 제거한다.
5. 기존 카드 수정 중 폐기하면 원래 카드 데이터는 변경하지 않으며, 저장된 카드는 삭제하지 않는다.
6. 기존 `저장`, `저장 후 다음 항목 입력`, 가공조건 산출기와 팝업 중복 열림 방지는 유지한다.

#### B. 대시보드 제목 열과 데이터 열 정렬

1. 헤더와 첫 데이터 행의 각 열 경계를 `grid_bbox()`로 실측해 어느 열부터 어긋나는지 특정한다.
2. 헤더와 모든 행 슬롯이 동일한 픽셀 열 폭을 강제로 공유하도록 열 배치 함수를 보강한다.
3. 저장된 사용자 열 폭이 있더라도 헤더와 행에 똑같이 적용하고, 최소 폭 때문에 한쪽만 늘어나지
   않도록 긴 텍스트는 셀 내부에서 말줄임 처리한다.
4. 제목 열 사이의 세로 구분선을 실제 열 경계에 다시 배치하고 항상 헤더 글자 위에서 보이게 한다.
5. 구분선 드래그로 열 폭을 변경한 후에도 제목과 데이터 행이 함께 움직이고 재시작 후 같은 폭으로
   복원되는 기존 기능을 보존한다.
6. 데이터 행 전체에 불필요한 세로 테두리는 추가하지 않고, 사용자가 요청한 제목 열 구분을
   명확하게 표시한다.

#### C. 열처리 표시 복구

1. `update_row_slot()`에서 `heat_label`에 카드의 `heat_treat`, `hrc_min`, `hrc_max`를 바탕으로
   `core.model.hrc_text()` 결과를 매번 넣는다.
2. 열처리 없음은 `-`, 열처리 있음은 `HRC 58~62`와 같은 기존 데이터 표기 규칙을 사용한다.
3. 슬롯 재사용 시 이전 카드의 열처리 문구가 다음 카드에 남지 않는지 확인한다.
4. 검색·정렬·더보기·설정의 사용자 지정 열 제목과 함께 사용할 때도 열처리 열이 유지되게 한다.

### 승인 후 필수 검증

1. 기존 카드 무수정: `Esc`, `X`, `취소/닫기`로 즉시 종료.
2. 기존 카드 수정: 확인 취소 시 입력 유지, 폐기 시 원래 카드 유지 후 종료.
3. 신규 빈 카드 및 신규 입력 중 폐기: 저장되지 않고 빈 카드도 목록에서 정리.
4. 저장 및 저장 후 다음 항목 입력: 기존 동작 유지.
5. 종료 후 같은 카드 재개방, 다른 팝업 및 메인 화면 조작 가능 여부 확인.
6. 기본 열 폭과 사용자가 저장한 열 폭 양쪽에서 헤더/첫 행/마지막 표시 행의 좌우 경계 일치 확인.
7. 열 구분선 드래그 전후 및 재시작 후 헤더와 행 정렬 확인.
8. 열처리 없음, HRC Min만 있음, Min~Max 모두 있음, 슬롯 재사용 상태의 표시 확인.
9. 문법 검사만으로 완료 처리하지 않고 실제 GUI 화면 캡처와 위젯 좌표로 확인한다.
10. 실제 GUI 검증을 통과한 뒤에만 PyInstaller와 v0.1.5 설치 파일을 다시 빌드한다.

### 보존 및 제외 범위

- 엑셀 양식, 엑셀 출력 계산, 단가, 보관함, 공유 폴더, 검색/정렬, 삭제/되돌리기 기능은 변경하지 않는다.
- 이번 결함 복구만 수행하며 버전 번호는 현재 `v0.1.5`를 유지한다.
- 사용자가 수정 중인 `견적_산정\양식\견적용.xlsx`와 미추적 사용자 파일은 건드리지 않는다.
- 사용자 승인 전에는 소스, 버전, EXE 및 설치 파일을 변경하지 않는다.

### 승인 대기

- 위 통합 계획에 대한 사용자 승인 후 구현을 시작한다.

## 2026-08-10 v0.1.6 입력창 종료 및 대시보드 열 표시 복구 완료

### 승인 및 반영 범위

- 사용자가 통합 계획을 승인하고 최종 배포 버전을 `v0.1.6`으로 지정했다.
- `estimate_app/ui/popup.py`, `estimate_app/ui/table.py`,
  `estimate_app/__init__.py`, `installer/Setup.iss`만 기능·버전 변경 대상으로 반영했다.
- 사용자가 수정 중이던 `견적_산정\양식\견적용.xlsx` 및 미추적 사용자 파일은 수정하지 않았다.

### 반영 내용

1. **입력창 Esc/X 종료 복구**
   - 원인: `close_popup()`이 신규 빈 카드 정리에 필요한 `has_item_data()`를 호출하면서도
     해당 함수를 import하지 않아 `Esc`·`X` 종료 시 `NameError`가 발생했다.
   - `has_item_data` import를 복구하고 `Esc`, 창 `X`, 새 `취소/닫기` 버튼을 하나의
     안전한 종료 함수에 연결했다.
   - 변경 없음은 즉시 닫고, 변경이 있으면 폐기 확인을 표시한다. 폐기 시 기존 카드는
     저장 전 값 그대로 두고, 신규 미저장 빈 카드만 목록·선택·세션에서 정리한다.
   - popup이 잡은 modal grab만 해제하고 자식 창이 잡은 grab은 건드리지 않게 했다.
2. **대시보드 제목 열 정렬·구분 복구**
   - 헤더와 데이터 행의 라벨 요청 폭을 한 글자로 제한해 긴 제목/데이터가 각각의 독립된
     `grid` 열 폭을 밀지 못하게 했다.
   - 품번·날짜 묶음 프레임의 grid 전파를 막아 행 쪽 열 폭만 넓어지는 것을 방지했다.
   - 헤더 열 구분선은 재배치 뒤 최상단으로 올려 제목 사이에 항상 표시되게 했다.
3. **열처리 표시 복구**
   - 행 갱신 때 누락됐던 `heat_label` 갱신을 추가했다.
   - 열처리 없음은 `-`, Min/Max가 있으면 `HRC58~62`, 한쪽만 있으면 `HRC58` 형식으로 표시한다.
4. **버전·빌드**
   - 앱/설치 파일 버전을 모두 `0.1.6`으로 변경했다.

### 검증 결과

- `python -m compileall -q estimate_app main.py` 통과.
- 실제 Tk 위젯 검증: 헤더와 데이터 행의 11개 열 폭이 모두 동일함을 `grid_bbox()`로 확인.
- 열처리 값 `HRC58~62` 및 열처리 없음 `-` 표시 확인.
- 실제 Tk 팝업 검증:
  - 신규 빈 카드 `Esc` 종료 시 카드 정리 확인.
  - 기존 카드 창 `X` 종료 시 저장 없이 카드 보존 확인.
  - 변경 중 카드 `Esc`에서 폐기 승인 시 원래 카드 값 보존 확인.
  - `취소/닫기` 버튼도 같은 종료 경로 사용 확인.
- `python -m PyInstaller --noconfirm --clean Machine_Estimate.spec` 성공.
- Inno Setup 6.7.3으로 설치 파일 생성 성공.
- 최종 패키징 EXE `dist\Estimate\Estimate.exe`를 5초간 실행해 정상 시작 확인 후,
  테스트로 시작한 프로세스만 종료했다.
- 양식 포함 확인: 원본과 번들 `견적용.xlsx`가 모두 22,460 bytes이며 SHA-256 해시가 일치한다.

### 빌드 절차 관련 기록

- `build.bat setup`은 한글 주석 인코딩 문제로 PyInstaller 단계가 정상 실행되지 않고 기존
  `dist` 파일로 설치 파일을 만들 수 있음을 확인했다.
- 이번 v0.1.6 최종 산출물은 이를 우회해 PyInstaller를 직접 실행한 뒤 Inno Setup을 직접
  실행하여 만들었다. 따라서 현재 수정된 코드와 사용자가 저장한 최신 양식이 포함돼 있다.

### 최종 산출물

- `installer\Output\MachineEstimate_Setup_v0.1.6.exe`
- 생성 크기: 9,673,086 bytes
## 2026-08-17 v0.1.6 신규품목 전용 등록창 및 수식 보존 출력 계획

### 요청 내용

- 상단 왼쪽에 `신규품목` 버튼을 만들고 신규 등록분만 별도 창에서 관리한다.
- 신규는 최근 수정이 아니라 최초 등록을 뜻하며, 신규 창에서는 먼저 등록한 품목부터 표시한다.
- 신규 입력 완료 후 전체를 본래 현황판으로 이관하고, 미이관 품목은 재실행 후에도 복원한다.
- 신규 창에서도 신규 목록 전체를 출력하며, 모든 출력은 원본 양식의 수식을 유지한다.

### 수정 계획

- 메인 데이터와 별도의 신규품목 컬렉션 및 세션 필드를 추가하고, 기존 세션과 하위 호환한다.
- `신규품목` 전용 목록창에 추가·수정·삭제·전체 출력·전체 이관 기능을 구성한다.
- 신규 여부와 수정 대기 상태를 분리하여 기존 품목 수정이 NEW로 표시되지 않게 한다.
- 엑셀 출력에서 기계/견적서 시트의 원본 수식을 행 수에 맞춰 복제하고 Excel 자동 재계산을 설정한다.
- 3·7·20·45건 수식 출력, 신규 등록 순서, 세션 복원, 일괄 이관 및 GUI를 검증한 뒤 v0.1.6 실행/설치 파일을 만든다.

### 반영 내용

- 상단 첫 버튼을 `신규품목`으로 교체하고 신규 등록분 전용 목록창을 추가했다.
- 신규 목록은 최초 등록 시각 오름차순으로 표시하며 추가·수정·삭제·전체 출력·전체 이관을 지원한다.
- 메인 `data`와 신규 `new_items`를 분리하고 로컬 세션에 자동 저장·복원하도록 확장했다.
- NEW 상태를 수정 대기 상태와 분리해 기존 품목 수정·엑셀 업로드는 신규로 표시되지 않게 했다.
- 기계 시트와 견적서 시트의 순번·금액·합계·참조·날짜 수식을 원본 양식 기준으로 복제하고 자동 재계산을 설정했다.
- 앱/설치 버전을 v0.1.6으로 갱신했다.

### 검증 결과

- `python -m compileall -q estimate_app main.py`, `git diff --check` 통과.
- 신규품목 세션 왕복, 최초 등록 순 정렬, 본래 현황판 전체 이관을 검증했다.
- 3·7·20·45건 출력에서 기계/견적서 수식, 합계 범위, 날짜 참조, 자동 재계산 설정을 확인했다.
- 원본 첫 데이터 행 수식과 v0.1.6 출력 첫 행 수식이 문자열 단위로 일치함을 확인했다.
- 실제 Tkinter 창에서 상단 첫 `신규품목` 버튼, 기존 `새 항목 추가` 제거, 신규 목록 정렬을 확인했다.
- `dist\Estimate\Estimate.exe` 패키징 및 5초 실행 시작 확인을 통과했다.
- `installer\Output\MachineEstimate_Setup_v0.1.6.exe` 생성에 성공했다(10,501,834 bytes).

### 미실행 항목

- 관리자 권한이 필요한 실제 설치 및 기존 버전 위 업그레이드 설치는 실행하지 않았다.

## 2026-08-30 v0.1.7 신규품목 이관 순서 보존

### 요청

- 견적 배열은 지금처럼 최신 입력건이 최상단에 유지된다(현행 유지).
- 신규 입력은 먼저 입력한 건이 가장 위, 마지막 입력건이 가장 아래로 배치되고,
  본래 대시보드로 이관할 때 그 순서 그대로 이관되어야 한다.
- 버전을 `v0.1.7`로 올린다.

### 원인

- `estimate_app/core/model.py:169` `filter_items()`가 현황판을 `(added_at, no)`
  내림차순으로 세운다.
- `estimate_app/core/model.py:182` `sort_new_items()`는 신규품목 창을 오름차순으로 세운다.
- 두 정렬 방향이 반대라, 이관 시 카드가 제 등록 시각을 들고 넘어가면 현황판에서
  마지막 등록건이 최상단으로 뒤집힌다.

### 결정

- 이관 블록 위치: 최상단(현황판의 '최신이 위' 규칙과 일관).
- 업로드 정렬: 현행 유지(요청 범위가 신규 입력이라 변경 위험을 최소화).

### 조치

- `filter_items()`의 2차 키를 `order_key`로 열어 두고 기본값은 `no`로 남겼다(하위 호환).
- `transfer_all()`에서 이관 시각을 배치 전체 `added_at`으로 통일하고 `order_key = -no`를
  부여해 블록 내부 순서를 등록 순서로 되돌렸다.
- 앱/설치 버전을 `0.1.7`로 갱신했다.

### 검증

- `python -m compileall -q estimate_app main.py` 통과.
- 정렬 시나리오 5종(업로드 현행 유지 / 1차 이관 / 2차 이관 / 하위 호환 / 검색) 전부 통과.
- 상세 기록은 `ver_plan.md`의 같은 날짜 항목 참고.
