# 실행 파일 생성 작업 기록

## 2026-08-03 승인된 작업

- 대상 파일: `machine_estimate_app.py`
- 목표: 더블클릭으로 실행할 수 있는 Windows 실행 파일 생성
- 방식: PyInstaller를 사용해 GUI용 단일 EXE 생성
- 포함 파일: `견적_산정\양식\견적용.xlsx` 양식을 EXE 내부에 포함
- 코드 보강:
  - 일반 실행과 EXE 실행 모두에서 양식 파일을 찾도록 경로 처리 추가
  - 저장 결과 `견적용_입력완료.xlsx`는 실행 파일이 있는 폴더에 생성되도록 변경
- 검증 예정:
  - 파이썬 문법 검사
  - PyInstaller 빌드 성공 확인
  - 생성된 EXE 실행 시작 확인

## 완료 결과

- 생성 파일: `exe_release\Machine_Estimate.exe`
- 빌드 방식: PyInstaller `--onefile --windowed`
- 양식 포함: `견적_산정\양식\견적용.xlsx`를 실행 파일 내부에 포함
- 실행 확인: EXE를 5초간 실행해 즉시 종료되지 않는 것을 확인
- 결과 저장 위치: EXE와 같은 폴더에 `견적용_입력완료.xlsx` 생성
- 최종 위치의 `exe_release\Machine_Estimate.exe`도 실행 시작 확인 완료.

## 2026-08-03 공정 시간 입력창 미표시 수정 계획

### 현상

- 항목 입력 팝업에서 기본 정보와 Size 입력 영역은 보이지만, 각 공정별 시간 입력 영역이 보이지 않는다.
- 코드에는 `설비 및 작업별 공수 시간` 입력 프레임이 존재하지만, 현재 팝업 배치/스크롤 구조에서는 실제 화면에서 사용자가 바로 확인하기 어렵다.

### 원인 판단

- 공정 시간 입력 영역이 Size 영역 아래쪽에 세로로 배치되어 있어, 창 크기/배율/스크롤 상태에 따라 화면에서 사라진 것처럼 보인다.
- 사용자는 공정 시간 입력을 반드시 입력해야 하므로, 숨겨질 수 있는 아래쪽 배치보다 항상 보이는 고정 영역 배치가 적합하다.

### 수정 계획

1. 항목 입력 팝업을 좌우 2단 구조로 변경한다.
2. 왼쪽에는 기존 `기본 정보`, `Size 입력`을 유지한다.
3. 오른쪽에는 `각 공정별 시간 입력` 영역을 항상 보이도록 고정 배치한다.
4. 기존 공정 항목과 계산 로직은 그대로 유지한다.
5. 저장/저장 후 다음 항목 버튼은 기존처럼 하단에 유지한다.
6. 수정 후 파이썬 문법 검사를 진행한다.
7. 실행 파일도 다시 빌드해서 `exe_release\Machine_Estimate.exe`를 갱신한다.

### 승인 대기

- 위 방향으로 수정 진행 승인을 기다린다.

## 2026-08-03 공정 시간 입력창 미표시 수정 완료

- 승인 후 `machine_estimate_app.py` 팝업 구조를 좌우 2단으로 변경했다.
- 왼쪽에는 `기본 정보`와 `Size 입력`을 유지했다.
- 오른쪽에는 `각 공정별 시간 입력` 영역을 항상 보이도록 배치했다.
- 기존 공정 항목, 계산 로직, 저장 로직은 유지했다.
- 다음 단계로 문법 검사와 EXE 재빌드를 진행한다.

## 재빌드 완료

- `exe_release\Machine_Estimate.exe`를 새 UI가 반영된 실행 파일로 갱신했다.
- 공정 시간 입력 영역이 오른쪽에 항상 보이도록 팝업 구조를 변경한 상태로 빌드했다.
- 최종 EXE 실행 시작 확인 완료: 5초 동안 즉시 종료되지 않음.

## 2026-08-03 공정 시간 입력 영역 재점검 및 추가 수정

- 문제: 오른쪽에 공정 시간 입력 영역이 여전히 나타나지 않음.
- 재점검 결과: 오른쪽 프레임은 코드에 있었지만, 바깥 스크롤 캔버스 구조가 폭 배치를 꼬이게 만들 가능성이 있음.
- 조치: 팝업 내부의 스크롤 캔버스 구조를 제거하고, 본문을 직접 좌우 2단 그리드로 고정 배치했다.
- 왼쪽 영역 최소 폭: 560px, 오른쪽 공정 시간 입력 영역 최소 폭: 420px로 지정했다.
- 팝업 기본 크기: 1080x560, 최소 크기: 980x520으로 조정했다.

## 2026-08-03 공정 시간 입력 영역 재빌드 완료

- 스크롤 캔버스를 제거한 팝업 구조로 EXE를 다시 빌드했다.
- `exe_release\Machine_Estimate.exe`를 새 빌드로 교체했다.
- 기존 `exe_release\Machine_Estimate.exe`가 실행 중이라 파일이 잠겨 즉시 덮어쓰기 불가.
- 같은 폴더에 새 수정본 `exe_release\Machine_Estimate_fixed.exe`로 생성한다.

## 2026-08-03 견적용.xlsx 로드/날짜별 누적 저장/UI 130% 확대 계획

### 요청 내용

- `견적용.xlsx` 양식을 기준으로 로드해서 사용한다.
- 입력 결과가 한 번 저장하고 끝나는 방식이 아니라 누적 입력 가능해야 한다.
- 작성 날짜 기준으로 구분할 수 있어야 한다.
- 창 UI의 폰트 크기를 현재의 130% 수준으로 키운다.

### 확인한 현재 구조

- 양식 파일 위치: `견적_산정\양식\견적용.xlsx`
- 양식 시트: `견적서`, `기계`, `Sheet1`
- 실제 입력 시트: `기계`
- 입력 시작 행: 7행
- 입력 주요 열:
  - B: 품번
  - C: 품명
  - D: Coment
  - E: 가능여부
  - F: Qty
  - G: Material
  - H: Size
  - K:T: 각 공정별 시간
  - U: SUM 수식
  - V: 단가 수식
  - W: 최종단가 수식
- 현재 저장 방식은 `견적용_입력완료.xlsx` 하나에 덮어쓰는 구조라 누적 저장에 부적합하다.

### 수정 계획

1. 프로그램 실행 시 `견적_산정\양식\견적용.xlsx`를 기준 양식으로 사용한다.
2. 저장 시 작성 날짜 기준 폴더를 만든다.
   - 예: `견적_산정\2026년도\08월`
3. 저장 파일명은 날짜 기준으로 생성한다.
   - 예: `견적누적_2026-08-03.xlsx`
4. 해당 날짜 파일이 없으면 `견적용.xlsx` 양식을 복사해서 새 날짜 파일을 만든다.
5. 해당 날짜 파일이 이미 있으면 `기계` 시트의 마지막 입력 행 다음에 새 항목을 이어 쓴다.
6. 입력된 항목만 저장하고, 빈 항목은 누적 저장 대상에서 제외한다.
7. 새로 추가되는 행에는 기존 양식 행의 수식/서식을 복사해 `SUM`, `단가`, `최종단가` 계산 구조를 유지한다.
8. UI 폰트는 기존 크기 대비 약 130%로 키운다.
   - 기본 글자: 10 → 13 수준
   - 팝업/버튼/표 글자도 함께 확대
   - 표 행 높이도 글자 크기에 맞게 증가
9. 수정 후 파이썬 문법 검사를 진행한다.
10. EXE를 다시 빌드해 실행 파일을 갱신한다.

### 승인 대기

- 위 계획대로 진행하려면 사용자 승인을 받은 뒤 수정한다.

## 2026-08-03 날짜별 누적 저장 및 UI 130% 확대 구현 완료

- `견적_산정\양식\견적용.xlsx`를 기준 양식으로 로드하도록 유지했다.
- 저장 시 `견적_산정\YYYY년도\MM월\견적누적_YYYY-MM-DD.xlsx` 파일을 대상으로 사용하도록 변경했다.
- 날짜 파일이 없으면 양식에서 새로 만들고, 이미 있으면 기존 입력 아래에 누적 저장하도록 변경했다.
- 빈 항목은 저장하지 않도록 했다.
- 새 입력 행에는 기존 양식의 서식과 수식을 복사/재생성하도록 했다.
- UI 기본 폰트, 버튼, 입력창, 표, 팝업 글자 크기를 약 130% 수준으로 확대했다.

## 2026-08-03 날짜별 누적 저장/UI 확대 EXE 재빌드 완료

- PyInstaller 빌드 완료.
- 최신 실행 파일을 `exe_release\Machine_Estimate_latest.exe`로 생성했다.
- 기존 `Machine_Estimate.exe`가 실행 중일 수 있어, 덮어쓰기 전에 프로세스 잠금 여부를 확인한다.
- 실행 중인 이전 프로세스가 없어 `exe_release\Machine_Estimate.exe`도 최신 빌드로 교체했다.
- 문법 검사 통과.
- 임시 파일 기준 누적 저장 검증 완료: 1차/2차 저장이 7행, 8행으로 이어짐.
- 8개 항목 저장 검증 완료: 합계 행 앞에 행 삽입, 합계 수식 `=SUM(W7:W14)` 확인.
- 최종 EXE 실행 시작 확인 완료: 5초 동안 즉시 종료되지 않음.

## 2026-08-04 카드형 견적 현황판 전환 계획

### 요청 내용

- `견적용.xlsx` 양식을 기준으로 입력한다.
- 입력된 견적 항목을 표 형태가 아니라 카드 형태로 저장/표시한다.
- TSERP에서 만든 현황판처럼 한눈에 볼 수 있는 카드형 화면으로 구성한다.
- 기존 날짜별 누적 저장 방식은 유지한다.
- UI는 현재 130% 확대 기준을 유지한다.

### 현재 상태

- 현재 메인 화면은 `Treeview` 표 형태로 10개 기본 행을 표시한다.
- 상세 입력은 항목 더블클릭 팝업에서 처리한다.
- 저장은 `견적_산정\YYYY년도\MM월\견적누적_YYYY-MM-DD.xlsx`에 날짜별로 누적 저장한다.
- `견적용.xlsx`의 `기계` 시트 구조는 유지 중이다.

### 수정 방향

1. 메인 화면을 카드형 현황판 구조로 변경한다.
   - 상단: 저장 버튼, 새 카드 추가 버튼, 오늘 날짜/총 항목/총 견적금액 요약
   - 본문: 입력된 견적 항목을 카드 형태로 나열
2. 카드에는 핵심 정보를 표시한다.
   - NO
   - 작성일
   - 품번
   - 품명
   - 가능여부
   - Qty
   - Material
   - Size
   - 공정 시간 합계
   - 최종단가
3. 카드 색상은 상태별로 구분한다.
   - 가능: 정상/진행 느낌
   - 검토필요: 주의 느낌
   - 불가: 경고 느낌
4. 카드 클릭 또는 `수정` 버튼으로 기존 상세 입력 팝업을 연다.
5. `새 카드 추가` 버튼으로 빈 견적 카드와 입력 팝업을 만든다.
6. 카드 저장 시 기존 날짜별 누적 Excel 저장 로직을 그대로 사용한다.
7. 카드 화면은 스크롤 가능하게 만들어 항목이 늘어나도 현황판처럼 볼 수 있게 한다.
8. 기존 `견적용.xlsx` 양식 구조와 계산/저장 수식은 변경하지 않는다.
9. 수정 후 검증한다.
   - 파이썬 문법 검사
   - 카드 추가/수정 로직 코드 확인
   - 날짜별 누적 저장 로직 유지 확인
   - EXE 재빌드 및 실행 시작 확인

### 작업 범위에서 제외

- 기존 `04.CSS_조건 산출기.hta`, `05.견적.hta`는 수정하지 않는다.
- 엑셀 양식 자체의 시트 구조는 변경하지 않는다.
- 별도 데이터베이스는 추가하지 않는다.

### 승인 대기

- 사용자 승인 후 `machine_estimate_app.py`를 카드형 현황판 UI로 수정하고 EXE를 다시 빌드한다.

## 2026-08-04 카드형 견적 현황판 구현 진행

- 기존 메인 `Treeview` 표 화면을 제거했다.
- 메인 화면을 상단 요약 영역과 스크롤 가능한 카드 목록으로 변경했다.
- 오늘 날짜 누적 파일이 있으면 `기계` 시트 내용을 읽어 카드로 표시하도록 했다.
- 카드에는 작성일, 품번, 품명, 가능여부, Qty, Material, Size, 시간합계, 최종단가, 메모를 표시한다.
- 상태별 카드 색상을 다르게 표시하도록 했다.
- 카드의 `수정` 버튼 또는 카드 더블클릭으로 기존 상세 입력 팝업을 열도록 했다.
- `새 카드 추가` 버튼은 빈 카드를 만들고 상세 입력 팝업을 연다.
- 기존 누적 파일에서 불러온 카드는 엑셀 행 번호를 기억하도록 했다.
- 불러온 카드를 다시 저장하면 중복 추가하지 않고 해당 행을 갱신하도록 했다.
- 신규 카드는 마지막 입력 행 다음에 추가되도록 검증했다.
- 임시 검증 결과: P1/P2 불러오기 후 P1 수정 + P3 추가 시 엑셀 행은 P1 수정본, P2, P3 총 3건으로 유지됨.

## 2026-08-04 카드형 현황판 점검 기록

- GUI 앱을 직접 실행하면 창이 떠 있는 동안 명령이 종료되지 않아 작업이 멈춘 것처럼 보일 수 있음을 확인했다.
- `python -m py_compile machine_estimate_app.py` 문법 검사는 통과했다.
- 시작 시 날짜 폴더를 만들지 않도록 `get_dated_output_path(create_dir=False)`를 로드 경로에 적용했다.
- 짧은 실행 확인 결과 앱이 5초 동안 즉시 종료되지 않아 정상 시작으로 확인했다.

## 2026-08-04 카드형 현황판 EXE 재빌드 완료

- PyInstaller 빌드 완료.
- `exe_release\Machine_Estimate.exe`와 `exe_release\Machine_Estimate_latest.exe`를 최신 카드형 현황판 버전으로 교체했다.
- 최종 EXE 실행 시작 확인 완료: 5초 동안 즉시 종료되지 않음.
- 카드형 현황판 UI, 기존 카드 갱신/신규 카드 추가 검증, 날짜별 누적 저장 구조를 반영한 빌드다.

## 2026-08-04 양식 업로드/저장/검색 기능 추가 계획

### 요청 내용

- 사용자가 가지고 있는 `견적용.xlsx` 계열 양식 파일을 업로드해서 프로그램 카드 목록으로 불러온다.
- 업로드한 내용을 카드형 현황판에 표시한다.
- 불러온 내용을 수정하거나 신규 카드와 함께 날짜별 누적 파일로 저장할 수 있게 한다.
- Search/검색 기능으로 카드 목록에서 원하는 항목을 찾을 수 있게 한다.

### 현재 상태

- 메인 화면은 카드형 현황판으로 구성되어 있다.
- 오늘 날짜 누적 파일이 있으면 `기계` 시트 내용을 카드로 불러온다.
- 날짜별 누적 저장은 유지되어 있다.
- 기존 카드는 엑셀 행 번호를 기억해서 다시 저장할 때 중복 저장하지 않고 갱신한다.

### 수정 계획

1. 상단 버튼 영역에 `양식 업로드` 버튼을 추가한다.
2. `tkinter.filedialog`를 사용해 사용자가 `.xlsx` 파일을 선택할 수 있게 한다.
3. 업로드 대상 파일에서 `기계` 시트를 찾는다.
4. `기계` 시트의 7행부터 합계 행 전까지 입력된 내용을 읽어 카드 데이터로 변환한다.
5. 업로드 방식은 안전하게 처리한다.
   - 기존 화면 카드와 합치기 전에 사용자 확인을 받는다.
   - 기본 동작은 현재 화면 카드에 추가한다.
   - 중복 품번/품명은 카드가 중복될 수 있음을 안내한다.
6. 업로드된 카드는 `excel_row=None`으로 두어 저장 시 오늘 날짜 누적 파일의 새 행으로 추가되게 한다.
7. 상단에 검색창과 검색 초기화 버튼을 추가한다.
8. 검색 대상은 다음 필드로 한다.
   - 품번
   - 품명
   - 가능여부
   - Material
   - Size
   - Coment
   - 작성일
9. 검색어가 있으면 해당 카드만 표시하고, 요약 영역에는 검색 결과 수를 함께 보여준다.
10. 수정 후 검증한다.
   - 파이썬 문법 검사
   - 업로드 파서 임시 검증
   - 검색 필터 검증
   - EXE 재빌드 및 실행 시작 확인

### 작업 범위에서 제외

- 엑셀 양식 자체의 시트 구조 변경은 하지 않는다.
- 별도 데이터베이스는 추가하지 않는다.
- 기존 HTA 파일은 수정하지 않는다.

### 승인 대기

- 사용자 승인 후 `machine_estimate_app.py`에 업로드/검색 기능을 구현하고 EXE를 다시 빌드한다.

## 2026-08-04 양식 업로드/검색 기능 구현 진행

- 상단에 `양식 업로드` 버튼을 추가했다.
- 상단에 Search 검색창과 `검색 초기화` 버튼을 추가했다.
- 선택한 `.xlsx` 파일의 `기계` 시트를 읽어 카드 데이터로 변환하는 공통 파서를 추가했다.
- 오늘 날짜 누적 파일 로드도 같은 파서를 사용하도록 정리했다.
- 업로드된 카드는 저장 시 오늘 날짜 누적 파일의 새 행으로 추가되도록 `excel_row=None`으로 처리한다.
- 검색 대상은 작성일, 품번, 품명, 가능여부, Material, Size, Coment, Qty, 시간합계, 최종단가로 구성했다.
- 문법 검사 통과.
- 테스트 파일 기준 업로드 파서 검증 완료: 저장된 `.xlsx`에서 카드 1건을 다시 읽음.
- 검색 검증 완료: 품번 `SEARCH`, Material `S45C`는 검색됨, 없는 단어는 검색되지 않음.
- 실행 오류 수정: `tk.Frame`의 `pady=(0, 10)` 옵션을 `pack(..., pady=(0, 10))`로 옮겨 Tkinter 화면 거리 오류를 해결했다.

## 2026-08-04 양식 업로드/검색 기능 EXE 재빌드 완료

- PyInstaller 빌드 완료.
- `exe_release\Machine_Estimate.exe`와 `exe_release\Machine_Estimate_latest.exe`를 업로드/검색 기능 포함 최신 버전으로 교체했다.
- 최종 EXE 실행 시작 확인 완료: 5초 동안 즉시 종료되지 않음.
- 업로드/검색/카드형 현황판/날짜별 누적 저장 기능이 포함된 빌드다.

## 2026-08-04 연도별 전체 견적 파일 자동 업로드 및 전체 화면 실행 계획

### 요청 내용

- `C:\Users\SumH\Codex\Estimate\견적_산정\2026년도` 경로 아래 각 월별 폴더에 있는 입력분을 프로그램 실행 시 자동 업로드한다.
- 업로드된 전체 내용을 카드 현황판에서 검색할 수 있게 한다.
- 프로그램 실행 시 창을 전체 화면 또는 최대화 상태로 연다.

### 확인한 현재 파일 구조

- 자동 업로드 대상 폴더: `견적_산정\2026년도`
- 확인된 월별 엑셀 파일: 총 18개
  - `04월`: 1개
  - `05월`: 4개
  - `06월`: 8개
  - `07월`: 5개
- 현재 프로그램은 실행 시 오늘 날짜 누적 파일 하나만 자동 로드한다.
- 수동 `양식 업로드` 기능은 이미 있다.
- 검색 기능은 이미 카드 목록에 적용되어 있다.

### 수정 계획

1. 실행 시 자동 로드 대상을 오늘 날짜 파일 1개에서 `견적_산정\2026년도` 전체 `.xlsx` 파일로 확장한다.
2. 월별 폴더 안의 모든 `.xlsx` 파일을 순회한다.
3. `견적_산정\양식\견적용.xlsx`는 기준 양식이므로 자동 업로드 대상에서 제외한다.
4. 각 파일에서 `기계` 시트의 7행부터 합계 행 전까지 입력 항목을 읽어 카드로 변환한다.
5. 카드에는 원본 파일명/원본 경로/월 정보를 함께 보관한다.
6. 검색 대상에 원본 파일명과 월 정보를 추가한다.
7. 같은 파일을 읽어온 카드는 저장 시 기존 원본 파일을 직접 수정하지 않고, 현재 날짜 누적 파일에 새 카드로 저장되도록 한다.
8. 기존 수동 `양식 업로드` 기능은 유지한다.
9. 실행 시 창은 Windows 최대화 상태로 시작한다.
   - `root.state("zoomed")` 우선 적용
   - 실패 시 기존 큰 창 크기 유지
10. 수정 후 검증한다.
   - 파이썬 문법 검사
   - `견적_산정\2026년도` 스캔 파일 수 확인
   - 실제 월별 파일에서 카드 로드 수 확인
   - 검색 대상에 파일명/월 포함 확인
   - EXE 재빌드 및 실행 시작 확인

### 주의 사항

- 월별 기존 파일 원본은 자동으로 수정하지 않는다.
- 저장은 기존 방식대로 오늘 날짜 누적 파일에 반영한다.
- 기존 HTA 파일과 엑셀 양식 구조는 변경하지 않는다.

### 승인 대기

- 사용자 승인 후 `machine_estimate_app.py` 수정 및 EXE 재빌드를 진행한다.

## 2026-08-04 연도별 전체 자동 업로드 및 최대화 실행 구현 완료

- 실행 시 창이 Windows 최대화 상태로 열리도록 `root.state("zoomed")`를 적용했다.
- `견적_산정\2026년도` 아래 월별 `.xlsx` 파일 전체를 자동 스캔하도록 변경했다.
- 실제 확인 결과 자동 업로드 대상 파일은 18개이며, 18개 파일 모두에서 카드 데이터가 확인되었다.
- 자동 업로드 카드 수: 319건.
- 오래된 양식의 Size가 H~J에 나뉜 경우 `19 x 820 x 1200` 형태로 합쳐 표시하도록 했다.
- 오래된 양식의 공정 시간 영역에 수식 셀이 섞인 경우 숫자 변환 오류가 나지 않도록 수식/문자값은 0으로 처리하는 안전 변환을 추가했다.
- 카드에 원본 월/파일명을 표시하고, 검색 대상에도 포함했다.
- 과거 원본 파일에서 자동 업로드된 카드는 기본적으로 저장 대기 대상이 아니며, 수정한 경우 오늘 날짜 누적 파일에 저장되도록 했다.

## 2026-08-04 연도별 전체 자동 업로드/최대화 EXE 재빌드 완료

- PyInstaller 빌드 완료.
- `exe_release\Machine_Estimate.exe`와 `exe_release\Machine_Estimate_latest.exe`를 최신 버전으로 교체했다.
- 기존 `Machine_Estimate.exe`가 2개 실행 중이라 기본 실행 파일명은 Windows 파일 잠금으로 교체되지 않았다.
- `exe_release\Machine_Estimate_latest.exe`는 최신 빌드로 교체 완료했다.
- 최신 EXE 실행 시작 확인 완료: 7초 동안 즉시 종료되지 않음.
- 기본 파일명 교체는 실행 중인 기존 `Machine_Estimate.exe` 창을 모두 닫은 뒤 가능하다.

## 2026-08-04 20:50:21 기계 시트 카드 업로드 재점검 및 실행파일 갱신
- 사용자 지정 파일 견적_산정\2026년도\04월\PLK972304 외.xlsx의 기계 시트만 읽어 카드로 변환되도록 재확인함.
- 해당 파일의 기계 시트에서 27개 카드가 추출됨을 확인함.
- 카드 UI에 K:T 공정 시간 표시 영역을 추가함: 5축, 4축, 3축, 선반, 범용, 사상, 3차원, 연마, 지그, 프로그래밍.
- 상단 업로드 버튼 문구를 기계 시트 업로드로 변경하여 업로드 대상이 명확히 보이게 함.
- 연도 폴더 견적_산정\2026년도 자동 로드 시 18개 파일에서 319개 행을 읽고, 실제 카드 표시 데이터 196건을 확인함.
- Tkinter 실제 위젯 검사 결과 전체화면 상태와 공정 시간 라벨/값 표시를 확인함.
- PyInstaller로 실행 파일을 재빌드하고 exe_release\Machine_Estimate.exe, exe_release\Machine_Estimate_latest.exe를 최신본으로 갱신함.
- 기존 실행 중이던 구버전 Machine_Estimate.exe 프로세스 2개는 파일 교체를 위해 종료함.
- 최신 Machine_Estimate.exe 7초 실행 검증 결과 정상 실행됨.

## 2026-08-05 07:31:45 멈춤 버그 및 딥블루 UI 개선 계획

### 현재 점검 결과
- 실행 중인 Machine_Estimate.exe가 2개 확인됨. 중복 실행 상태에서 파일 로드/저장/교체가 겹치면 멈춤이나 잠금이 자주 발생할 수 있음.
- load_existing_cards()가 시작 시 견적_산정\2026년도의 모든 xlsx를 UI 스레드에서 즉시 읽음. 이 동안 창이 멈춘 것처럼 보일 수 있음.
- efresh_table()이 검색/저장/수정 때마다 표시 대상 카드 전체를 파괴 후 재생성함. 현재 카드 300건 내외 기준 라벨 위젯이 1만 개 이상 생성되어 멈춤 원인이 됨.
- 팝업 입력창은 1080x560, 좌/우 컬럼 고정 폭에 130% 폰트가 적용되어 Size 입력/공정 입력 영역이 짤릴 수 있음.
- 현재 색상은 밝은 카드형 UI라 사용자가 요청한 TSERP와 동일한 딥블루 모드가 적용되어 있지 않음.

### 수정 계획
1. 멈춤 완화
   - 시작 시 자동 업로드는 백그라운드 스레드에서 수행하고, UI에는 로드 중 상태를 먼저 표시함.
   - 검색 입력은 키 입력마다 즉시 전체 렌더링하지 않고 짧은 지연 후 1회만 반영하도록 디바운스 처리함.
   - 카드 렌더링은 한 번에 전체를 만들지 않고 기본 표시 개수를 제한하고 더 보기 방식으로 추가 표시함.

2. 중복 실행/파일 잠금 방지
   - 실행 파일 교체 전 프로세스를 확인하고 종료 후 교체함.
   - 앱 내부에서는 저장 중 버튼 중복 클릭을 막아 같은 파일을 동시에 저장하지 않도록 함.

3. TSERP 딥블루 UI 적용
   - 배경, 헤더, 카드, 검색 영역, 공정 시간 칩을 딥블루 계열로 통일함.
   - 글자 대비는 밝게 조정하고 상태 색상은 딥블루 배경에서도 보이도록 재설정함.

4. 입력창 짤림 수정
   - 팝업 기본 크기를 확대하고 최소 크기를 올림.
   - 팝업 내부에 스크롤 가능한 입력 영역을 적용하여 작은 화면에서도 하단 버튼/공정 입력이 잘리지 않게 함.
   - 기본정보/Size/공정별 시간 Entry 폭과 컬럼 weight를 조정함.

5. 검증 및 배포
   - python -m py_compile machine_estimate_app.py 실행.
   - Tkinter 위젯 크기 검사로 공정 입력 Entry 10개가 표시되는지 확인.
   - PyInstaller로 EXE 재빌드 후 exe_release\Machine_Estimate.exe와 Machine_Estimate_latest.exe 갱신.
   - 작업 완료 후 er_plan.md에 실제 수정/검증 결과를 한글로 기록.

### 승인 필요
- 위 범위대로 수정 진행하려면 사용자 승인 후 코드 수정 및 실행 파일 재빌드를 진행함.

## 2026-08-05 07:51:46 멈춤 버그 개선, TSERP 딥블루 UI, 입력창 짤림 수정 완료

### 수정 내용
- 시작 시 견적_산정\2026년도 엑셀 전체 로드를 UI 스레드에서 직접 수행하지 않고 백그라운드 스레드 + Queue 폴링 방식으로 변경함.
- Tkinter를 백그라운드 스레드에서 직접 호출하지 않도록 수정하여 main thread is not in main loop 예외 가능성을 제거함.
- 검색 입력은 250ms 디바운스 방식으로 변경하여 키 입력마다 전체 카드가 재생성되지 않게 함.
- 카드 렌더링은 기본 40개만 표시하고 더 보기 버튼으로 40개씩 추가 표시하도록 변경함.
- 저장 중복 클릭 방지를 위해 저장 진행 중 재진입을 차단함.
- 메인 UI를 TSERP 계열 딥블루 모드로 변경함: 배경 #07111f, 패널 #0d1b2e, 카드 #132b46.
- 카드/공정 시간 칩/상태 배지를 딥블루 배경에서 읽기 쉬운 색상으로 조정함.
- 팝업 입력창을 1380x820, 최소 1240x720으로 확대하고 내부 스크롤 Canvas를 적용함.
- 공정별 시간 입력을 2열 압축 배치에서 1공정 1행 배치로 변경하여 130% 폰트에서도 Entry가 짤리지 않게 함.
- 새로 작성된 한국어 UI 문구가 깨진 부분을 유니코드 기준으로 복구함.

### 검증 결과
- python -m py_compile machine_estimate_app.py 통과.
- Tkinter 실제 위젯 검사 결과 견적_산정\2026년도 데이터 319건 로드 완료.
- 첫 화면 렌더링은 40개 카드 + 더보기로 제한되어 card_container 직접 자식 41개 확인.
- 팝업 크기 1380x820, Entry 20개, 최소 Entry 폭 104px 확인.
- 딥블루 배경 색상 #07111f 확인.
- exe_release\Machine_Estimate.exe 실행 확인 완료.
- exe_release\Machine_Estimate.exe, exe_release\Machine_Estimate_latest.exe 최신 빌드로 갱신함.

## 2026-08-05 설치 파일 제작 및 업데이트 방식 도입 계획

### 요청 내용 (nextup v.0.0.1MD)

- Inno Setup Compiler를 이용해 설치 파일을 제작한다.
- 실행 폴더에 업데이트 폴더를 만들고, 이후 기능/개선 사항은 그 폴더를 통한 업데이트 방식으로 배포한다.
- 버전은 v0.0.1로 시작한다.

### 확인한 현재 상태

- `Inno Setup 7`이 `C:\Program Files\Inno Setup 7\ISCC.exe`에 설치되어 있음.
- `machine_estimate_app.py`에는 버전 정보가 없음.
- 빌드 산출물은 `exe_release\Machine_Estimate.exe` (PyInstaller onefile, windowed).

### 사용자 결정 사항

- 설치 위치: Program Files (표준 Windows 설치 경로).
- 업데이트 방식: 수동 교체 방식. 설치 폴더 안에 `update` 폴더를 두고, 새 버전 설치 파일(Inno Setup installer)을 그 폴더에 넣으면 프로그램이 실행 시 감지해서 교체를 안내한다.

### 수정 계획

1. `machine_estimate_app.py`
   - `APP_VERSION = "0.0.1"` 상수를 추가하고 창 제목에 버전을 표시한다.
   - 앱 시작 시 `{실행파일 폴더}\update` 폴더를 확인해 `.exe` 설치 파일이 있으면 사용자에게 실행 여부를 확인하는 안내창을 띄운다.
   - 사용자가 승인하면 해당 설치 파일을 실행한다. 실제 파일 교체/재시작은 Inno Setup의 `CloseApplications`/`RestartApplications` 기능에 위임한다.
2. Inno Setup 스크립트 신규 작성: `installer\Setup.iss`
   - `AppId`는 고정 GUID로 지정해 이후 버전에서도 동일 설치로 인식되게 한다.
   - `AppVersion 0.0.1`, 설치 대상 `Program Files\기계 시트 표준 견적 입력 시스템`.
   - `exe_release\Machine_Estimate.exe`를 설치 파일에 포함한다.
   - 설치 시 `update` 빈 폴더를 함께 생성한다.
   - 시작 메뉴 바로가기를 생성하고, 바탕화면 아이콘 생성 여부는 설치 중 선택하게 한다.
   - `CloseApplications=yes`, `RestartApplications=yes`로 설정해 실행 중인 앱을 감지해 종료 후 재시작하도록 한다.
3. `ISCC.exe`로 `Setup.iss`를 컴파일해 설치 파일(`installer\Output\...`)을 생성한다.
4. 실제 설치(Program Files에 설치)는 관리자 권한이 필요하므로 이번 단계에서 자동 실행하지 않고, 생성된 설치 파일만 검증한다.
5. 수정 후 검증한다.
   - 파이썬 문법 검사
   - `ISCC.exe` 컴파일 성공 확인
   - 생성된 설치 파일 존재 확인
6. `ver_plan.md`에 구현/검증 결과를 기록한다.

### 작업 범위에서 제외

- 자동 설치 실행(Program Files 쓰기)은 사용자 승인 없이 진행하지 않는다.
- 기존 카드형 현황판 기능/저장 로직은 변경하지 않는다.

## 2026-08-05 업데이트 v0.0.2 선택 다운로드 계획

### 요청 내용 (`nextup v.0.0.2.MD`)

- 선택 다운로드 기능을 추가한다.
- 다운로드 양식은 기존 설정 양식인 `견적_산정\양식\견적용.xlsx` 구조를 따른다.
- 조임쇠 영역 기준 다중 검색 후 항목을 선택하고 다운로드할 수 있게 한다.
- 신규 입력 시에는 별도 창에서 신규분만 먼저 저장한 뒤 다운로드할 수 있게 한다.

### 현재 상태

- 앱/설치 스크립트 버전은 `0.0.1`이다.
- 현재 화면에는 업로드, 새 카드 추가, 날짜별 누적 저장, 단일 검색 기능이 있다.
- 현재 저장은 전체 저장 대상을 날짜별 누적 파일에 저장하는 방식이며, 선택 항목만 별도 양식으로 내보내는 기능은 없다.

### 수정 계획

1. `APP_VERSION`과 `Setup.iss` 버전을 `0.0.2`로 올린다.
2. 카드별 체크박스와 선택 상태 관리 기능을 추가한다.
3. 상단에 `전체 선택`, `선택 해제`, `선택 다운로드`, `신규 입력 다운로드` 버튼을 추가한다.
4. 검색어를 공백/쉼표 기준으로 나누어 다중 검색이 가능하게 한다.
5. `조임쇠 영역`은 별도 컬럼 확인 전까지 품번, 품명, Coment, Material, Size, 원본 파일명 검색 대상으로 적용한다.
6. 선택 다운로드는 기존 `견적용.xlsx` 양식을 복사해 선택 카드만 `기계` 시트 구조에 맞춰 기록한다.
7. 저장 위치는 파일 저장 창으로 사용자가 지정하고 기본 파일명은 `선택견적_YYYY-MM-DD.xlsx`로 제안한다.
8. 신규 입력 다운로드는 별도 입력 창에서 신규 항목만 작성한 뒤 그 항목만 먼저 다운로드 파일로 저장한다.
9. 기존 날짜별 누적 저장, 카드형 현황판, 자동 업로드, 업데이트 감지 방식은 유지한다.
10. 문법 검사, 선택/검색/다운로드 검증, EXE 재빌드, Inno Setup 설치 파일 생성을 진행한다.

### 승인 대기

- 사용자 승인 후 코드 수정, EXE 재빌드, `MachineEstimate_Setup_v0.0.2.exe` 생성을 진행한다.

## 2026-08-09 v0.0.9 저장형태·현황판·설정창 개편 계획

### 요청 내용 (`v0.0.9.md`)

1. 저장형태 변경 및 현황판 수정 - 누적 표시를 업로드 시간 내림차순(먼저 올린 카드가 아래)으로,
   현황판 맨 앞에 No 부여, 품명과 소재 사이에 Comment 표기.
2. Shift/Ctrl 키를 윈도우 파일 선택처럼 사용, 카드는 원클릭이 아니라 더블클릭으로 열기.
3. PDF 출력 제외(엑셀만), 견적서 시트 내용 전체 표기, 단가 합계 `####` 해소,
   회사/직위/작성자를 설정창에서 입력해 양식에 반영(회사명 `(주)텍스타`, 작성자 `생산부장 황성문`),
   `yyyy-mm-dd` 출력 날짜 기재.
4. 기능 제거 - 보조기능의 `신규 입력 다운로드`, `날짜별 누적 저장`, 카드의 기계 시트 파일 표기, 엑셀 행 표기.
   No는 엑셀 행이 아니라 1번의 화면 순번으로 기재.
5. 카드 입력 - Comment를 수동으로 늘려 2줄 이상 작성, Material에 열처리 체크 시 HRC 경도
   입력창을 아래에 활성화, HRC는 Min/Max를 각각 입력.
6. 기능키 추가 - 설정창을 별도로 만들고 저장하면 전체 적용. 공정별 단가(카드에서는 고정값),
   Material 목록 선택/삭제, 회사명·작성자 기재.
7. 양식 수정 - 견적서(시트 1번) 맨 위 작성 부분을 설정창에서 입력해 지정.

추가 요청(대화): 바탕화면 바로가기 아이콘이 아직 파이썬 아이콘이다. 업데이트 플랜은 개별 파일로
만들지 말고 이 `plan.md`에 통합해서 기록한다.

### 확인한 현재 구조 (직접 재현해 확인한 사실)

- **3-2 `견적서 내용이 전체 표기 안됨`의 실제 원인**: 현재 코드로 3 / 7 / 20 / 45건을
  `excel_io.export_items()`로 내보내 덤프한 결과, 견적서 항목 참조(C/E/G열)는 45건까지 전부 정상으로
  채워졌다(견적서 15행~합계행 직전, 기계 시트 합계행도 14→27→52로 정상 확장). 즉 항목 누락이 아니라
  **견적서 상단 블록이 양식의 플레이스홀더 그대로**인 것이 문제다.
  `B4='회사명㈜ 귀중'`, `B5='  담당자 : 대상업체 담당자 직위'`, `B6='  부   서 : 구매팀 '`,
  `B8='  사업명 : 사업명'`, `F5~F9`(대표자/사업자번호/주소/전화/담당자)가 한 번도 채워지지 않는다.
  이는 요청 7-1과 같은 지점이다.
- **3-6 날짜**: 견적서 `B7`은 `=기계!Q18` 참조인데 기계 `Q18`이 문자열 `'YYYY-MM-DD'`라 날짜가 안 나온다.
  기계 `Q18`에 실제 날짜를 쓰면 견적서 날짜는 참조로 자동 반영된다.
- **3-3 `####`의 실제 원인**: 기계 시트 **X열(최종단가) 폭 9.0**, W열(단가) 폭 12.0인데 서식이
  회계 서식(`_-* #,##0_-;...`)이라 좌우 여백까지 필요하다. 백만 단위부터 잘리고 합계는 확실히 깨진다.
  견적서 G/H는 각 13.0(병합 26)이라 여유가 있다.
- **푸터 위치는 고정이 아니다**: 회사명/날짜/작성자는 기계 `Q17/Q18/Q19`에 있는데, 데이터 행이 늘면
  합계행과 함께 밀린다(20건→Q30/31/32, 45건→Q55/56/57). 항상 **합계행 +3 / +4 / +5**였다.
- **선택 체크박스 기존 버그**: `ui/widgets.py`의 `make_checkbox`가 생성 시점의 `checked`(항상 False)를
  클로저에 고정해 `on_toggle(not checked)`가 언제나 `True`다. 슬롯 재사용 구조에서 체크 해제가 안 된다.
- **아이콘**: `dist`와 설치본(`C:\Program Files (x86)\Estimate\Estimate.exe`) 양쪽에서 아이콘 리소스를
  추출해 확인한 결과 **정상적으로 파란 `E` 아이콘이 박혀 있다**. 바탕화면 바로가기
  (`C:\Users\Public\Desktop\Estimate(견적).lnk`)도 대상 경로가 맞고 `IconLocation=",0"`이다.
  즉 남은 원인은 **셸 아이콘 캐시(v0.0.2~v0.0.4 시절 파이썬 아이콘이 같은 경로로 캐시된 것)**이며,
  보조 요인으로 `estimate.ico`의 4개 엔트리가 전부 PNG 압축(DIB 아님)이라는 점이 있다.
- **단가의 주인 문제**: `core/session.py`가 `rates`를 저장하고 `restore_session()`이
  `self.rates.update(...)`로 덮어쓴다. 설정창이 단가의 주인이 되면 세션 복원이 설정을 무효화한다.

### 수정 계획

1. **설정 저장소 신설** (`core/settings.py`) - `%LOCALAPPDATA%\MachineEstimate\settings.json`에 저장한다
   (`{app}`은 Program Files라 일반 권한으로 못 쓴다). 항목은 공정별 단가, Material 목록,
   회사명·직위·작성자, 견적서 상단 9칸이다. 파일이 없거나 깨져도 코드 기본값으로 뜬다.
   `core/session.py`에서 `rates` 저장/복원을 제거해 설정창을 단가의 유일한 주인으로 만든다.
2. **설정창** (`ui/settings_dialog.py`) - 공정별 단가 입력, Material 목록 추가/삭제,
   회사·작성자, 견적서 상단 입력을 한 창에 담는다. 저장하면 즉시 전체에 적용한다.
3. **현황판** (`ui/table.py`, `ui/dashboard.py`) - 맨 앞에 화면 순번 `No` 열을 추가하고,
   품명과 소재 사이에 `Comment` 열을 넣는다. 표시용 순번은 `display_no`로 따로 두고
   내부 키 `item["no"]`(선택·팝업·슬롯 매핑)는 그대로 둔다. 정렬은 `added_at` 내림차순에
   `no` 내림차순을 2차 키로 더해 같은 초에 올라온 카드의 순서를 고정한다.
4. **선택 동작** - `make_checkbox`의 클로저 버그를 먼저 고친다(클릭 시점의 상태를 읽게 한다).
   행 클릭은 단일 선택, `Ctrl+클릭`은 토글, `Shift+클릭`은 기준점부터 범위 선택으로 바꾸고,
   카드 열기는 **더블클릭**으로 옮긴다. 범위 계산은 `self.data` 순서가 아니라 화면에 보이는
   순서를 기준으로 한다.
5. **카드 입력창** (`ui/popup.py`) - Comment를 `tk.Text` 여러 줄로 바꾸고 아래에 드래그 핸들을 붙여
   높이를 수동으로 늘릴 수 있게 한다. Material은 설정창 목록에서 고르는 콤보박스로 바꾸고,
   그 아래 `열처리` 체크박스를 둔다. 체크하면 `HRC [Min] ~ [Max]` 입력칸이 활성화된다.
   공정별 단가는 입력칸에서 읽기 전용 표시로 바꾼다(설정창 값 고정). 정보 타일에서
   `기계 시트 폴더`/`기계 시트 파일`/`엑셀 행`을 빼고 `NO`는 화면 순번을 보여 준다.
6. **엑셀 출력** (`core/excel_io.py`) - 저장할 때 견적서 상단 9칸과 기계 시트 푸터
   (합계행 +3/+4/+5 = 회사명 / `yyyy-mm-dd` / 직위+작성자)를 설정값으로 채운다.
   기계 W·X열 폭을 넓혀 `####`을 없앤다(서식은 그대로 둔다). 열처리를 체크한 항목은
   Material 값 뒤에 `HRC00~00`을 붙여 기록한다(기계 시트에 열처리 전용 열이 없다).
7. **기능 제거** - 보조기능의 `신규 입력 다운로드`, 헤더의 `날짜별 누적 저장`, PDF 출력 전체
   (`core/pdf_export.py`, 출력 형식 선택창)를 걷어낸다. 누적 저장이 사라지면 `save_pending`(NEW 배지)을
   해제할 곳이 없어지므로, **선택 다운로드 성공 시 해제**로 옮겨 영구히 남는 배지를 만들지 않는다.
8. **아이콘** - `estimate.ico`를 16/32/48은 DIB, 256은 PNG인 표준 조합으로 다시 만든다.
   `installer/Setup.iss`는 `estimate.ico`를 `{app}`에 설치하고 바로가기에
   `IconFilename`을 직접 지정해 exe 단위 캐시를 우회한다. 기존 바로가기를 `InstallDelete`로 지우고
   다시 만들되, 그때 `desktopicon` 작업의 `unchecked`를 없앤다(체크를 안 하면 지우기만 하고
   새로 안 만들어 아이콘이 아예 사라진다). `ChangesAssociations=yes`와 `ie4uinit.exe -show`로
   셸 아이콘 캐시 갱신을 유도한다.
9. 버전을 `estimate_app/__init__.py`와 `installer/Setup.iss` 양쪽 모두 `0.0.9`로 올리고,
   `MachineEstimate_Setup_v0.0.9.exe`를 만든다(파일명 버전이 `updater`의 비교 기준이다).

### 작업 범위에서 제외

- `v0.0.7_plan.md` / `v0.0.8_plan.md`는 과거 기록이라 그대로 두고, 이번 계획만 이 파일에 통합한다.
- 견적 양식(`견적_산정\양식\견적용.xlsx`) 원본과 과거 저장 파일은 손대지 않는다.
  열 폭·상단 문구는 저장할 때 코드가 출력 파일에만 적용한다.
- `Machine_Estimate.spec`의 `_ssl`/`ssl`/`_hashlib` 제외는 유지한다(네트워크 기능이 없다).

## 2026-08-09 v0.0.9 구현 완료 및 설치 파일 생성

### 반영 내용

1. **설정 저장소 신설** (`core/settings.py`) — `%LOCALAPPDATA%\MachineEstimate\settings.json`.
   공정 단가 / Material 목록 / 회사·직위·작성자 / 견적서 상단 9칸을 담는다. 파일이 없거나
   깨져도 코드 기본값으로 뜬다. Material 기본 목록은 기존 견적 파일 18개의 Material 열을
   실제로 읽어(헤더 문구로 열을 찾아) 많이 쓰인 순서로 17개를 추렸다.
   `core/session.py`에서 `rates` 저장·복원을 **제거**했다 — 설정창이 단가의 주인인데 세션에도
   담아 두면 프로그램을 켤 때 옛 세션 값이 새 설정을 덮어쓴다.
2. **설정창** (`ui/settings_dialog.py`, 요청 6번) — 탭 3개(공정 단가 / Material 목록 /
   회사·견적서 양식). Material은 목록에서 여러 개를 골라 한 번에 지울 수 있고, 입력칸에서
   Enter로 바로 추가된다. 저장하면 `app.apply_settings()`가 단가·목록·회사 정보를 즉시
   전체에 적용하고 표의 금액을 다시 계산한다. 견적서 상단은 라벨(`담당자 :` 등)을 뺀
   알맹이만 입력받고 양식 문구는 저장할 때 코드가 붙인다.
3. **현황판** (`ui/table.py`, `ui/dashboard.py`, 요청 1번) — 맨 앞에 화면 순번 `No` 열,
   품명과 소재 사이에 `Comment` 열을 넣었다. 표시 순번(`display_no`)은 화면 차례이고
   내부 키 `item["no"]`(선택·팝업·슬롯 매핑)는 그대로 둔다. 정렬은 `added_at` 내림차순에
   카드 번호 내림차순을 2차 키로 더해, 한 번에 여러 건을 올려 시각이 같아도 순서가 고정된다.
   Comment의 줄바꿈은 표에서 한 줄로 접어(`one_line`) 행 높이가 깨지지 않게 했다.
4. **선택 동작** (요청 2번) — 클릭은 그 행만 선택, `Ctrl+클릭`은 더하고 빼기,
   `Shift+클릭`은 기준 행부터 범위 선택, 카드 열기는 **더블클릭**으로 옮겼다. 범위는
   저장 순서가 아니라 화면에 보이는 순서(`visible_nos`) 기준이다.
   **기존 버그도 함께 고쳤다**: `ui/widgets.py`의 체크박스가 만들 때의 상태(`checked`, 항상
   False)를 클로저에 담고 있어 어느 행을 눌러도 늘 "켜기"만 전달됐다(슬롯 재사용 구조라
   체크 해제가 안 됐다). 상태 판단을 현재 선택을 아는 대시보드로 옮겼다.
5. **카드 입력창** (`ui/popup.py`, 요청 5번) — Comment를 `tk.Text` 여러 줄로 바꾸고 아래
   손잡이를 끌어 높이를 늘릴 수 있게 했다(2~20줄). Material은 설정창 목록 콤보박스(목록에
   없는 소재는 직접 입력 가능)로 바꾸고, 그 아래 `열처리` 체크박스를 뒀다. 체크하면 바로
   아래 `HRC [Min] ~ [Max]` 칸이 활성화된다. 공정 단가는 입력칸에서 읽기 전용 표시로 바꿨다.
   정보 타일에서 기계 시트 폴더·파일과 엑셀 행을 뺐고 `NO`는 화면 순번을 보여 준다.
6. **엑셀 출력** (`core/excel_io.py`, 요청 3·7번) — 저장할 때마다 기계 시트 푸터
   (합계행 +3/+4/+5 = 회사명 / `yyyy-mm-dd` / 직위+작성자)와 견적서 상단 9칸을 설정값으로
   채운다. 견적서 날짜 칸(B7)은 `=기계!Q##` 참조인데, 항목이 늘면 푸터가 밀리므로 **참조도
   실제 푸터 행으로 다시 쓴다**(양식에 박혀 있던 `=기계!Q18`을 두면 20건만 넘어도 빈 칸을
   가리킨다). 단가·최종단가 열 폭을 15로 넓혀 `####`을 없앴다(서식은 그대로 뒀다).
   열처리를 체크한 항목은 Material 값 뒤에 `HRC58~62`를 붙여 적고, 다시 읽을 때
   `split_material()`이 소재와 경도로 되돌린다.
7. **기능 제거** (요청 3-1, 4번) — 보조기능의 `신규 입력 다운로드`, 헤더의 `날짜별 누적 저장`,
   PDF 출력 전체(`core/pdf_export.py`, 출력 형식 선택창)를 걷어냈다. 누적 저장이 사라져
   `save_pending`(NEW 배지)을 해제할 곳이 없어지므로 **선택 다운로드 성공 시 해제**로 옮겼다.
   `write_items_to_sheet()`의 행 재사용 로직(`excel_row`/`excel_file`)과 `paths.get_estimate_root_dir()`도
   쓰이지 않게 되어 함께 정리했다.
8. **아이콘** — `estimate.ico`를 16/32/48은 DIB, 256은 PNG인 표준 조합으로 다시 만들었다
   (외부 라이브러리 없이 zlib만으로 PNG를 풀어 DIB로 다시 담았고, 그림 자체는 그대로다).
   `installer/Setup.iss`는 `estimate.ico`를 `{app}`에 따로 설치하고 바로가기가 그 파일을
   직접 가리키게 했으며(`IconFilename`), 기존 바로가기를 지우고 새로 만들고,
   `ChangesAssociations=yes`와 `ie4uinit.exe -show`로 셸 아이콘 캐시 갱신을 유도한다.
   `desktopicon` 작업의 `unchecked`를 없앴다 — `InstallDelete`는 작업 선택과 무관하게 도는데
   체크를 안 하면 지우기만 하고 새로 안 만들어 아이콘이 아예 사라지기 때문이다.
9. **화면 배율(DPI) 대응 수정** — 고배율 화면에서 헤더 세 줄(제목/요약/안내)의 y 좌표가
   112px 안에 고정돼 있어 글꼴만 커지면서 서로 겹쳤다. 실제 글꼴 높이를 재서 줄을 쌓고
   캔버스 높이도 거기에 맞춘다. 팝업·설정창도 화면 안에서 잘리지 않도록 크기와 열 비중을
   다시 잡았다(저장 버튼 줄을 먼저 고정, 오른쪽 공정표에 최소 폭 확보, 긴 안내문에 wraplength).
10. 버전을 `estimate_app/__init__.py`와 `installer/Setup.iss` 양쪽 모두 `0.0.9`로 올렸다.

### 검증 결과

- **문법**: `python -m compileall estimate_app main.py` 통과.
- **엑셀 출력(3 / 7 / 20 / 45건)**: 실제 코드 경로(`excel_io.export_items`)로 파일을 만들어
  셀을 직접 읽어 확인했다. 기계 푸터 회사명 `(주)텍스타` / 날짜 `2026-08-09` / 작성자
  `생산부장 황성문`이 항목 수에 따라 밀린 자리(Q17→Q30→Q56)에 정확히 들어갔고, 견적서
  상단 9칸이 모두 설정값으로 채워졌으며, 날짜 참조도 `=기계!Q18 / Q31 / Q56`으로 실제
  푸터 행을 따라갔다. 단가·최종단가 열 폭 15 적용, 견적서 항목 참조 수는 3/7/20/45건 모두
  건수와 일치했다.
- **`####` 원인 재확인**: 고치기 전 상태에서 기계 X열(최종단가) 폭이 9.0, W열 12.0이고
  서식이 회계 서식이라 백만 단위부터 잘리는 것을 확인한 뒤 폭을 넓혔다.
- **HRC 왕복**: 열처리를 켠 카드를 저장한 파일을 다시 읽어 소재/열처리/Min/Max가 그대로
  복원되는 것과, 켜지 않은 카드가 영향받지 않는 것을 확인했다. `split_material()` 경계
  6가지(`HRC58~62`, `HRC 58 ~ 62`, `HRC58`, 경도 없음, 빈 값, `AL6061-T6`처럼 숫자·하이픈이
  섞인 소재명)도 확인했다.
- **정렬**: 같은 초에 올라온 5건이 카드 번호 내림차순으로 고정되고, 시각이 다른 카드가
  맨 위로 오는 것을 확인했다.
- **설정 저장/복원**: 저장 후 다시 읽어 회사명·작성자 조립(`부장 홍길동`)과 Material의
  중복·공백 제거를 확인했고, 검증 뒤 원래 값으로 되돌렸다.
- **세션에 단가가 없음**: 저장된 JSON에 `rates` 키가 없고 `session.load()` 결과에도 없음을 확인했다.
- **화면**: 실제 창을 띄워 `PrintWindow`로 창별 캡처를 떠 확인했다(백그라운드 세션이라
  화면 전체 캡처로는 창이 뒤에 있어 잡히지 않았고, DPI 인식을 안 하면 캡처가 잘린다는 것도
  같이 확인했다). 현황판(맨 앞 No, 품명↔소재 사이 Comment, 최신 카드가 맨 위, 선택 행
  틴트, 헤더 버튼이 `기계 시트 업로드 / 새 항목 추가 / 설정`으로 바뀐 것), 카드 입력창
  (NO 순번, Material 콤보, 열처리 체크 시 HRC 활성, 여러 줄 Comment, 읽기 전용 단가와
  금액, 하단 저장 버튼), 설정창 3개 탭을 모두 눈으로 확인했다. 표의 열 폭도 실측해
  합계 1809px로 창(1824px) 안에 들어가는 것을 확인했다.
- **빌드**: `python -m PyInstaller Machine_Estimate.spec` 성공. `dist/Estimate/Estimate.exe`
  2,832,274바이트, `_internal/assets`에 새 `estimate.ico`(19,341바이트)와 견적 양식이 포함된
  것을 확인했다. 빌드된 exe를 직접 실행해 창 제목이 `Estimate(견적) v0.0.9`로 뜨는 것과
  exe에 박힌 아이콘이 파란 `E`인 것을 확인했다.
- **설치 파일**: `MachineEstimate_Setup_v0.0.9.exe` 9,575,826바이트 생성(경고 없이 컴파일).

### 미실행 항목

- **관리자 권한으로 실제 설치해 바탕화면 아이콘이 바뀌는지는 확인하지 못했다.** 이번 작업은
  "exe·바로가기·아이콘 파일 쪽에서 할 수 있는 것은 다 했다"까지다. 설치 후에도 예전 아이콘이
  남아 있으면 셸 캐시가 원인이므로, 명령 프롬프트에서 `ie4uinit.exe -show`를 실행하거나
  탐색기를 다시 시작하면 즉시 갱신된다.
- `####` 해소는 열 폭을 15로 넓힌 것까지 확인했고, 엑셀이 실제로 그리는 화면은 확인하지
  못했다(openpyxl로는 렌더링을 볼 수 없다).
- 마우스로 직접 Shift/Ctrl 클릭하거나 더블클릭하는 상호작용은 이전 버전들과 같은 이유
  (백그라운드 세션·화면 배율)로 실측하지 못했다. 코드 경로와 캡처로 대신 확인했다.
- `v0.0.7_plan.md` / `v0.0.8_plan.md`는 과거 기록이라 손대지 않았다. 이번 계획과 결과만
  이 파일에 통합했다.
- 기존 `견적_산정` 폴더의 과거 파일과 양식 원본(`견적용.xlsx`)은 손대지 않았다.
  열 폭·상단 문구는 저장할 때 출력 파일에만 적용된다.
